<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".PinkHouseApplication"
        android:allowBackup="true"
        android:icon="@drawable/pink_house_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/pink_house_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PinkHouse">
        <receiver
            android:name=".core.notification.DepositReminderReceiver"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PinkHouse.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
