# 主题皮肤重构方案（历史主题下线说明）

原首批主题包已按产品决策下线：商店不再展示，也不再允许购买或启用；对应图片资源和源素材目录已移除。

当前主题皮肤架构、slot 列表、素材入库规则和验收流程以 `docs/THEME_SKIN_REPLICATION_BEST_PRACTICES.md` 以及后续迁入的 `tools/` / 正式 docs 为准。`temp/_harness/MANIFEST.yaml`、`EXEC_PLAN*.md`、`ACCEPT_PLAN*.md` 仅作为历史 harness / 迁移前参考；删除 `temp` 前，仍有效的主题元数据、素材清单、导入脚本、验收规则和 PSD/image2 说明需要先迁入正式位置。

兼容约束：历史商品的静态定义只作为旧本地归档解码参考保留，不要重新注册到 `ThemeSkinManager.products`，也不要恢复已删除素材。不得修改 `ThemeSkinSlot.rawValue`、`theme_skin.owned` / `theme_skin.active_selection` 持久化 key 或 18 个 slot 数量；默认主题不得泄漏主题装饰。
