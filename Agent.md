# Agent.md — 少女心愿（Pink House）Agent 与开发者总规范

> 本文件是**给人看的完整规范**（架构 / 业务开发 / 审查 / 提交）。
> `AGENTS.md` 由 Codex 自动注入，是**本机事实的机器清单**（iCloud 仓库路径、Xcode beta 命令、模拟器命令）。
> 冲突时的裁决：
> - 本机事实类（路径、命令、SDK 版本）→ 以 `AGENTS.md` 为准；
> - 规范流程类（分层、命名、审查、提交）→ **以本文件为准**。
>
> ⚠️ 动工前先确认一件事：`AGENTS.md` 声明的 Swift/Xcode 源仓库在 iCloud（`/Users/muniao/Library/Mobile Documents/...`），而本工作区是 `/Users/sangyu/develop/Pink_House`。**两者谁是本次开发的目标仓库，必须先向用户确认再动手**，不要凭猜测往 iCloud 路径写文件。

---

## 0. 硬性约束（Hard Constraints）

以下条目**无例外**。任何 Agent、任何 IDE、任何阶段都不得绕过。

| # | 约束 | 判定标准 |
| :-- | :-- | :-- |
| **H1** | **遇到不懂必须联网搜索** | 见下方 0.1 详述 |
| **H2** | **禁止 force push** | 不得出现 `git push -f` / `--force-with-lease` / 强推覆盖远程分支 |
| **H3** | **禁止跳过 hook** | 不得使用 `--no-verify`、`--no-gpg-sign` |
| **H4** | **代码第一事实** | 文档与代码冲突时，以代码为准，并同步修订文档 |
| **H5** | **中文交流 + 中文提交** | 对话、注释、提交信息一律中文；提交信息一句话说清楚 |
| **H6** | **单文件 ≤ 500 行** | 超限必须拆分或给出拆分计划 |
| **H7** | **改完必须验证** | 至少编译通过；能跑测试的跑测试；UI 改动按验收图基线核对 |
| **H8** | **不编造** | 不知道就说不知道，然后去搜、去读代码、去问人 |
| **H9** | **不动用户未授权的删除** | 个人目录 / `temp/` / 历史文件，未经明确确认不删 |

### 0.1 H1：遇到不懂必须联网搜索（最高优先级）

**规则**：在以下任一情形出现时，**必须先联网搜索或查阅官方文档，再动手写代码**。

1. 用到不确定的系统 API、框架行为、SDK 变更（SwiftUI / SwiftData / CloudKit / StoreKit / RealityKit / iOS 版本差异）。
2. 遇到不认识的第三方库、命令行工具、构建参数。
3. 报错信息看不懂，或修复方案是"记忆里的"。
4. 涉及平台审核规则、隐私合规、支付规范、地图 / 位置数据合规等外部规则。
5. 项目里出现从未见过的自定义框架（如 `ThemeSkin`、`GaussianSplatting`、`PetChat`），且无文档说明。
6. 需要给出"最佳实践""性能对比""版本差异"类结论。

**执行方式**：

```
不确定 → 先搜索（WebSearch / 官方文档 / Apple Developer / 仓库内 docs/）
       → 交叉验证至少 1 个权威来源
       → 把结论和来源写进回复或文档
       → 才动手写代码
```

**明令禁止**：

- ❌ "我记得应该是这样" → 直接写
- ❌ 凭训练记忆编造 API 签名、参数名、回调时机
- ❌ 搜索失败就放弃，改为猜测实现

**搜索失效时的降级顺序**（不能跳过前一步）：

1. 联网搜索（首选）
2. 读本仓库 `docs/`（优先最新修改的）
3. 读源码中同类实现的现有写法
4. 向用户提问，并说明"我已查过 X、Y，仍未确认 Z"

> 一句话：**记忆用来提效，不用来当依据。**

### 0.2 额外的仓库级硬约束（沿用 AGENTS.md）

- `ThemeSkinSlot.rawValue` 不改（持久化兼容）
- `ThemeSkinManager` 存档 key 不改：`theme_skin.owned` / `theme_skin.active_selection`
- 主题素材必须进 `Assets.xcassets/ThemeSkin/<namespace>/`，不走 Bundle 文件路径
- 三件套 Components（Home / TabBar / Wardrobe）的程序化绘制兜底**必须保留**
- 主题未启用时，禁止任何主题装饰泄漏到默认皮肤
- 不允许跨主题混搭组件
- slot 数量固定 **18** 个，禁止增减
- 旧下线主题商品的静态定义不改，但不得重新注册进商店列表

---

## 1. 架构规范

### 1.1 分层与依赖方向

```text
┌──────────────────────────────────────────────────────┐
│  Views/          页面与组件（SwiftUI）                │
│  ├ MainTabView（容器 + 自绘 TabBar）                  │
│  ├ Wardrobe / OOTD / Pet / PetChat / Wealth / …      │
│  └ DesignSystem / Components（复用件）                │
└───────────────▲──────────────────────────────────────┘
                │ 单向依赖
┌───────────────┴──────────────────────────────────────┐
│  ViewModels/      视图状态（@Observable）             │
└───────────────▲──────────────────────────────────────┘
                │
┌───────────────┴──────────────────────────────────────┐
│  Services/        业务能力（单例 Manager / Service）  │
│  ├ AI/  IAP/  PetChat/  SkirtMarket/  ThemeSkin/      │
│  ├ 同步：iCloudSyncManager / CloudSyncManager          │
│  └ 基建：BackupService / NotificationManager / …       │
└───────────────▲──────────────────────────────────────┘
                │
┌───────────────┴──────────────────────────────────────┐
│  Models/ + SwiftData  @Model + ModelContext           │
│  Utilities/  Utils/  Rendering/（无业务状态的纯工具）  │
└──────────────────────────────────────────────────────┘
```

**依赖铁律**

| 规则 | 说明 |
| :-- | :-- |
| 单向向下 | `Views → ViewModels → Services → Models`。**禁止反向依赖**（Model 里不得 import 业务 Service） |
| 跨模块走 Service | 模块 A 要用模块 B 的能力，走 B 的 Service/Manager，不要直接 import B 的 View |
| 单例仅限无状态容器 | `xxx.shared` 只允许用于全局无状态服务（配置、网络、日志、主题），有状态的业务数据必须注入 |
| 工具层纯净 | `Utilities/` 不得反向依赖 `Views/` 或业务 `Services/` |
| 视图不写业务规则 | 价格校验、状态机、权限判定一律下沉到 Service；View 只做展示与事件转发 |

### 1.2 目录职责（速查，详见 `docs/目录结构说明.md`）

| 目录 | 放什么 | 不放什么 |
| :-- | :-- | :-- |
| `ItemManager/Models/` | `@Model` 数据模型、枚举配置 | 业务逻辑 |
| `ItemManager/Views/<模块>/` | 该模块的页面与私有组件 | 跨模块通用组件（放 `Views/Components/`） |
| `ItemManager/Services/` | 可复用的业务能力、系统能力封装 | UI |
| `ItemManager/ViewModels/` | 跨页面共享的视图状态 | 一次性页面状态（用 `@State`） |
| `ItemManager/Utilities/` `Utils/` | 无业务状态的工具与扩展 | 业务规则 |
| `ItemManager/Rendering/` | 图片加载、Metal、图表渲染 | 业务判断 |
| `ItemManager/Assets.xcassets/` | **所有**图片/颜色/主题素材 | — |
| `docs/` | 架构、规范、方案、验收记录 | 代码 |
| `tools/` `scripts/` | 可复用脚本与工具 | 一次性临时脚本（放 `temp/`） |
| `temp/` | 临时输入与历史归档 | **长期 source of truth**（见 `docs/TEMP_GOVERNANCE_AND_KNOWLEDGE_MIGRATION.md`） |

### 1.3 数据层规范

| 主题 | 规范 |
| :-- | :-- |
| 持久化 | 主用 SwiftData；新增 `@Model` 必须登记进 `ModelContainer` |
| 迁移 | 任何模型字段增删，必须写迁移并走 `SwiftDataMigrationManager`；**不得裸改 `@Model`** |
| 云同步 | CloudKit 写操作必须幂等、可重放；敏感字段不进 Public DB |
| 图片 | 大图落沙盒 + 缩略图缓存；禁止把图片二进制塞进 SwiftData |
| 备份 | 新增可持久化数据时，同步更新 `BackupService` 的导出/导入契约 |
| 软删除 | 列表默认过滤 `deletedAt == nil`，硬删除仅用于回收站清空 |
| 并发 | SwiftData 的 `ModelContext` 操作回到主线程；耗时计算放后台但结果回主线程落地 |

### 1.4 已知技术债与新增代码的红线

| 债 | 现状 | 新增代码怎么做 |
| :-- | :-- | :-- |
| 大文件 | 39 个 > 1000 行（最大 `NewbieGuideManager.swift` 4162 行） | 新增文件严格 ≤ 500 行；改动大文件时顺手拆出一块，并在提交信息注明 |
| 根目录散落模型 | `Item.swift` / `Clothing.swift` / `Tag.swift` / `Brand.swift` 在 `ItemManager/` 根 | 新模型一律进 `Models/`；**不批量迁移旧文件**（改动面太大，风险高） |
| 新旧并存 | `PetChatView.swift` vs `PetChatViewLegacy.swift` | 新功能只改新版；Legacy 只修 bug，不新增能力 |
| 双同步实现 | `CloudSyncManager` 与 `iCloudSyncManager` 并存 | 动同步前先确认该模块用哪个，禁止两边都写 |
| 历史文档过期 | Godot / CoreData 相关描述已失效 | 读到冲突立刻标注并修订，不要照抄 |

---

## 2. 业务开发规范

### 2.1 接到需求的固定流程

```text
① 拆解需求（docs/业务拆解方法.md）
   └ 反复追问真实业务目的 → 一句话通俗方案 → 用户确认
② 定位落点
   └ 哪个 Tab / 哪个模块 / 哪个 Service / 哪个 Model
③ 写方案
   └ 影响面 + 数据变更 + 迁移方案 + 回滚方案（≥ 中等改动必写）
④ 编码
   └ 遵循本文件第 1、2 章
⑤ 自验
   └ 编译 → 单测 → 模拟器按验收图基线核对
⑥ 阶段审查（第 3 章）
   └ 派子代理四项审查 → 通过
⑦ 提交（第 4 章）
   └ git add → 中文一句话 commit
```

**规模门槛**（决定跳过哪些步骤）：

| 规模 | 判定 | 必做 |
| :-- | :-- | :-- |
| 小 | 单文件内、无数据变更、无外部依赖 | ①⑤⑦ |
| 中 | 多文件、或新增字段、或新增第三方能力 | ①②③⑤⑥⑦ |
| 大 | 新模块、或改数据模型、或动支付/同步/迁移 | 全部 ①~⑦，**且先出书面方案待确认** |

### 2.2 编码规范

| 项 | 规范 |
| :-- | :-- |
| 文件长度 | ≤ 500 行；超限按「视图 / 子视图 / 组件 / 逻辑」拆分 |
| 命名 | 类型 `UpperCamelCase`，成员 `lowerCamelCase`；视图后缀 `View`，服务后缀 `Service`/`Manager`，模型无后缀 |
| 注释 | **中文，写「为什么」**；不写复述代码的废话注释 |
| 魔法数字 | 业务可调整的参数一律提取为具名常量（策划会改），禁止散落字面量 |
| 本地化 | 所有用户可见文案走 `appLocalized`，**禁止硬编码中文**；新增语言目录同步补齐 |
| 空状态 | 列表 / 统计组件必须处理无数据（`hasData` + 友好文案） |
| 错误 | 不静默吞异常；用户可见失败要有提示，技术细节进 `Logger` |
| 权限 | 相册 / 相机 / 通知 / 位置，先申请再使用，被拒要有降级路径 |
| 主题 | 颜色取 `ThemeManager`，禁止写死 hex；新增主题装饰不得泄漏到未启用状态 |
| 性能 | 列表用 `LazyVStack`/`LazyVGrid`；图片异步 + 降采样；避免在 `body` 里做重计算 |
| 可测性 | 纯逻辑抽成无 UI 依赖的函数/类型，便于单测 |

### 2.3 SwiftUI 约定（沿用项目既有模式）

- 折叠面板：`@State private var expanded: Bool` + `withAnimation`，折叠态显示摘要而非空白。
- 「最近一条数据」：`items.first(where: { $0.date >= now }) ?? items.last`。
- 复用组件：用**命名元组**传参，如 `(month: Int, count: Int, hasData: Bool)`。
- 导航：统一由 `MainTabView` 容器调度，模块内不自建第二套 Tab。
- 底部异形 TabBar 的避让用 `customBottomNavigationAvoidanceInset`，不要硬编码高度。

### 2.4 禁止清单

- ❌ 在 View 里直接写网络请求、CloudKit 查询、StoreKit 交易
- ❌ 直接改 `@Model` 而不写迁移
- ❌ 硬编码文案、颜色、尺寸、价格、档位
- ❌ 复制一份 Legacy 文件改造（应改新版）
- ❌ 为"以后可能用到"提前写抽象（YAGNI）
- ❌ 一次提交混入无关改动（重构 + 新功能 + 格式化）

---

## 3. 阶段审查：派子代理（每阶段必做）

**触发时机**：每完成一个阶段任务，在 `git commit` **之前**。审查不通过 → 修复 → 重新审查，**不允许带问题提交**。

### 3.1 四项审查

| # | 审查项 | 核心问题 | 不通过典型 |
| :-- | :-- | :-- | :-- |
| **R1** | **代码是否满足需求** | 需求点逐条对照，是否都实现了？有没有多做？边界/异常/空态是否覆盖？ | 漏实现、多做了需求外功能、异常分支缺失 |
| **R2** | **代码是否满足技术规范** | 是否符合第 1、2 章？≤500 行？无硬编码？注释合规？无反向依赖？ | 超限文件、硬编码文案、Model 依赖 Service |
| **R3** | **技术文档是否符合架构** | 新增/修改的文档是否与代码一致？是否同步更新了受影响的既有文档？ | 文档描述新接口但代码没改、架构图与实际分层不符 |
| **R4** | **git 环境是否正确** | 分支对不对？有无误提交（密钥、大文件、`temp/`、`.env`）？工作区是否干净？有无未跟踪的必要文件？ | 在 `main` 上直接改、把 `.env` 加进暂存区 |

### 3.2 子代理派发方式

给子代理的 prompt **必须自包含**（它看不到当前对话），至少包含：

1. 仓库绝对路径
2. 本阶段做了什么（文件清单 + 一句话目的）
3. 原需求原文（或需求文档路径）
4. 要它执行 R1~R4，并按 `docs/阶段审查清单.md` 逐项给「通过 / 不通过 + 证据」
5. 明确：**只读审查，不改代码**（或授权它修小问题，二选一，说清楚）
6. 输出格式：结论 + 问题清单（文件:行号 + 问题描述 + 建议修法）

### 3.3 审查结论处理

| 结论 | 动作 |
| :-- | :-- |
| 四项全通过 | 进入第 4 章提交 |
| 有不通过项 | 修复 → 重新派审（只复审失败项 + 受影响项） |
| 争议项 | 记录进 `MEMORY.md` 的「待裁决」，并向用户说明后按用户裁决执行 |

---

## 4. Git 与提交规范

### 4.1 提交信息

**格式**：`<type>: <中文一句话>`

**要求**：中文、言简意赅、一句话说清楚做了什么，**不写过程、不写"修改了某某文件"**。

| type | 用途 |
| :-- | :-- |
| `feat` | 新功能 |
| `fix` | 修 bug |
| `refactor` | 重构（无功能变化） |
| `perf` | 性能优化 |
| `docs` | 仅文档 |
| `test` | 仅测试 |
| `chore` | 构建 / 配置 / 依赖 |

示例：

```
feat: 心愿尾款新增按月汇总视图
fix: 修复衣橱统计把定金重复计入总价的问题
refactor: 拆分 PetChatView 的消息气泡组件
```

### 4.2 提交前自检

```bash
git status                      # 确认分支与暂存内容
git diff --stat                 # 确认改动范围
git diff --name-only            # 确认无 .env / 大文件 / temp/
git log --oneline -5            # 确认提交信息风格一致
```

**提交动作**：

```bash
git add <明确的文件路径>          # 禁止 git add -A / git add .
git commit -m "feat: 中文一句话"
```

### 4.3 分支

- 主分支 `main`；功能分支命名：`codex/<主题>`、`cursor/<主题>`、`主题名`。
- **禁止 force push**，**禁止 `--no-verify`**。
- 合并前确保编译通过 + 相关单测通过。

---

## 5. 业务拆解方法（给初级 PM / 需求同学）

完整方法论见 **[docs/业务拆解方法.md](docs/业务拆解方法.md)**。核心四步：

```text
① 听懂表面需求  →  ② 追问真实目的（至少 3 轮"为什么"）
                 ↓
④ 用表格+图对齐  ←  ③ 一句话通俗方案（非技术背景能听懂）
```

**一句话方案模板**：

> 谁（角色）在什么场景下，因为什么问题，现在要做什么，做完后能达到什么可衡量的结果。

**示例**：

> 买了预售裙的姑娘，在月底看账时，因为记不清哪条裙子还没补尾款、要补多少，现在打开「心愿尾款」就能按月份看到待付清单和总金额，做完后不用再翻聊天记录，也不会漏掉补款 deadline。

---

## 6. 快速参考

| 我要… | 去哪 |
| :-- | :-- |
| 了解项目全貌 | [README.md](README.md) |
| 写业务代码 | 本文件第 1、2 章 |
| 拆解一个需求 | [docs/业务拆解方法.md](docs/业务拆解方法.md) |
| 审查一个阶段 | [docs/阶段审查清单.md](docs/阶段审查清单.md) |
| 查目录该放什么 | [docs/目录结构说明.md](docs/目录结构说明.md) |
| 查业务黑话 | `docs/PROJECT_CONCEPTS.md` |
| 查历史踩坑 | [MEMORY.md](MEMORY.md) |
| 做主题皮肤 | `docs/少女衣橱主题皮肤重构方案.md` + `docs/THEME_SKIN_REPLICATION_BEST_PRACTICES.md` |
| 清理 temp | `docs/TEMP_GOVERNANCE_AND_KNOWLEDGE_MIGRATION.md` |
