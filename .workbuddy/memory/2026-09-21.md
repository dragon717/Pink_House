# 2026-09-21

## 时光馆重构方案 V2.0（docs/时光馆_店家上新模式重构实现方案.md）

- 用户提供《少女心愿‑店家上新/历年系列 V1.1‑Dev 开发文档》，要求对时光馆做与少女心愿模式一致的重构方案。
- 代码实证结论：
  - ShopCatalog（Models/Store/Views/Ops/WardrobeInserter）已完整落地七实体模型 + 用户端四页 + 最小运营闭环；`Clothing.swift:150-152` 已有 catalogProductID/catalogVariantID/catalogSaleEventID 三引用字段。
  - 时光馆现状：TimeHallView.swift 3469 行巨石（4 模式），店家上新目前是 EntryCard→fullScreenCover 挂在时光馆内；Rebuild/ 目录是半成品（camelCase vs kebab-case 品牌身份错位）。
  - V1.1 P0 缺口（V1.0 方案曾过度乐观写"零新增"）：批量多单品录入、单品粒度审核（部分过/驳）、店家/系列/商品编辑+引用保护删除归档、实体归档标记（Catalog 实体无 archivedAt 字段）、草稿缺 images/variants/sizeChart 字段。
- V2.0 方案关键决策：
  - CatalogShop/Series/Product 各加 `archivedAt: Date?`（decodeIfPresent 向后兼容），查询层在 ShopCatalogStore 统一过滤。
  - 覆盖层合并规则升级：同 id 实体整体替换（支持编辑已发布实体），saleEvents 永远只追加。
  - 新增 CatalogBatchEntrySession（批次录入）+ ShopCatalogReferenceGuard（引用保护）。
  - 分阶段：P0 基线 → P1 首屏切换 → P2 运营端补齐（核心增量）→ P3 数据迁移 → P4 旧管道下线 → P5 清理 → P6 云分发（复用 THRelease/THDataPack 协议）。

## 重构实施落地（同日晚间，Phase 0–3 完成）

- 分支 feature/timehall-shop-catalog-rebuild，5 个 commit：checkpoint → Phase 1 → 测试修复 → Phase 2 → Phase 3 种子合并。
- Phase 1：TimeHallView 首屏直挂 ShopCatalogBrowseView（onLegacyArchive 参数替换左上角关闭按钮为「馆藏档案」）；`.toolbar(.hidden)` 必须只挂 legacyRoot，否则藏掉新首屏导航栏（踩坑）。
- Phase 2 落地：CatalogShop/Series/Product + archivedAt（decodeIfPresent）；Store 合并规则同 id 替换 + SaleEvent 只追加；查询层归档过滤 + isSeedShop/Series/Product 判定；CatalogProductDraft + batchID/images/variants/sizeChart；parseBatch 按 URL 切分逐条容错；CatalogBatchEntrySession 持久化 batches.json；submitBatch/review（状态机补 submitted→draft 驳回）；ShopCatalogReferenceGuard（Clothing.catalogProductID 命中含软删除即禁止物理删除）；OpsManageView（编辑/归档/删除 UI）。
- Phase 3：tools/timehall_migration/（Python，6 单测）；--scope catalogue（152KB 画册年鉴）已并入 shop-catalog.json（10 店家/40 系列/109 商品/161KB）；--scope full（30MB 全量在售）留作 THDataPack 云分发。
- 关键坑：①测试 ModelContainer 局部变量被回收 → save 抛 No eligible connection available（NSException，try/catch 拦不住），容器必须保活；②内存容器测试里避免 context.save()（同 context fetch 含 pending changes 即可）；③种子实体物理删除不可能（Bundle 只读），用 isSeed* 判定退化为归档。
- 验证：ShopCatalog 测试族 37/37 绿；UITest TimeHallShopCatalogFirstScreenUITests 绿；Python 迁移 6/6 绿。
- 剩余：Phase 4/5 旧管道下线（有意推迟，等 treasured.v1→心愿运行时迁移方案补齐）；Phase 6 云分发可选。

## 收藏迁移补齐（同日深夜，Phase 4/5 前置闭环）

- 用户确认口径：treasured.v1 → 心愿按 **PriceMode.wishlist**（isDepositPlan=true、定金 0、全款待付）+ 自动一次性触发；方案 §5.6 初稿的 isDepositPlan=false 与运行时「已拥有」判定冲突，已在文档更正。
- 实现：TimeHallTreasuredWishMigrator（Services/ShopCatalog），挂 TimeHallView.task；匹配两级：th-<itemID> 直查 → 旧馆种子 item→productCode 二级解析；幂等双保险 = UserDefaults 标记键 timeHall.treasured.wishMigration.v1 + 逐条 catalogProductID 护栏；未匹配留旧馆归档，treasured.v1 永不删改。
- 测试坑：UserDefaults suite 跨用例残留（load() 并集污染）→ setUp/tearDown 整域清 v1/v2/marker；#Predicate 内不能访问对象属性（product.id），必须先捕获局部变量。
- 验证：迁移测试 5/5 绿；ShopCatalog 全族 + 迁移 42/42 绿。
- Phase 6 评估结论：本轮不做——THDataPack 分发需真实运营需求与 CloudKit 侧配置，full 范围 30MB 包已备好（output/timehall_migration），接入点（ShopCatalogOpsView 导出整包）已留。
- 现状：Phase 4/5 前置已全部补齐，可随时执行；合回 main 待用户决定。

## Phase 4 拆分批次（同日凌晨）

- 用户确认节奏：先拆分保留入口（馆藏档案入口保留一个使用周期，全量下线等迁移数据验证）。
- TimeHallView.swift 3503 行 → 36 行壳层（首屏店家上新挂载 + 馆藏档案显隐 + Tab 复位 + 收藏迁移 .task）；旧四模式+品牌列表+商品详情整体迁入 Views/TimeHall/Legacy/TimeHallLegacyArchiveView.swift，逐字保留，仅剥离壳层职责（onClose 回调替代 showsLegacyArchive/showsShopCatalog 状态；fullScreenCover 店家上新与 isRoutePageActive 复位归壳层）。
- 拆分前先验证文件自包含（所有 TimeHall 顶层类型 0 外部引用），使机械拆分零风险；一次编译通过。
- TimeHall*Tests 5 个文件全是纯逻辑测试（无视图耦合），旧馆服役期全部保留；首屏冒烟 UITest 已有。
- 验证：BUILD SUCCEEDED 一次过；129 单测绿；首屏 UITest 绿；grep 验收 Legacy 符号零外泄。
- 剩余（等使用周期验证后）：移除馆藏档案入口、Midsummer/Rebuild 路由下线、TimeHall*Tests 旧管线用例退役、合回 main。

## V1.1 P0 缺口补齐（同日凌晨，MVP 审计驱动）

- 对照 V1.1 §6 MVP 9 条审计发现 2 缺口并补齐（commit 已提交）：
  ① 差价百分比：CatalogPriceArchive.stockOverReservationDeltaPercent（delta/预约价×100，预约价缺失或为 0 → nil）；详情页价格档案卡差额行追加 ±x.x%。
  ② 驳回原因：CatalogProductDraft.rejectReason（Optional 合成解码兜底）；review(_:approve:reason:) 驳回写入（trim 空白→nil），advance 到 submitted 统一清空；OpsView 驳回按钮改 alert+TextField（iOS16 原生支持），草稿行红字展示原因。
- 测试技巧：旧格式向后兼容测试用「编码当前对象 → JSONSerialization 剥离新键 → 解码」构造，避免手写 JSON 漏非可选默认值字段（合成解码缺 key 抛 keyNotFound）。
- 验证：ShopCatalogV11GapTests 7/7；全量回归 136/136 绿。
- 其余 7 条 MVP 标准审计已覆盖。剩余仍为：使用周期验证后下线旧馆（入口移除/路由退役/Phase 5 收口）+ 合回 main 待用户决定。

## 批次录入整批归属整合（同日凌晨，用户需求驱动）

- 用户需求：同一系列多单品整合到同一条链路展示，不要单品与链接一一对应的分散结构。经确认方向 = 运营端录入整合（用户端系列聚合本已存在：店家主页/上新首页卡片以系列为单位，系列详情页就是单一入口）。
- 根因：CatalogBatchEntrySession 早有整批同店同系列字段、createBatch 也会写入草稿，但 UI 从未提供设置入口，批次行不可点 → 运营只能逐条草稿手动补系列 = 分散结构根源。
- 落地：ShopCatalogBatchDetailView（整批归属区：既有/新建店家 + 既有/新建系列+年份季节，「应用到整批 N 条单品」；本批单品按系列分组展示）；DraftStore.applyBatchAttribution（同步会话+未发布草稿，published/archived 不回写）；OpsView 批次行 onTapGesture → sheet，标题尾缀「未指定归属」提示。
- 验证：ShopCatalogBatchAttributionTests 3/3（整批同步+会话持久化/发布条目跳过/同系列聚合发布=1 系列 3 单品多品类齐全）；全量回归 52/52 绿。

## 系列页点菜式改版（同日凌晨，用户需求驱动）

- 需求：系列对外只保留一个主链接入口（替代 N 张单品独立卡片）；点进为点菜式选购页（按 JSK/KC/小物/包 分区、明码标价、各带商品照、自由勾选合并加入）；规格选择图文绑定（选中规格切换对应照片）。
- 落地：① ShopCatalogSeriesView 重写 = 单张系列主卡（封面/名称/年份季节/N 个单品/品类汇总/价格区间/整体系列状态标），tap → sheet 点菜页；② 新文件 ShopCatalogSeriesMenuView：品类分区行（照片+现货/预约价+状态标+规格配色 chips+勾选圆圈），底部「已选 N 件·合计」→ 复用 WardrobeMergeView 管线，行内「>」保留商品详情次级入口（尺码表/销售历史完整可达）；③ 图文绑定数据链路：CatalogProductVariant + imageAssetID（Optional 合成解码兜底），运营 variantsText 三段式「颜色,尺码,图片」按 originalURL 匹配 images 行生成绑定，回读反向还原。
- 既有系列页多选模式/分类 chips 整体被点菜页吸收替代；UITest 首屏冒烟不进系列页，不受影响。
- 验证：BUILD SUCCEEDED 一次过；单测 54/54（含 variant 绑定向后兼容+回环 2 项）；首屏 UITest 绿。

## 配色图文批量绑定脚本（约 04:50–05:05）
- 新增 `scripts/bind_seed_variant_images.py`：种子 JSON variant.color × 商品 images asset originalURL 按归一化名称（忽略大小写/空白/分隔符/后缀）全等匹配，命中写 `variant.imageAssetID`。
- 警告覆盖：无法匹配 / 同名多图歧义 / 已绑定冲突（默认保留，--force 覆盖）/ 同商品配色归一化重名；幂等（已绑正确→skip，无变更不写盘，写回带 .bak）；摘要含成功/跳过/失败，有失败退出码 1。
- 合成种子验证 5 条路径全过（成功、跳过、歧义、冲突、force）。真实种子 285 条配色全无法匹配（文件名 archive-catalog-XX 不含配色词），核查日志在 temp/bind_seed_variant_warnings_20260921.log，种子文件未被改动。
- 文档 `docs/时光馆_店家上新模式重构实现方案.md` 新增 §12；后续配色实拍图按配色命名入库后重跑即可自动绑定。

## 点菜页缩略图大图预览（约 05:08–05:15）
- `ShopCatalogSeriesMenuView`：行内缩略图包成 Button（a11y「查看大图」）→ `viewerProduct: CatalogProduct?` → `.fullScreenCover(item:)` 弹 `ShopCatalogImageViewer`（复用商品详情大图查看器，黑底/滑动翻页/双指缩放/左上 xmark 关闭）。
- 起始页 = 缩略图当前展示图（选中配色绑定的照片，图文联动贯通到大图）；viewerReferences 取商品全部 images。页面原有布局/配色/勾选交互零改动。
- 验证：`xcodebuild -target ItemManager -sdk iphonesimulator`（skill 三开关绕沙箱）BUILD SUCCEEDED；未跑模拟器 UI 验收（纯增量 UI，复用既有组件）。

## 商品详情按钮按预约状态条件渲染（约 05:17–05:26）
- `ShopCatalogProductDetailView` 操作区重构：新增 `ShopCatalogPurchasePhase`（enum，文件级、纯逻辑可单测）+ `resolve(reservationStatuses:stockWindowOpen:hasStockPrice:hasReservationPrice:)`。优先级：预约中 > 预约未开始 > 预约已结束 > 现货 > neutral。
- 四态按钮策略：预约中→主按钮【加入心愿】+ 我已经预约；预约未开始→【加入心愿】（开售提醒语义，toast「开售时提醒你来买」）；现货在售→【加入少女衣橱】（无加入心愿）；预约已结束→置灰【预约已结束】标签 + hasStock 时引导加入衣橱、无现货仅提示。已有心愿/衣橱记录分支不变。
- 主按钮统一走 `primaryButton`（ThemeSkinPrimaryButtonStyle 同款样式）；新增 `ShopCatalogStore.SaleWindowStatus.isOpenLike`（open/ongoing）私有扩展。
- 新增 `ItemManagerTests/ShopCatalogPurchasePhaseTests.swift` 10 用例（四态+ongoing+优先级+兜底），only-testing 跑 TEST SUCCEEDED 10/10。验证边界：主 target 编译 + 该测试类，未跑 UI 验收。

## 路由收口：删除系列合并大卡中转页（约 05:36–05:45）
- 用户反馈：店页系列卡不应经过 SeriesView（大卡中转页）再进点菜页，SeriesView 冗余应删。
- `ShopCatalogShopView`：系列卡 NavigationLink→SeriesView 改为 Button→`menuSeries: CatalogSeries?`→`.sheet(item:)` 直达 `ShopCatalogSeriesMenuView`（.large detent，自带 NavigationStack/完成按钮，行内「>」仍可进商品详情）。
- `ShopCatalogSeriesView` 结构体整体删除（含合并大卡/封面回退/价格区间/状态标汇总逻辑）；全仓引用清零（仅文档描述更新）。`docs/时光馆_店家上新模式重构实现方案.md` 两处模块表同步。
- 主 target BUILD SUCCEEDED；未跑 UI 验收（纯路由改动）。

## 开售提醒真实通知（约 05:44–05:49）
- 新增 `Services/ShopCatalog/ShopCatalogSaleReminder.swift`：upcomingSaleStart（未来预约窗口最早 startAt 纯函数）+ schedule（授权检查→UNCalendarNotificationTrigger 注册，identifier 前缀 shop-catalog-sale-reminder-，幂等覆盖）+ cancel。
- `ShopCatalogProductView.addToWishlist(reminder:true)` 接入：成功入心愿后调度；未定档→toast「开售时间待公布」；权限拒→toast 引导去设置。通知点击暂不深链。
- 新增 ShopCatalogSaleReminderTests 8 用例（挑时间/幂等/文案），与 Phase 10 用例一起 only-testing 跑 18/18 绿。
- 待办剩：配色图入库重跑脚本、警告人工核查、旧馆下线 Phase 5、模拟器 UI 走查、合回 main。

## 开售提醒深链 + 取消闭环（约 10:52–11:00）
- 通知点击深链：NotificationManager.didReceive 识别 shop-catalog-sale-reminder → TabNavigationManager.navigateToShopCatalogProduct（新增 pendingShopCatalogProductID + 切 Tab 4）→ ShopCatalogBrowseView navigationDestination(isPresented:) 压 ShopCatalogProductView。沿用尾款通知深链模式。
- 提醒取消：DeleteTracker.applyDeletedClothings 软删心愿记录（isDepositPlan+catalogProductID）时调 ShopCatalogSaleReminder.cancel，覆盖本机+iCloud 同步删除。
- 主 target BUILD SUCCEEDED；回归 SaleReminder+PurchasePhase 18/18 绿。待办剩：配色图入库重跑脚本、警告人工核查、旧馆下线 Phase 5（等使用周期）、模拟器 UI 走查、合回 main。

## 模拟器 UI 走查（约 11:26–11:33）
- 新增 `ItemManagerUITests/ShopCatalogMenuFlowUITests.swift`：真实启动模拟器走完整链路——时光馆 tab → 店家列表 Alice Girl → 店家主页系列卡「雪国来信」直达点菜页（验证中转页已删）→ 缩略图「查看大图」→ 查看器「关闭大图」回点菜页 → 行内「>」进商品详情 → 完成回店家主页；6 张 keepAlways 截图。
- 两轮跑：首轮 71.6s 绿但「行内详情」走了兜底分支——锚点错（详情页导航标题是「商品详情」非商品名，截图证实页面已打开）；修正为硬断言后二轮 69.2s TEST SUCCEEDED。
- 截图归档 `output/shop_catalog_menuflow_uitest_20260921/`（00–05）。四项今日 UI 改动中三项已模拟器实证（直达路由/大图预览/详情可达）；四态按钮与深链为逻辑单测背书，通知深链需真机到点触发。
- 剩余待办不变：配色图入库重跑脚本、警告人工核查、旧馆下线 Phase 5（等使用周期）、合回 main（等用户拍板）。

## 今日工作提交收口（约 12:04–12:20）
- 全量单测回归（ItemManagerTests 全量，iPhone 18 Pro）：479 过 / 5 失败——失败全在 PetChatGuidanceEngineTests(2)+PetGenerativeUITests(3)，与验收技能记录的既有失败吻合且不在改动范围，判定无关。
- 分支 feature/timehall-shop-catalog-rebuild 新增 5 提交：f6fec1dc 批量绑定脚本 / c8b26aca 大图预览+路由收口 / f1a03390 四态按钮+开售提醒闭环 / 553a3155 UITest 走查 / c6482611 文档与记忆。工作区 clean。
- 分支合回 main 待用户拍板；旧馆下线 Phase 5 待使用周期；配色图入库等素材。

## 四态按钮 UI 实证补全（约 12:49–12:57）
- 新增 UITest 第二用例 testProductDetailActionButtonMatchesSalePhase：种子天然双态硬断言——JSK（预约 09-10~09-28）渲染【加入心愿】+【我已经预约】；单肩包（现货无截止）渲染【加入少女衣橱】且断言无【加入心愿】。TEST SUCCEEDED 67.5s，截图 10/11 归档 output/shop_catalog_menuflow_uitest_20260921/。
- 至此今日 UI 改动全部经模拟器实证（直达路由/大图预览/详情可达/四态按钮）；深链需真机通知触发。提交 b8c92fc8 + 893e85be。
- 方案文档内剩余项全部用户门控：合回 main（待拍板）、配色实拍图（等素材）、旧馆下线 Phase 4/5（等使用周期）、Phase 6 云分发（可选未来）。

## 合回 main（约 13:00–13:05，用户拍板）
- AskUserQuestion 确认「合回 main」→ main fast-forward 至 e70be575（23 提交，含此前本地未推的 Phase 2-6 提交一并推送）。
- main 上构建 sanity BUILD SUCCEEDED；git push origin main 成功（2e5a9366..e70be575），main 与 origin/main 同步。
- feature/timehall-shop-catalog-rebuild 分支保留未删；后续只剩配色实拍图入库（等素材）与旧馆下线 Phase 4/5（等使用周期）。

## 运营端完整能力对照审计（约 13:13，用户问「做到哪一环」）
- 用户未见运营端的原因：入口被 CreatorAccess 白名单门控（MeView「运营工具」，仅 isCreator 可见）。
- 需求文档 §4（=方案 Phase 2）逐条审计：4.1 批次录入/淘宝批量导入/批量提交/单品粒度审核 ✅；◐ 手动录入一次仅一条草稿（缺「同会话 +添加单品」）。4.2 三实体编辑/归档/引用保护删除 ✅；◐ UI 字段不全——Shop 缺 logo/封面/简介、Series 缺封面/简介（模型字段均已有，仅录入 UI 缺）；Product 已发布实体仅可编辑名称/分类，图片/配色尺码/尺码表/销售事件深度编辑 UI 缺（草稿编辑器只覆盖草稿）。4.3 状态流转 ✅。4.4 防重复（名称/别名匹配、同名现货追加 SaleEvent）✅。
- 待补三缺口：①已发布商品深度编辑 UI；②Shop/Series 编辑补 logo/封面/简介；③手动录入批量多单品。

## 运营端三缺口补齐（约 14:05–14:23，用户「补齐」）
- ①已发布商品深度编辑：新 `ShopCatalogProductDeepEditView` + `ShopCatalogDraftStore.updatePublishedProduct`（product+assets+variants+sizeChart 一次落盘，未变图片行复用原 asset id）+ `appendSaleEvent`（追加式，缺省尾款=价格−定金）。
- ②店家/系列编辑 alert→sheet，补 Logo/封面/简介字段。
- ③手动录入改批次会话（startManualBatch→BatchDetailView），批次页加「＋添加单品」+ 单品行 NavigationLink 进草稿编辑器；顺手清掉 showsTaobaoImport/showsManualDraft 死状态。
- 新 `ItemManagerTests/ShopCatalogOpsDeepEditTests` 4 用例（全字段替换/断链保护/追加式/白名单 viewer 注入——注意 setTestOverride(nil) 会回落真实判定）；运营回归 28 绿，主 target BUILD SUCCEEDED。

## 用户反馈双修复（约 16:36–16:50）
- ①发布系列不可见：根因 = 销售记录无日期→ongoing→currentSeries 不认→掉历年→无年份 chip→不可见。修复 currentSeries 把 ongoing 算当前上新 + ShopView 年份条补「未标年份」chip（YearFilter enum 重构 selectedYear）。
- ②图片上传：新 ShopCatalogImagePickerButton（PhotosPicker→落盘 1600px JPEG→local: 引用）+ ShopCatalogImageStore + Resolver local: 分支；接入草稿编辑器/深度编辑（商品图片 append、尺码表/价格表原图 replace）+ 店家 Logo/封面、系列封面。
- 测试 +4（可见性×2、图片回环+路径穿越拒绝），DeepEditTests+V11 共 17/17 绿。

## 「小狗仪仗队」系列消失根因排查与修复（约 16:53–17:20，c410b56a）
- 数据取证：模拟器（iPhone 18 Pro，唯一装 App 的设备）整台搜「小狗/仪仗队/小狗仪仗队」（明文+\uXXXX 转义）零命中；全模拟器无任何 shop-catalog-override.json；运营端唯一写盘路径是 publish→saveOverlay。结论：用户发布过、数据后被删。
- 根因：单测宿主 = 主 App，FileManager.default 即用户真实沙盒。5 个 ShopCatalog 测试套件（Phase456/V11Gap/OpsDeepEdit/BatchAttribution/OpsV11）在 tearDown 删除真实 shop-catalog-override/drafts/batches.json → 每轮回归（9/20 02:59、9/21 03:11/03:42/12:06…）跑完把用户发布数据清掉 → 「发布后刷新看不到」。此前的 ongoing 修复只是次生 bug，不是本因。
- 根源修复：新增 `ShopCatalogStorage`（nonisolated enum，useTemporaryForTesting/restoreDefaultForTesting，nonisolated(unsafe) testOverride）；DraftStore 四个 URL（drafts/batches/batchesStatic/overlay）+ ShopCatalogImageStore.directory 全部接入；5 套件 setUp 注入临时目录、tearDown 还原清理，删掉对生产文件的一切 removeItem。
- 回归防线：新增 ShopCatalogStorageIsolationTests（4 用例：URL 重定向/发布只写临时目录且生产目录零快照变化/还原清理/注入互不串扰）。6 套件 42/42 绿。已推送 main（c410b56a）。
- ⚠️ 用户数据无法恢复（无备份）：小狗仪仗队需重新发布；此后不会再被测试清掉。
- 教训沉淀到 MEMORY.md：单测触碰宿主 App 沙盒 = 数据事故模式，新测试一律走注入式目录。

## 补录草稿「无法上传图片」排查（约 17:36–17:40）
- 结论：代码里 ShopCatalogOpsView 补录草稿编辑器（productInfoSection）上一轮已接入 ShopCatalogImagePickerButton（「添加商品图片」append + 「添加尺码表/价格表原图」replace，ShopCatalogOpsView.swift:564/584）。
- 用户截图（16:51）仍是旧文案/无按钮 → 模拟器上跑的是旧安装包（16:50 修复后只跑过测试，未重装正式包）。
- 处理：xcodebuild build（Debug）→ simctl install + launch（pid 51953）；二进制内检出新增文案确认新 UI 已就位。无代码改动、无需提交。
- 经验：修复完成 ≠ 用户可见——凡改 UI 必须重新构建并 simctl install 到模拟器，再让用户验证。

## 店家商品库「淘宝导入 / 批量导入」下线（约 17:56–18:00）
- 需求：用户实测后要求直接下线 ShopCatalogOpsView 里的「淘宝导入（分享文本/链接）」与「批次录入（多链接多单品）批量解析」两个入口。
- 改动：删 importSection / batchSection 的导入 UI 与 importBatch()、importText/batchImportText/importProxy 死状态；批次列表保留（改名「批次」，无批次时整节隐藏），手动录入批次（补录上新）的展示与批量提交不受影响；文件头注释同步。
- 保留：ShopCatalogTaobaoParser（Service 层，有单测引用）与 draft.sourceURL 字段不动。
- 验证：xcodebuild -target ItemManager（绕坑三开关）BUILD SUCCEEDED（1m30s，增量）；重装模拟器并重启。未跑单测（本次纯 UI 删除）。

## 淘宝/批量导入死代码深度清理（约 18:05–18:12）
- 逐文件删除：ShopCatalogOps.swift 整个 ShopCatalogTaobaoParser 枚举（parse/makeDraft/parseBatch/ParsedListing/BatchParseOutcome 约 150 行）、createBatch 的 failures 参数与失败文案分支、CatalogBatchEntrySession.sourceTexts（Codable，旧 JSON 多余 key 会被忽略、解码兼容）、CatalogProductDraft.sourceURL（发布链路从不读取的只写字段）；ShopCatalogOpsView.swift 草稿编辑器「来源/淘宝链接」Section + createBatch 调用实参；两个测试文件删 G6/Phase6 淘宝解析用例（ShopCatalogOpsV11Tests 2 条、ShopCatalogPhase456Tests 1 条）。
- 保留不动：批次列表与手动录入批次流程（submitBatch/applyBatchAttribution/BatchDetailView）；衣橱侧 BatchImportView/HomeView 的「批量导入」是另一域功能；Localizable.xcstrings 里「淘宝导入（分享文本 / 链接）」key 已无引用但无害未删；docs 历史文档未改。
- 验证：build-for-testing TEST BUILD SUCCEEDED；-only-testing 跑 ShopCatalog 三个测试类 20 条用例全过（V11 7 / Phase456 10 / BatchAttribution 3）；新包已重装模拟器并重启。

## 实体管理页 iOS 27 索引列表改造

- 需求：常驻固定搜索栏（不随滚动）+ 右侧 A-Z 索引滑块（点按/拖动跳转）。
- 新增 `ItemManager/Views/ShopCatalog/AZIndexedList.swift`：AZPinnedSearchBar（置于 List 外层 VStack 实现 pin）、AZIndexRail（GeometryReader 等分槽位，DragGesture minimumDistance 0 同时覆盖点按+滑动，UISelectionFeedbackGenerator 触觉）、AZIndexHUD（中央大字母）、azLiquidGlass 修饰符（iOS 26+ glassEffect，低版本回退 ultraThinMaterial）。
- 分组纯逻辑 `AZIndexGrouping`（nonisolated）：CFStringTransform 中文转拼音首字母，其它归 #；`ShopCatalogOpsManageView` 店家 section 改为 A-Z 分组 List + ScrollViewReader.scrollTo(header id)，搜索时过滤店家/别名并隐藏索引；系列/商品 section 保留。
- 新增 `ItemManagerTests/AZIndexGroupingTests.swift` 6 用例全过。
- 构建环境：`name=iPhone 17 Pro` destination 匹配失败 exit 70（scheme 只认 iOS 27 runtime 那批），必须用 `id=FA7332BE-...`（iPhone 18 Pro, iOS 27.0）；首次 `test` 报 "signal term before establishing connection"，原命令重跑即 TEST SUCCEEDED。

## 用 Apple 官方 Agent Skill 规范重构本地 skill（约 23:50–00:25）

- 参考标准来源：本机 Xcode 27.0 (27A266a) 自带官方 skill，一条命令导出 10 个到 /tmp/xcode-apple-skills：
  `xcrun agent skills export --output-dir <绝对路径>`（device-interaction / swiftui-specialist / swiftui-whats-new-27 /
  uikit-app-modernization / modernize-tests / audit-xcode-security-settings / adopt-c-bounds-safety /
  app-intents-specialist / app-intents-whats-new-27 / building-document-based-swiftui-applications）。
  注意：网上文章说的 7 个是 Xcode 27 beta，正式版导出的是 10 个（名字也有变：test-modernizer→modernize-tests、c-bounds-safety→adopt-c-bounds-safety）。
- Apple 规范要点：SKILL.md 只放 frontmatter(name+description) + 触发/不触发 + references 索引（知识型仅 16–40 行）；
  细节全拆到 references/*.md，每条索引写清 "Use when …"；可固化的步骤放 scripts/；description 要长且塞进具体报错原文做路由。
- 重构对象：`.trae/skills/xcode开发`（80 行单体，且写死 /Users/muniao iCloud 路径 + Xcode-beta 假设，本机全失效）、
  `.trae/skills/ios-user-profile`（198 行可复制代码教程，且与真实实现已漂移：customAvatarPath 早已从"存绝对路径"改成"只存文件名"）。
- 产物：xcode 技能 → SKILL.md + references/{environment-facts,build-commands,sandbox-pitfalls,device-verification,uitest-pitfalls,snapshot-testing}.md
  + scripts/{detect_environment,build,verify_fresh_build}.sh；ios-user-profile → SKILL.md + references/5 篇。
- 实测：`scripts/build.sh app` 增量 17 秒 BUILD SUCCEEDED；verify_fresh_build 的 STALE/OK 分支都验过。
- 新发现的硬事实：模拟器必须选 **iOS 27.0 runtime**（iPhone 18 Pro/18 Pro Max）；名字存在的 iPhone 17 Pro 是 iOS 26.3，
  做 destination 会 exit 70。detect_environment.sh 的推荐逻辑 = 取 simctl 列表里最后一个 runtime 分组的第一台 iPhone。
- BSD stat 坑：`stat -f '%m %N' 多文件` 会退化成 filesystem 模式输出并报 "'%m %N': No such file or directory"；
  要时间戳就对单个文件 `stat -f %m`。
- 与 sync_skills.sh 兼容：rsync 整目录 + 重写 name，`--dry-run --to-codex` 22 个全通过，references/scripts 会一起同步。

## 实体管理「系列乱序」根因修复（接上条）
- 根因：只有店家走 AZIndexGrouping，系列/商品仍 `ForEach(store.catalog?.series ?? [])` 按录入顺序展示。
- 修复：排序下沉到 Service 层 `Services/ShopCatalog/AZIndexGrouping.swift`（新增 sortedByName），Store 增加 shopsSortedByName()/seriesSortedByName()/productsSortedByName() 作为展示列表唯一出口；OpsView 的店家/系列选择器也改为走访问器。
- 页面改为店家/系列/商品 segmented 分栏，每栏独立 A-Z 分组 + 索引滑块（消除同一列表重复字母的跳转歧义）；搜索按名称/别名/分类/所属店家过滤。
- 单测新增「全量字典序排序与录入顺序无关」，共 7 用例全过（TEST SUCCEEDED）。
