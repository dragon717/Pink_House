# 2026-09-17

## 品牌首页顶栏右上角「上传上新」主操作入口
- 需求：把上传入口从系列详情的隐藏位置提到品牌界面右上角，显眼主操作（icon+文字，不入折叠菜单/弹窗）。
- 实现（均在 `MidsummerBrandView.swift`）：
  - `MidsummerTopBar` 增加可选 `onUpload`，非 nil 时右上角渲染「⊕ 上传上新」橙色胶囊（`brandOrange`/`orangeSurface`，沿用 `topBarAddButton` slot 主题皮肤接口）；标题/副标题加 `minimumScaleFactor(0.75)` + `layoutPriority(1)`，小屏先缩放再截断，按钮完整可点不重叠。
  - 新增 `MidsummerUploadFlow`（pickSeries → form）+ `MidsummerUploadSeriesPicker`（轻量选系列 sheet，`.medium/.large` detents），点选系列后直达 `MidsummerListingFormView` 四步表单，不绕道工作台。
  - 入口仅品牌首页 route（`.home`）显示；系列详情仍保留「上新管理」。
- 测试：快照新增 01a（常规宽度带按钮）/ 01b（320pt 小屏降级）；UI 新增 `testE_BrandTopRightUploadEntry`（入口可见可点 → 选系列 → 直达 4 步表单）。3 快照 + 1 UI 全绿（TEST SUCCEEDED，4m27s）。
- 截图归档 `output/验收截图_上传入口右上角/`（40 入口 / 41 选系列 / 42 表单第一步）。

## 上传上新改为直达新建系列（第二稿）
- 用户要求：点「上传上新」不再从既有系列里选，直接进新建系列编辑（即四步表单第①步），由用户自填系列档案，完成后继续上新步骤；选系列环节整体移除；文案保持米团调性。
- 新增 `MidsummerCustomSeriesStore`（本地 JSON，Application Support/MidsummerListing/custom-series.json，与 ListingStore 同构）；`MidsummerStore.recomputeCatalog()` 把自建系列并入 byID 并订阅其变更——上架新品挂自建系列 id 后走与种子/云端系列完全相同的 feed/详情/入库链路。
- `MidsummerListingFormView`：`series` 改为可选，nil = 新建系列模式；`ensureCustomSeries()` 幂等创建系列（id 前缀 `midsummer-custom-`，stage 用 LaunchStage→Stage 映射，首图存为封面 `midsummer-custom-series-<id>.jpg`，launchedOn 空串排最前）；新建模式第①步顶部加「先创建新系列」说明条（sparkles，brandOrange）。
- `MidsummerBrandView`：删除 `MidsummerUploadFlow` 枚举与 `MidsummerUploadSeriesPicker`，`sheet(isPresented:)` 直达 `MidsummerListingFormView(store:)`。
- 测试：新增 `MidsummerCustomSeriesStoreTests`（往返 + 跨实例持久化）；`testE` 重写为「直达表单 + 断言无选系列弹窗 + 填标题进第②步」；端到端 `testPublishListingAndInsertToWardrobe` 复跑仍绿。
- 坑：`stageMapping[draft.stage] ?? .preview` 因字典 key 可选产生类型歧义编译错，用 `draft.stage.flatMap { ... }` 解。
- 截图归档 `output/验收截图_上传直达新建系列/`（40 入口 / 41 直达表单 / 42 填标题 / 43 第②步）。

## 双视角重构：用户视图 / 创作者视图
- 需求：同一套组件骨架、按视图状态切换交互与可编辑性；切换控件只对运营白名单可见，普通用户看不到。
- 新增 `MidsummerViewMode.swift`（viewer/creator + `MidsummerViewModeStore`，显式 `ObservableObjectPublisher`——`@MainActor`+`@Published` 合成 conformance 会挂）；`MidsummerStore.canEnterCreatorView`（allowed→可见 / denied→不可见 / unresolved→本机创作者模式放行）。
- 顶栏：右上角两段胶囊分段控件（当前视角橙色高亮+图标），创作者视图下其正下方挂「上传上新」；用户视图无上传。
- 首页卡片：创作者视图加「点击编辑名称 / 图片 / 描述」角标 + 更紧凑留白；点击直接进编辑态。
- 详情页 `MidsummerItemDetailSheet`：同一骨架，`startsEditing`/编辑按钮切换——编辑态为名称输入框 + 描述 TextEditor + 换主图 PhotosPicker + 保存（走 `MidsummerCloudService.publish` 整条重写链路，保存后 `refreshFromCloud()` 回读）；用户视图只读。
- 坑 1：SwiftUI 容器 `.accessibilityIdentifier` 会合并给子元素——两段切换按钮报同一个 identifier，UI 测试改用 label（「用户视图」/「创作者视图」）查询。
- 坑 2：`CreatorMode.shared` 原本只在设置页创建，UI 测试启动参数（reset/enable）要等设置页打开才生效 → 在 `ItemManagerApp.init` 提前 `_ = CreatorMode.shared`，否则门控读到上次运行残留值，用例互相污染。
- 测试：testE 改为「白名单可见切换 → 切创作者 → 上传直达新建系列」；新增 testF「普通用户看不到切换与上传」；单测+快照 25 例复跑全绿。
- 截图归档 `output/验收截图_双视角/`（40 用户视图 / 41 创作者视图 / 50 无创作者控件）。

## 详情页四类价格口径（定金/尾款/现货价/预约价）
- 用户要求：上新阶段（含预售）详情页只保留四类价格，参考价/划线价/会员价/到手价/促销标签一律不渲染；定金+尾款成对（支付定金后展示尾款），现货价与预约价互斥；缺数据不出现空白/占位/错误文案。
- `MidsummerModels.swift` 新增 `MidsummerItemDTO.DetailPriceRow` + `detailPriceRows(stage:)`（纯函数，可单测）+ `isPresaleStage(_:)`（图透/定金/尾款=预售线）。口径：①定金+尾款按数据逐行出，缺哪类少哪行；②归集商品 SKU 口径统一 balance → 区间整体按「尾款」；③预约价 vs 现货价由 `series.stage` 定优先（预售线优先预约价，出货/再贩/现货优先现货价），优先类缺数据退另一类，绝不同时出现；④现货价只认 price 且口径 shop/未标（SKU 侧同）；⑤reference 口径不展示；⑥四类全空才显示「价格待补充」诚实态。
- `MidsummerBrandView.swift` `MidsummerItemDetailSheet`：`priceTextWithKind` +「预约价 待补充」行替换为四类价格区；headline 带 `midsummer-detail-price-headline`，附加行带 `midsummer-detail-price-row-<label>`，pending 带 `midsummer-detail-price-pending`（供 UI 测试精确定位，避免「定金」等字样与规格档位选项撞车）。
- 测试：新增 `MidsummerDetailPriceDisplayTests`（11 例：成对/缺尾款不占位/互斥/阶段回退/参考价隐藏/SKU 区间/混合口径不归类/全空）；`MidsummerGoodsItemUITests.testMergedSakuraItemCoversBothLinks` 改为断言预约价/定金/尾款行不渲染（樱花小羊 inStock 只有现货价）。单测 68 例 + UI 3 例全绿。
- 坑 1：本机模拟器只有 iPhone 17 Pro（iOS 26.3.1 / 27.0），`iPhone 16 Pro` destination 已不可用。
- 坑 2：本机没有 Xcode-beta（AGENTS.md 的 DEVELOPER_DIR 段落是另一台机器的口径），直接用默认 /Applications/Xcode.app。
- 坑 3：UI 测试里「全页不得出现待补充」断言范围过大（上新日期等行合法带待补充文案），必须用价格区 identifier 收窄。
- xcstrings 顺带入库（上一轮「取消换图/已选新主图/当前视角」等自动抽取）。

## 上新表单字段归组 + 款式字段（③商品与尺码）
- 用户要求：保持 4 步不变，把尺码与名称/分类/价格归入同一步同屏；常用尺码 XS/S/M/L/XL/XXL/均码/F 标签多选；保留新增尺码；新增「款式」字段（尺码区下方，每款=前置图+后置该款价格，可多选可新增）；款式与衣橱同口径（发布后按款式归类/筛选/显示该款价）。
- 交互确认（AskUserQuestion）：选「③ 合并步 + ④ 确认发布」切分；款式价格写入 SKU 逐款价。
- `MidsummerListing.swift`：新增 `MidsummerListingStyle`（id/name/imageFile/price，Codable，styles 可选 nil=旧存档）；`makeItemDTO` 重写款式逻辑：款式名进 variant 规格组（系列没有的新款式自建选项 st-<name>）、图进 variantImageNames（无图按商品图顺序对位）、价写 SKU 逐款价（只约束款式组），variantOptionNames 保留同步作旧口径。
- `MidsummerListingFormView.swift`：步骤枚举 ③ sizes → itemBasics（名称/分类/尺码/款式/价格五卡同屏）、④ pricing → confirm（出处+汇总）；常用尺码 presetSizes 改为用户指定八档；draft 用 `[MidsummerListingStyleDraft]`（image + imageFile 回显保底）替代 styleNames；款式 PhotosPicker 用「stylePickerTarget + stylePhotoItem」单 picker 复用；提交时款式图经 `MidsummerListingStore.saveStyleImage`（-style-<index>，必须在 saveImages 之后）落盘；prefill 兼容旧存档（styles nil → 只回显名字）。
- 删除旧 pricingStep（约 65 行死代码）；summaryCard「关联款式」改「款式」行（名+逐款价）。
- 测试：`MidsummerListingTests` 新增 3 例（自定义款式进款式组/逐款价成 SKU+未填回退单品价/旧存档无 styles 不回归）；`MidsummerListingFlowUITests` 全流程改走新步骤结构（③同屏断言尺码+款式+价格、款式区在尺码区下方、④确认发布），校验用例改为断言停留在第 3 步。单测 3 组全绿 + UI 4/4 通过（5m41s）。
- 坑：`MidsummerListingStyleDraft` 带 UIImage 别加 Hashable（NSObject hash 语义不可靠）；struct View 里不能写 `[weak self]` 闭包回显。
- 验收截图：`output/验收截图_表单字段归组与款式/`（19 张，manifest 重命名）。

## 上新阶段收敛为四档（定金/尾款/预约价/现货）
- 用户截图标注：第②步「选择上新阶段」只保留定金、尾款、现货、预约价（移除图透/出货/再贩，新增「预约价」档）。
- `MidsummerLaunchStage`：6 档 → 4 档（deposit/balance/preorder/inStock）；priceFields：定金=[定金,尾款]、尾款=[尾款]、预约价=[预约价]、现货=[现货价]；priceHint 同步。
- `MidsummerListing.stage` getter 加旧 raw 降级映射：shipping→inStock、rerun→preorder、teaser→nil（语义不存在，创作者重选）；解码不崩。
- `MidsummerStage`（系列级）新增 `preorder` case（Codable 兼容，旧 JSON 不受影响）；`isPresaleStage` 纳入 preorder → 详情页四类价格口径里预约价阶段走预售线（预约价优先现货价）。系列徽章用 labelZH/symbolName，编译器强制补全。
- 表单 stageMapping 四档直映；顺手把「第 4 步价格配置」旧文案改成第 3 步（价格已并入③商品与尺码）。
- 测试：testStagePriceFieldsLinkage 改四档 + 断言 allCases==4；新增 testLegacyStageRawMapping（旧 raw 降级）。UI 用例（deposit 开关 + 全流程）2/2 通过。
- 验收截图：`output/验收截图_阶段四档/`。

## 洛丽塔服饰三级分类体系落地（中断续跑完成）
- 用户给术语体系（连衣裙类 OP/JSK/SK/背带裙/SP/AP/罩裙、套装 FS/set；内搭类 衬衫/泡泡袖/飞袖羊腿袖；小物类 头饰 边夹发带BNT/KC、配件 腰封围裙假领、鞋包 lo鞋lo包、其他 胸针袜类手套），要求对圈选商品分类整合。
- `MidsummerModels.swift`：新增 `MidsummerItemCategory`（dress/inner/trinket 三大类）+ `MidsummerItemKind` 扩为 15 具体类型（新增 suspenderSkirt/sp/ap/overdress/puffBlouse/gigotBlouse/hairItem/parts/shoesBag），`kind.category` 归属 + `kinds(in:)`；旧 raw op/jsk/skirt/blouse/accessory/set 冻结兼容（种子 JSON 与云端记录在用）。`infer(fromName:)` 按新词表扩充（罩裙→overdress、围裙→parts、背带→suspenderSkirt、泡泡袖/羊腿袖→内搭类、bnt/bonnet→hairItem、lo鞋lo包→shoesBag 等）。
- 表单 `kindCard`：Picker 菜单改三级分组 chips（三个大类分区，当前大类橙点标记），identifier `listing-kind-<raw>`。
- 测试：`testItemKindInferencePrefersSpecificKeywords` 扩充新词表 + 大类归属抽查。编译 0 错误；单测 35 例全绿；UI 全流程 testPublishListingAndInsertToWardrobe 通过。
- 验收截图：`output/验收截图_三级分类体系/`。
