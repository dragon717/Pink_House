# 2026-09-17

## 品牌首页顶栏右上角「上传上新」主操作入口
- 需求：把上传入口从系列详情的隐藏位置提到品牌界面右上角，显眼主操作（icon+文字，不入折叠菜单/弹窗）。
- 实现（均在 `MidsummerBrandView.swift`）：
  - `MidsummerTopBar` 增加可选 `onUpload`，非 nil 时右上角渲染「⊕ 上传上新」橙色胶囊（`brandOrange`/`orangeSurface`，沿用 `topBarAddButton` slot 主题皮肤接口）；标题/副标题加 `minimumScaleFactor(0.75)` + `layoutPriority(1)`，小屏先缩放再截断，按钮完整可点不重叠。
  - 新增 `MidsummerUploadFlow`（pickSeries → form）+ `MidsummerUploadSeriesPicker`（轻量选系列 sheet，`.medium/.large` detents），点选系列后直达 `MidsummerListingFormView` 四步表单，不绕道工作台。
  - 入口仅品牌首页 route（`.home`）显示；系列详情仍保留「上新管理」。
- 测试：快照新增 01a（常规宽度带按钮）/ 01b（320pt 小屏降级）；UI 新增 `testE_BrandTopRightUploadEntry`（入口可见可点 → 选系列 → 直达 4 步表单）。3 快照 + 1 UI 全绿（TEST SUCCEEDED，4m27s）。
- 截图归档 `output/验收截图_上传入口右上角/`（40 入口 / 41 选系列 / 42 表单第一步）。

## 上传上新改为直达新建系列（第二稿）
- 用户要求：点「上传上新」不再从既有系列里选，直接进新建系列编辑（即四步表单第①步），由用户自填系列档案，完成后继续上新步骤；选系列环节整体移除；文案保持米团调性。
- 新增 `MidsummerCustomSeriesStore`（本地 JSON，Application Support/MidsummerListing/custom-series.json，与 ListingStore 同构）；`MidsummerStore.recomputeCatalog()` 把自建系列并入 byID 并订阅其变更——上架新品挂自建系列 id 后走与种子/云端系列完全相同的 feed/详情/入库链路。
- `MidsummerListingFormView`：`series` 改为可选，nil = 新建系列模式；`ensureCustomSeries()` 幂等创建系列（id 前缀 `midsummer-custom-`，stage 用 LaunchStage→Stage 映射，首图存为封面 `midsummer-custom-series-<id>.jpg`，launchedOn 空串排最前）；新建模式第①步顶部加「先创建新系列」说明条（sparkles，brandOrange）。
- `MidsummerBrandView`：删除 `MidsummerUploadFlow` 枚举与 `MidsummerUploadSeriesPicker`，`sheet(isPresented:)` 直达 `MidsummerListingFormView(store:)`。
- 测试：新增 `MidsummerCustomSeriesStoreTests`（往返 + 跨实例持久化）；`testE` 重写为「直达表单 + 断言无选系列弹窗 + 填标题进第②步」；端到端 `testPublishListingAndInsertToWardrobe` 复跑仍绿。
- 坑：`stageMapping[draft.stage] ?? .preview` 因字典 key 可选产生类型歧义编译错，用 `draft.stage.flatMap { ... }` 解。
- 截图归档 `output/验收截图_上传直达新建系列/`（40 入口 / 41 直达表单 / 42 填标题 / 43 第②步）。
