# 2026-09-19

## 上新商品用户侧/入库丢「颜色·尺码」栏位 —— 根因修复（00:24–00:45）
- 用户报障（口述「时光管理」= 仲夏物语/时光馆国牌链路）：运营手动上传商品时填了颜色分类/款式与尺码，但用户看商品与入库时只当「无规格单品」处理。
- **根因两处**：
  1. `MidsummerListing.makeItemDTO` 尺码组整段包在 `if let sizeGroup` 里——系列没有基础条目（自建系列 `items: []`）或系列单品没整理过尺码组时，尺码组整个丢弃（规格面板无尺码栏位、入库尺码只能回退全部并集）；
  2. `MidsummerCloudService` 云端往返不携带规格：`publish(item:)` 不写 specGroups/skus、`makeItem(from:)` 恒置 nil（注释「规格尚未接入上传表单」已过期）——创作者投稿、改价面板整条重写、跨设备同步后商品全部退化成无规格单品。
- **修复**：
  - `MidsummerListing.swift`：①系列无尺码组时按 `sizes` 自建「尺码」组（id "size"、role .size、选项名=创作者原样，区别于系列匹配分支的「S码」）；②DTO `colors` 从款式名 `parseVariantFields` 解析去重派生（详情页「配色」行与入库兜底的来源），解析不出不硬造。
  - `MidsummerCloudService.swift`：publish 写 `specGroupsJSON` / `skusJSON` 字符串字段（避开 CloudKit 空列表限制），makeItem 解码还原；新增 internal static 纯函数 `specGroupsJSON/skusJSON/decodeSpecGroups/decodeSkus` 供单测；更新过期注释。入库链路（MidsummerWardrobeDraftBuilder/SpecResolver）无需改——显式尺码/颜色选择恢复后自然吃到。
- **测试**：`MidsummerListingTests` +3 用例（自建系列尺码组+配色派生+默认可选全+SKU 3 条 / 无尺码不出空组 / 云端 JSON 往返无损+脏数据安全落 nil），26/26 绿；回归 MidsummerSpecResolverTests/PriceEditingTests/PresalePhaseTests/CustomSeriesStoreTests/DetailPriceDisplayTests 全绿。
- 命令与边界：`xcodebuild -scheme ItemManager -destination 'platform=iOS Simulator,name=iPhone 18 Pro' -only-testing:ItemManagerTests/<上述套件> … test`（带 skill 三沙箱开关：`-IDEPackageSupportDisableManifestSandbox=YES`、`ENABLE_USER_SCRIPT_SANDBOXING=NO`、`-Xfrontend -disable-sandbox`；本机**没有** Xcode-beta，AGENTS.md 的 DEVELOPER_DIR 别照抄）。只跑了单测目标，未跑 UI 测试；主 target 编译经测试构建间接验证。
- 环境坑：本会话沙箱里 xcodebuild 连坑 1 都过不去（`sandbox-exec: sandbox_apply: Operation not permitted` 直接在包解析阶段失败），需 dangerouslyDisableSandbox + skill 三开关才能跑。

## 价格档位标注组根因修复（第二轮，00:55）

- 用户反馈：手动上传商品用户侧颜色/尺码已正常，但入库缺「价格档位」栏位。
- 根因：`MidsummerListing.makeItemDTO` 的 pricing 标注组只走继承（`if let pricingGroup`），自建系列/系列单品无该组时运营填的价格配置不会生成档位组——与上一轮尺码组丢失同一条根因模式。
- 修复：`MidsummerListing.swift` 无继承源时按 listing 实际填写的价格字段自建 `pricing` 组（id/名/role 与种子一致；选项只含已填档位，顺序 现货价→预约价→定金→尾款；全空不造组）。入库备注 specText 与默认选中自动吃到。
- 测试：MidsummerListingTests 新增 3 用例（定金尾款自建+specText 含档位、现货/全空边界、有继承源时原样继承不回归），29/29 全绿；回归 MidsummerSpecResolver/PriceEditing/PresalePhase/CustomSeriesStore/DetailPriceDisplay 共 87 用例全部通过。
- 未提交；改动文件：`ItemManager/Models/Midsummer/MidsummerListing.swift`、`ItemManagerTests/MidsummerListingTests.swift`。

## 上新阶段收敛：定金+尾款并入预约价（01:14-02:16）

- 用户口径：上新阶段只留「预约价 / 现货」两档；定金+尾款=预约价，是预约价的拆分填法（可填全款，也可配定金、尾款自动算），不是独立阶段。
- 实现（保留 4 个 enum case 兼容旧存档与相位机，不 remap 旧 raw——旧测试/旧数据依赖 .deposit/.balance 可读写）：
  - `MidsummerLaunchStage.selectableCases = [.preorder, .inStock]`，表单阶段 chip 只显示这两档；
  - `priceFields`：preorder → [.preorder, .deposit, .balance]；`priceHint` 同步改文案；
  - `presalePhase`：preorder 阶段**配了定金**的商品沿用定金→尾款自动流转（纯全款预约返回 nil）；.deposit/.balance 旧分支保留；
  - `MidsummerListingStore.refreshPresaleTransitions` 守卫扩展到 preorder+deposit；
  - 表单：预售时间窗 UI 挂到 preorder 阶段（旧 deposit/balance 编辑兼容保留）；删除「选定金阶段自动开定金开关」的 onChange；提交时 preorder 阶段写入 depositEndsAt/balanceEndsAt；价格卡 hint 更新。
- 测试：testStagePriceFieldsLinkage 重写（selectableCases + preorder 三配置项）；新增 testPreorderStageWithDepositFlowsPresale；testNonPresaleStagesReturnNil 改为纯全款口径；UI 测试 testDepositAndBalanceAreOptionalToggles 改用 listing-stage-preorder + 手动开定金开关（未实际执行 UI 用例）。
- 验证：build-for-testing 通过；7 个套件 128/128 全绿（MidsummerListing/PresalePhase/CreatorAccess/SpecResolver/PriceEditing/DetailPriceDisplay/CustomSeriesStore）。中途模拟器启动卡死一次（did not return a process handle），shutdown all + build-for-testing 分步后 test-without-building 恢复。
- 改动文件：Models/Midsummer/MidsummerListing.swift、Views/Midsummer/MidsummerListingFormView.swift、Services/Midsummer/MidsummerListingStore.swift、ItemManagerTests/MidsummerListingTests.swift、ItemManagerTests/MidsummerPresalePhaseTests.swift、ItemManagerUITests/MidsummerListingFlowUITests.swift。未提交。

## 一键入库分阶段口径：定金期一键加入定金 + 心愿尾款同步（02:27–02:50）

- 用户需求：一键入库按预售相位处理——①定金期显示「一键加入定金」，入库同步心愿尾款、商品同时进衣橱；②定金结束后创作者更新尾款时间，后续页面可查尾款；③尾款结束进现货阶段（创作者更新现货价），一键入库同时展示预约价与现货价。
- 实现：
  - `MidsummerItemDTO` 新增 `depositEndsAt`/`balanceEndsAt`（var+默认 nil 向后兼容），`makeItemDTO` 填充；
  - `MidsummerSpecResolver` 新增 `MidsummerPriceTierPick` + `selectedPriceTier`（按选项**名称**识别四档，种子/自建档位组 id 不同但名称统一）；
  - `MidsummerWardrobeDraftBuilder.makeDraft/makeSetDraft` 新增 `presalePhase` 参数：定金/尾款期恒走定金尾款口径（isDepositPlan=true 同步心愿尾款），尾款窗口（定金截止=尾款开始、尾款截止=尾款结束）写进 `finalPaymentDate/finalPaymentEndDate`，备注写「尾款时间：…（已同步心愿尾款）」；预售结束按所选档位走现货或定金口径（未选档位现货价优先），现货口径把 deposit/balance 清零（首版漏了，单测逮到）；phase=nil 沿用旧口径。倒挂配置窗口压平保证 end>=start；
  - `MidsummerWardrobeInserter.quickInsert/quickInsertSet/openEditor` 透传 phase；
  - UI：`TimeHallWardrobeInsertButtons` 加 `quickInsertTitle` 覆盖（定金期主按钮「一键加入定金」）；规格面板 drawer/panel 加 `presalePhase`（**注意 MidsummerSpecPanel 有显式 init，新参数必须同步进 init 签名**），定金期确认按钮「加入定金」+ 定金期提示行；`MidsummerItemDetailSheet` 内部 `presalePhase` 计算属性（MidsummerListingStore 按 item.id 查询，品牌页/系列详情页两个入口自动生效）；入库吐司追加「定金 ¥x 已同步心愿尾款」。
- 测试：MidsummerSpecResolverTests 新增 6 用例（定金期窗口同步、尾款期窗口压平、预售结束现货/定金档位、默认档位、tier 按名称识别）；61/61 全绿（SpecResolver 44 + PresalePhase 17）。
- 命令与边界：build-for-testing 通过；`xcodebuild test -only-testing:'ItemManagerTests/MidsummerSpecResolverTests' -only-testing:'ItemManagerTests/MidsummerPresalePhaseTests'`（skill 三沙箱开关 + 本机 iPhone 18 Pro）。只跑了单测，未跑 UI 测试/模拟器验收；创作者更新尾款时间/现货价复用既有表单与改价面板（MidsummerListingFormView 步骤②、MidsummerPriceEditing），未新增编辑入口。未提交。

## 「分类与款式」表单布局优化 + 白名单灰度（02:58–03:10）

- 需求：①「分类与款式」名称固定页面最顶部；②字段重排为 分类与款式→价格→尺码；③分类与款式上方预留「预约价格表」空位；④填完具体分类（如 OP）后该分类下方自动显示尺码表；⑤仅限运营白名单命中的创作者。
- 实现：
  - `CreatorAccess` 新增 `isWhitelistedCreator` + 纯函数 `isWhitelistedGate`：**只认 CloudKit 白名单命中（gate == .allowed）**，本机创作者模式开关不放行（灰度能力，模拟器上默认走旧布局）；
  - `MidsummerListingFormView`：
    - 步骤标题 `sectionTitle` 从 ScrollView 内挪到滚动区外固定顶栏（identifier `listing-step-title`），任何步骤都钉在页面最顶部；
    - `itemBasicsStep` 按白名单分双布局：白名单 → `preorderPriceTableSlot`（预约价格表占位卡，虚线框不可交互，identifier `listing-preorder-price-slot`）→ categoryStyleCard → priceCard → sizesCard；非白名单 → 旧布局（尺码池在前）逐像素不动；
    - 款式卡尺码表出现条件分级：白名单 = 填完分类名（款式名非空）+ 已选尺码即显示（不再等传图）；旧布局保持「有图 + 有尺码」。款式卡空尺码提示改为位置中性文案。
- 测试：CreatorAccessTests 新增 testWhitelistedCreatorGateRequiresExplicitAllow（allowed/denied/unresolved 三态矩阵）；build-for-testing 通过；CreatorAccess 12 + MidsummerListing 30 + MidsummerSpecResolver 44 = 86/86 全绿。UI 测试未跑（模拟器白名单判定为 unresolved → 走旧布局，既有 UI 用例不受影响）。
- 改动文件：ItemManager/Services/CreatorAccess.swift、ItemManager/Views/Midsummer/MidsummerListingFormView.swift、ItemManagerTests/CreatorAccessTests.swift。未提交。

## 上新四项修复（03:35–04:00）

1. **移除「OP」自动前缀**：`MidsummerListingFormView.autoStyleName` 不再用主分类短标拼前缀（分类选择入口下线后 kinds 恒默认 [.op]，任何输入都被强加「OP 」）；输入原样保留，草稿 kinds 注释同步更新。
2. **价格与上新阶段联动**：`priceCard` 按 draft.stage 分三态——现货 → 新增 `spotPriceText` 现货价卡（identifier listing-spot-price）；预约价（含旧存档定金/尾款）→ 原预约价/定金/尾款卡；未选阶段 → 引导文案。款式卡价格占位、款式尺码表回退价、③步校验、`assembledListing` 发布映射（现货写 price+priceKind=.shop 并清预约价组，反之亦然）、`prefill` 回显全部按阶段走。UI 测试 3 处同步更新（现货→现货价、跨系列用例改走预约价阶段、「OP 白色」→「白色」）。
3. **款式图上传修复**：根因 = `onChange(of: stylePhotoItem)` 同步把 `stylePickerTarget` 置空，异步加载闭包里现读恒为 nil，guard 失败图片落不进款式；改为 onChange 先捕获目标 id 局部量再异步 apply。loadPicker 失败文案改中性「图片读取失败」。
4. **系列删除交互**：品牌页（MidsummerHomeContent）系列行新增 `MidsummerSwipeRightDeleteRow`——删除键位于系列项左侧、向右滑动露出（拖过半程停驻），点击弹 confirmationDialog 二次确认；权限口径与详情页一致（创作者 + `midsummer-custom-` 自建系列），普通用户/种子系列无手势。删除失败走 alert。系列详情页原底部删除按钮保留。
- 验证：build-for-testing 通过；单测 71/71 全绿（Listing 30 + PresalePhase 17 + DetailPriceDisplay 11 + PriceIntegrity 13）。UI 测试已更新断言但未实跑；改动未提交。

## 种子/创作者系列统一管理：删除 + 改价入口（04:35–04:40）

- 需求：种子系列管理界面加删除按钮与改价入口；创作者上传系列管理界面与种子系列布局/视觉/交互完全统一。
- 实现：
  - 新增 `MidsummerHiddenSeriesStore`（Services/Midsummer/）：种子/云端系列不可变，删除语义 = 本机隐藏（JSON 账本 hidden-series.json，与 CustomSeriesStore 同构，requireCreator(.seriesDelete) 门控，支持 restore 误删恢复）；
  - `MidsummerStore.recomputeCatalog()` 按 hiddenIDs 过滤（含自建系列），订阅账本变更自动重算；
  - `MidsummerSeriesDetailView`（种子与创作者系列共用同一详情页，布局天然统一）：`canDeleteSeries` 放宽为 canEditContent（删除区两类系列同款视觉）；删除执行分支——custom 前缀真删除、其余走隐藏账本（上架商品均逐条真删）；确认弹窗文案按类型分支（本机移除可恢复 vs 不可恢复）；商品行右侧新增创作者「改价」按钮（identifier series-item-price-edit-<id>，listing→本地编辑面板 / 基础条目→CloudKit 面板，复用既有 openPriceEditor 与价格总表点行改价并存）；
  - 品牌首页滑动删除（MidsummerSwipeRightDeleteRow）同步放开 deletable = isCreator，删除函数同分支，确认文案动态。
- 测试：新增 MidsummerHiddenSeriesStoreTests（3 用例：hide/restore 往返、跨实例持久化、非创作者拒绝且数据不动）；build-for-testing 通过；Hidden 3 + CustomSeries 2 + Listing 30 + CreatorAccess 12 = 47/47 全绿。UI 测试未跑；改动未提交。
- 改动文件：ItemManager/Services/Midsummer/MidsummerHiddenSeriesStore.swift（新）、MidsummerStore.swift、MidsummerSeriesListView.swift、MidsummerBrandView.swift、ItemManagerTests/MidsummerHiddenSeriesStoreTests.swift（新）。
