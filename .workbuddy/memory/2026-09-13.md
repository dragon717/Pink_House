# 2026-09-13

## 建立架构与开发规范文档体系

审查项目后产出 6 份文档，已提交（`a9c8e7ea` docs: 建立架构与开发规范文档体系并明确业务拆解方法）：

- `README.md`（新建）：项目总览、技术栈、快速上手、文档地图、技术债
- `Agent.md`（新建）：架构规范（分层 + 依赖铁律）、业务开发规范、9 条硬约束 H1~H9、阶段审查、git 提交规范
- `MEMORY.md`（重写）：长期记忆索引 + 踩坑 + 技术债台账
- `docs/目录结构说明.md`（新建）
- `docs/业务拆解方法.md`（新建）：面向初级 PM 的四步法 + 4 张表 + 3 张 mermaid 图
- `docs/阶段审查清单.md`（新建）：R1~R4 审查清单 + 子代理 prompt 模板

关键决策：
- `Agent.md` 与 `AGENTS.md` 分工：AGENTS.md 是 Codex 注入的本机事实清单，Agent.md 是给人看的规范；冲突裁决为「本机事实看 AGENTS.md，规范流程看 Agent.md」。
- H1 硬约束 = 遇到不懂必须联网搜索，写明触发情形、执行方式、明令禁止、搜索失效时的 4 级降级顺序。
- 「不使用命令行编译 Xcode」（.trae/rules/my.md）与「可跑 xcodebuild」（AGENTS.md）的冲突，以 README 口径调和：人工在 Xcode 编译，Agent 自证/验收才用命令行。
- 用户说的 memory.md 落到根目录 `MEMORY.md`（大小写不敏感同文件），每日流水另归 `.workbuddy/memory/`。

## 子代理审查流程首次跑通

- 第一轮审查判「不通过」：发现事实错误——`.lproj` 实际 9 个，文档多处写 10 个（README、目录结构说明、阶段审查清单、业务拆解方法）。另 Agent.md R2.13 举例的 `ClothingEditView.swift.backup` 真实存在且已被 git 跟踪，MEMORY.md 台账漏登记。
- 修复后复审通过，才 commit。这个「审查不通过不允许提交」的闭环要固化下来。

## 待用户确认（阻塞项）

- 目标仓库：`AGENTS.md` 声明 Swift/Xcode 源在 iCloud `/Users/muniao/Library/Mobile Documents/com~apple~CloudDocs/游戏/github/Pink_House`，本工作区是 `/Users/sangyu/develop/Pink_House`。已在 Agent.md 顶部和 MEMORY.md 标为「动工前必问」。

## 项目体检数据（2026-09-13）

- Swift 文件：全仓 450 个，`ItemManager/` 下 399 个
- 大文件：>1000 行 39 个，500~1000 行 66 个；最大 `NewbieGuideManager.swift` 4162 行
- 语言：9 个 lproj
- 空目录（功能已下线或代码在别处）：`Views/BigWorld`、`ThreeD`、`PerlerBeads`、`SkirtMarket`、`Wardrobe`、`DesignSystem`、`NewbieGuide`、`SceneEditor3D`
