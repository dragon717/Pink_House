# 2026-09-16

## 仲夏上新按千牛口径调整：预约价 + 款式对应图 + 去掉参考来源 ✅

用户要求（千牛商家后台口径）：每单品独立商品条目（已有 ✔）；价格三档「现货价 / 预约价 / 定金尾款」字段齐全按需选填；去掉详情页「参考来源」类内容；补录单品图按图2（粉色JSK/粉色OP）与款式一一对应。用户确认：去掉「价格来源说明」+「查看原文出处链接」（仅单品详情弹窗）；款式图交互=按款式分组各配图位。

改动（4 文件）：
1. `MidsummerModels.swift`：DTO 加 `preorderPrice: Int?`（默认 nil 向后兼容）与 `variantImageNames: [String: String]?`（款式名→本地文件名）；`priceText` 加预约价分支（定金分支之后）。
2. `MidsummerCloudService.swift`：`publish(item:)` 加 `variantImages: [(name, image)]` 参数，按序写 `variantImage1..8` 资产槽 + `variantImageMap` JSON 字符串字段（款式名→槽位字段）；`makeItem`/fetch 侧解析映射并复制资产回填；`MidsummerPublishedImages` 加 `variantImageNames`；record 同步写 `preorderPrice`。注意：publish 是整条重写，款式图必须全量带上。
3. `MidsummerBrandView.swift`（单品详情弹窗）：删 `priceProvenanceCaption` 与「查看原文出处」Link；新增「款式对应图」区块——每款一行（图位 56pt + 补录/更换/删除），款式名取法：specGroups 款式组选项 → 「颜色+类型短标」（colors 空时回退 series.colors），key 稳定保证资产槽位不串。
4. `MidsummerContributeView.swift`：DraftItem 加 `preorderPriceText`，itemCard 价格改两行（现货价/预约价、定金/尾款），priceSummary/makeItem/DraftSnapshot（ItemSnapshot.preorderPriceText 可选向后兼容）全链路。
5. `MidsummerSeriesListView.swift`：价格总表 case (nil, nil) 下 preorderPrice 优先归「预约价 · 全款预约」组，不再靠「只有定金」猜。

⚠️ CloudKit Console 需为新字段配 schema：`MidsummerItem.preorderPrice`(Int)、`variantImage1..8`(Asset)、`variantImageMap`(String)——未配置前上传这些字段会被丢弃/报错（见 docs/MIDSUMMER_TALE_CLOUDKIT_SETUP.md 口径）。

## 移除详情页「单品图片」宫格区块 ✅（03:10）

用户圈出详情弹窗里的「单品图片」宫格 + 追加图片行，要求彻底删除（非隐藏）。`MidsummerBrandView.swift` 改动：
- 删 `itemImageSection` 属性、`appendItemImages`、`deleteItemImage`、`supplementPhotoItem` state、对应 `.onChange`、`maxItemImages` 常量；`uploadedImageNames` 也删（`workingImageNames` 改为直接从 item 计算，仍供头图 + 款式图整条重写用）。
- 保留：款式对应图区块（`variantImageSection`）与其上传/删除、`itemImageMessage`/`isUploadingItemImage`（款式流程共用）、详情头图。
- 清理 `Localizable.xcstrings` 死键 4 个：单品图片 / 单品图待补充——运营者可在下方上传 / 补录单品图 / 追加图片（还可传 %lld 张）。
- 验证：`build-for-testing` TEST BUILD SUCCEEDED（沙箱三开关照 skill）；`MidsummerDetailCoverUITests` TEST SUCCEEDED（1 test, 0 failures）；新构建已装回 iPhone 18 Pro 模拟器并启动。
- 注意本机无 `/Applications/Xcode-beta.app`，直接用默认 Xcode 27（`xcodebuild -version` = 27A266a）；DEVELOPER_DIR 写 beta 路径会报 missing path。

## 樱花小羊款式分类 × 尺码表整理页 ✅（03:41）

用户要求：按款式分类（同款式不同颜色归一类），每分类下方填对应尺码表；数据来自之前抓取的淘宝链接。产出 `output/樱花小羊_淘宝原始信息/樱花小羊_款式分类与尺码表.html`（复用同目录 img/ 颜色缩略图）。
- 全部尺码表都在 desc02–desc12 详情长图里：SK(d02)、内搭(d03)、定位花jsk(d04)、无腰op(d05)、段段jsk(d06)、罩裙+围裙(d07)、针织开衫+立体小脸包(d08)、草帽+bb帽(d09)、袜子+领结(d10)、立体胸针(d11)、花丸+软发圈(d12)。
- 9 分类：SK¥449 / 定位花JSK¥599 / 段段JSK¥999 / 无腰OP¥699 / 内搭¥369 / 针织开衫¥279 / 罩裙¥399 / 围裙¥359 / 小物（小脸包¥119+bb帽¥229）。
- 颜色图映射从旧报告 HTML 的 color-card 结构正则提取（color-name → img/color_*.jpg，16 条）。
- itemId=1032370386538，店铺「仲夏物语原创设计」。taobao 详情还含袜子/领结/草帽/花丸/软发圈/胸针等 APP 未收录小物，本次未展开。
- 技巧：70 张 desc 图先用 PIL 拼 contact sheet（每张 10 图）快速定位尺码表所在图，再放大转录，省大量多模态读取。

## 尺码表图接入 APP（用户反馈「iOS 显示不出来」）✅（03:59）

用户以为又是图片格式问题（上次 WebP 伪 .jpg）。排查：`output/樱花小羊_淘宝原始信息/img` 91 张**全部已是真 JPEG**（上轮 sips 转过），格式没问题。真正原因：详情页尺码表读 `item.sizeChartImageName`，该字段从未填过 → 永远显示「待补充」，爬到的尺码表图从未接入 APP。

接入方案（按款式分列）：
1. PIL 按最大空白行段切分合并图：desc07→罩裙+围裙、desc08→开衫+小脸包、desc09→草帽+bb帽；产出 10 张 `Resources/Midsummer/SeedImages/seed-sakura-chart-*.jpg`（quality 90）。
2. `MidsummerSeedImageImporter.filePrefixes` 加 `seed-sakura-chart-`。
3. DTO 加 `sizeChartImages: [SizeChartEntry]?`（struct{style,imageName}，数组保序）。⚠️ 两个坑：DTO 是 Hashable，嵌套类型也要 Hashable；字段声明顺序决定 memberwise init 参数顺序——放在 sizeChartImageName 之后、variantImageNames 之前，否则既有调用点报 argument order 错。
4. CloudService：publish 写 `sizeChartMap` JSON 字符串字段（镜像 variantImageMap 模式）；makeItem 用 decodeSizeChartMap 还原。⚠️ CloudKit Console 需再加 schema 字段 `sizeChartMap`(String)。
5. 种子 JSON 樱花小羊条目加 sizeChartImages 10 条；ContributeView 乐观更新构造点补传该字段防丢失。
6. sizeChartSection：charts 非空 → DisclosureGroup 内按款式分列（款式名 + scaledToFit 图，id `midsummer-sizechart-<款式>`）；否则回落旧单图/待补充逻辑。
7. 新 UI 测试 testSizeChartsShownPerStyle（MidsummerDetailCoverUITests）：导航→展开尺码表→断言 SK 款图渲染。

## 手机上分类尺码表页不显示（根因修复）
- 排查：`樱花小羊_款式分类与尺码表.html` 引用的 16 张图全部存在且均为真 JPEG，HTML 本身无 iOS 不兼容内容——格式不是问题。
- 根因：页面依赖相对路径 `img/` 目录（16 张外链图），传到手机（微信/隔空投送/文件 App）通常只有单个 HTML 文件，图全挂；微信甚至无法预览本地 HTML。
- 修复：脚本把 16 张图 base64 内联，生成自包含单文件 `樱花小羊_款式分类与尺码表_手机版.html`（1.7MB，零外部引用），单文件传手机即可完整显示。原版保留供桌面端使用。
- 经验：给手机看的静态页必须做成 base64 内联的自包含单文件，别依赖相对路径资源。

## PetAI "Publishing changes from within view updates" 根因修复
- 报警点：PetAIService.swift:600 `self.petName = petName`（updateConfiguration 内，唯一 @Published 写入）。
- 根因：调用链在视图更新周期内同步执行——PetHomeView onAppear/onChange → updateWardrobeContext → setupAIService → updateConfiguration；设置页 "AISettingsChanged" 通知也会同步到达。期间写 @Published 触发运行时警告。
- 修复：非发布属性（role/apiKey/provider/history）保持同步写；@Published 写入（PetAIService.petName、PetViewModel.aiService）改为 DispatchQueue.main.async 延迟到更新周期外发布，值未变则跳过。
- 验证：xcodebuild build SUCCEEDED，已装回模拟器。

## 樱花小羊小物配件链接整合（item 1031690555405）
- 短链 e.tb.cn/h.8sBeLWLfE3Zcrko → item 1031690555405「现 仲夏物语樱花小羊草帽袜子项链花丸胸针枕头胸针小物Lolita」（同店 仲夏物语原创设计）。
- 抓取：headless 被风控（punish），有头 Chromium + 关闭 AutomationControlled + 桌面 UA 通过；数据在 `window.__ICE_APP_CONTEXT__`（`var b = {…}`，需 raw_decode）；详情长图在 pcADescUrl（market.m.taobao.com desc 页）。8 SKU 全 F 码：草帽¥149 / 袜子¥89×2色 / 项链¥89 / 立体胸针¥69 / 花丸¥79 / 枕头胸针¥59×2色。
- 产出：output/樱花小羊小物_淘宝原始信息/（raw_data.json + 原始信息 HTML + 41 图）；分类尺码表页追加 ⑩–⑮ 六类（29 张 desc 图中 4 张含尺码表，2 张合并图按空白切开），手机版重新内联（24 图 2.1MB）。
- APP：种子 JSON 樱花小羊系列新增条目 midsummer-2026-sakura-lamb-goods（8 颜色分类 + F 码 + sizeChartImages 6 张），19 张新种子图（seed-sakura-goods-*/seed-sakura-chart-*），Importer 前缀加 seed-sakura-goods-。
- 注意：desc 图中有 1 张 PNG 伪 .jpg 已改名；下载图均为真 JPEG/PNG。
- 验证：MidsummerPriceIntegrityTests 曾 2 例失败——旧断言「樱花小羊系列仅 1 个归集商品 / ¥119–999」已按新形态更新（2 个归集商品：主链接 ¥119–999 + 小物 ¥59–149，系列区间 ¥59–999）；重跑 37 例 0 失败，新构建已装回模拟器。
- 抓取坑：xcodebuild -only-testing 传了不存在的测试类名会直接 TEST FAILED（退出码 65），先确认测试类存在。

## 一键入库页面接入小物条目（验收）
- 结论：品牌页 feed（MidsummerStore.itemFeed）与系列详情页均是对 series.items 的通用渲染，上一轮写入种子的「樱花小羊 小物」自动以同构卡片出现（徽章/名称/商品页价区间/尺码行/一键入库按钮），无需改产品代码。
- 新增 UI 验收 MidsummerGoodsItemUITests.testGoodsItemConsistentWithOriginalOnBrandPage：主/小物卡片 a11y 标签同格式（名称，商品页价 ¥区间）；详情页 现货徽章→名称→价格 纵向有序 + 包含款式/尺码/单品尺码表行齐备；一键入库→规格面板 颜色分类 (8) + F码。首跑失败：规格面板的「F码」选项是 Button 不是 staticText，改 .any 查询后 TEST SUCCEEDED。
- 经验：规格面板缩略格子版式里选项（含尺码 F码）是按钮；XCUITest 断言用 .any 而非 staticTexts。





8. 验证：TEST BUILD SUCCEEDED；UI 两用例 TEST SUCCEEDED；MidsummerPriceIntegrityTests+SpecResolverTests 37 用例 0 失败；新构建已装回 iPhone 18 Pro 并启动。UI 测试截图确认 SK 尺码表图完整渲染。
9. XCUITest 新坑两条：SwiftUI `Image` 默认不是 a11y 元素，identifier 不进层级，需 `.accessibilityElement(children:.ignore)+label`；DisclosureGroup 的 identifier 会覆盖全部子元素 identifier（子元素 identifier 全变父级的），但子元素自己的 accessibilityLabel 保留——断言用 label 最稳。

验证：主 target `xcodebuild -target ItemManager`（三沙箱开关）BUILD SUCCEEDED；`-only-testing` 跑 Midsummer 5 个测试类 68 用例全过（iPhone 18 Pro 模拟器，未跑全量、未做 UI 交互验收）。

## 樱花小羊淘宝链接（item 1032370386538）SKU 全量采集 ✅

复用 scrapers 登录态 + Playwright（一次性脚本在 /tmp/tb_sku_dump.py、/tmp/tb_sku_parse.py，未进仓库），从 item 页内嵌 `skuBase`/`skuCore` JSON 提取。关键事实：
- 颜色分类共 **16 个选项**，真实颜色只有 **粉色 / 蓝绿色 / 奶白色** 三种（种子 json 里的「浅青色」实为「蓝绿色」，「米色」当前链接已无）。
- 小物类实际是 2 类 3 款：**立体小脸包 奶白色（F码 ¥119）**、**bb帽 奶白色 / 蓝绿色（F码 ¥229）**——用户口径里的「奶绿色BB帽」实际链接写的是「蓝绿色」。
- 全链接逐款挂牌价（预售期，¥起价口径）：sk 449 / 内搭 369 / 定位花jsk 599 / 无腰op 699 / 段段jsk 999 / 罩裙 399 / 围裙 359 / 针织开衫 279 / 小脸包 119 / bb帽 229。种子 json 里「price=119 是定金口径」的旧 note 有误——119 只是最低 SKU（小脸包）的价格。
- 尺码实际有 XS/S/M/L/XL/XXL/F 七档，比种子的 S/M/L/XL/均码 多。
- 种子「款式」组的「印花JSK」在商品页叫「定位花jsk」；「开衫」叫「针织开衫」。
- 未改动种子 json（用户只要分类清单）；后续若细分小物需同步改 specGroups/SKU 与相关测试。

## 樱花小羊「链接原始信息」HTML 报告 ✅

用户要求：严格按链接原始内容输出整个系列（名称/图/规格/价格/详情描述），不改名不归类不复用衣橱分类，缺什么如实标注。产出 `output/樱花小羊_淘宝原始信息/`：
- `樱花小羊_链接原始信息.html`（自包含报告，图片全本地化 img/ 共 91 张）+ `raw_data.json`（结构化原始数据）。
- 采集链路：item 页内嵌 JSON（itemBase 标题/5 主图、skuBase/skuCore 55 SKU、重点参数「品牌=仲夏物语/适用季节/款式/出售状态」）+ Playwright 拦截 `mtop.taobao.detail.getdesc` jsonp（需剥 `mtopjsonpN(...)` 壳，componentData 按 layout ID 取 picUrl）→ 详情 70 张纯图片流、无文字。
- 关键技巧：desc 页 `market.m.taobao.com/app/detail-project/desc/index.html?f=desc/icoss...` 直接 curl 是空壳，必须带登录态渲染；JSONP 两次请求（第一次 token 空），取 body>1000 的那次。
- 缺失项（报告已如实标注）：页面无完整材质/尺寸表参数，详情描述无文字。
- 注意：alicdn 图片实际是 WebP（.jpg 后缀，浏览器可正常渲染）；desc 里 750×11~27 的细条图是页面原始分隔条，非下载失败。
- **iOS 显示修复**：WebP 内容 + .jpg 扩展名 + 服务器按 image/jpeg 返回 MIME 的错配导致 iOS WKWebView/文件预览拒绝解码。修复 = 全部 91 张按文件头检测（RIFF/WEBP、\x89PNG）后经 `sips -s format jpeg -s formatOptions 90` 转成真 JPEG（文件名不变、HTML 无需改、desc 细条图尺寸保留）。采集脚本以后落盘时应转码，不能只信任 .jpg 后缀。

## 樱花小羊图片作为静态素材打进 iOS APP ✅

用户要求：修复后的图片放进 APP 作静态素材，编译后直接可见。实现（3 处代码 + JSON + 20 张图）：
- **图片**：`ItemManager/Resources/Midsummer/SeedImages/` 20 张真 JPEG（5 主图 + 15 款色组合图）。工程是 Xcode 16 PBXFileSystemSynchronizedRootGroup，文件放入即自动入包（bundle 根平铺），无需改 pbxproj。70 张详情长图无对应 UI 展示位，未打包（避免 +8MB 包体）。
- **`MidsummerSeedImageImporter`**（新文件）：启动后异步把 bundle 内 `seed-sakura-lamb-` 前缀图拷入 ImageManager Images 目录；幂等（已存在跳过），只增不删（用户删图不会被覆盖回）。`MidsummerStore.init` 调用，完成后 `objectWillChange.send()` 防首启停在占位图。
- **`MidsummerCoverView`**：`UIImage(named:)` 失败时回退 `ImageManager.loadImage`（顺带修复云端上传封面本地文件名显示不出来的隐患）。
- **种子 JSON**：樱花小羊系列封面/单品封面/5 格主图宫格/款式选项图（按种子色序取第一个存在的组合）/颜色选项图/49 个 SKU 图/`variantImageNames`（款式对应图，9 款）全部填上 `seed-sakura-lamb-*.jpg`。Taobao 款名→种子 styleId 映射：sk→print-sk、定位花jsk→print-jsk、段段jsk→duan-jsk、无腰op→waistless-op、针织开衫→cardigan、立体小脸包/bb帽→accessory（accessory+cream 小脸包优先）。蓝绿色→light-cyan。
- **如实留空**：6 个 SKU 无图（米色开衫×4、粉色小物×1）——链接里已无这些组合，不虚构。
- 验证：BUILD SUCCEEDED ×2；.app 内 20 张 jpg；Midsummer 相关 3 测试类 54 用例全过；模拟器（iPhone 18 Pro）安装启动后 `Documents/Images/` 实测 20 张种子图全部导入——MidsummerStore 在主 TabView 启动即初始化，导入端到端跑通。边界：未做品牌页逐 slot 视觉截图验收。

## 樱花小羊规格面板对齐淘宝：扁平 16 项「颜色分类」✅

用户给了淘宝 SKU 面板截图（颜色分类 16 项，每项缩略图+名称+价格），要求我们的规格面板同样完整展示 16 项、布局/排列/展示形式一致。重构（此前是 款式9×颜色4×尺码 三维结构）：

- **种子 JSON**（`tb_restructure_spec.py` 一次性脚本，未进仓库）：specGroups 改两组——「颜色分类」role=variant（**group id 仍叫 style**，兼容 `options["style"]` 的测试与解析器）、16 个组合选项（id 如 `sk-pink`/`facebag-cream`/`bbhat-cream`，name 用淘宝原文「现 sk 粉色」等，image=组合图）；「尺码」role=size 7 档（S/M/L/XS/XL/XXL/F）。SKU 55 条（`tb-<combo>-<size>`，options {style,size}，price=淘宝挂牌价，priceKind=**shop「商品页价」**）。系列 depositMin/Max 清空（链接无定金口径），priceSource 改淘宝采集口径。`variantImageNames` 16 项按淘宝选项名。
- **SeedImages 补 1 张**：`seed-sakura-lamb-bbhat-cream.jpg`（此前 bb帽奶白与立体小脸包共用 accessory-cream），现共 21 张。
- **MidsummerSpecPanel UI**：组标题加「(N)」选项数（淘宝式「颜色分类(16)」）；thumbnailCell 选项卡名称下方新增价格行（`optionPrice(group:option:)` 取 SKU 表命中该选项的第一条带价组合，¥ 橙红色 9pt）。
- **测试**：`MidsummerPriceIntegrityTests` 归集断言更新——variantCount 16、价格集合 {119,229,279,359,369,399,449,599,699,999}、区间 ¥119–999、depositRangeText 断言 Nil（不得虚构定金）。`options["style"]` 相关断言无需改（group id 保留 style 是关键决策）。
- 验证：BUILD SUCCEEDED；6 个 Midsummer 测试类 **76 用例全过**；新包（21 图/16 组合/55 SKU/7 尺码）已装模拟器启动。边界：未做规格抽屉 UI 交互截图验收。

## 详情页头图与文字重叠修复 ✅

用户截图反馈：详情页头图（800×800 方图进「宽×200」槽位）溢出，盖住导航栏标题和「现货/名称/价格」区。根因：`MidsummerCoverView` 里 `scaledToFill()` 图会把容器撑到图片比例高度，`clipShape` 裁不到溢出部分（方图进方槽不溢出，所以只有详情页头图暴露）。修复：组件改为 `Color.clear.overlay { coverContent }.clipShape(...)`——overlay 不参与布局，溢出被真正裁掉；全部 8 个用点自动受益。详情页头图加 `accessibilityIdentifier("midsummer-detail-hero")`。
- 新增 `MidsummerDetailCoverUITests`：XCUITest 导航（时光馆 → 进入品牌档案第 5 个 → 点「樱花小羊」卡片）→ frame 几何断言头图 maxY ≤ 名称/价格 minY + 截图附件。教训：①单品名重构后是「樱花小羊」不是「樱花小羊 SK」，openDetail 找不到时 `guard return` 会让测试**空跑通过**——导航失败的分支必须 XCTFail；②Color.clear+overlay 结构下 SwiftUI Image 不暴露为 image 元素，要挂 identifier 用 descendants(any) 查。
- 验证：BUILD SUCCEEDED；截图确认头图 200pt 槽位内、文字全部在图下方零重叠。
- **最终方案定稿**：`MidsummerCoverView` = GeometryReader 拿槽位尺寸 → `scaledToFill().frame(w,h).clipped().accessibilityHidden(true)`，容器 `.accessibilityElement(children:.ignore)`。fill 图溢出在布局/绘制层被彻底钉死。
- XCUITest 坑补充：`XCUIScreenshot` 本 SDK 无 `cgImage`，走 `pngRepresentation` + ImageIO 解码；同名元素（如「现货」）多处出现时 `staticTexts["x"]` 直接报错，须 query + 按 frame 选（弹窗盖 feed 时取最靠下的）。头图 a11y frame 不可信（始终按未裁剪内容算），回归信号改为「徽章上方间隙带像素均匀性探针」（旧 bug 时该带是照片像素）。
- 验证：UI 验收 `MidsummerDetailCoverUITests` **TEST SUCCEEDED**（几何断言 + 像素探针 + 截图）；Midsummer 单测/快照 66 用例 0 失败；截图确认头图 200pt 槽位内、导航标题与名称/价格等文字全部在图下方零重叠；最新构建已装回模拟器。
- XCUITest 其他坑：同名元素（导航标题与正文商品名都叫「樱花小羊」）要按与锚点元素的相对位置过滤；详情页价格锚点用「商品页价」前缀（「参考价」口径已随 priceKind=shop 变化）。

## HTML 交付物全部重做为 iOS 原生页面 ✅

用户明确：最终落地目标只有原生 APP，此前 HTML 页面（款式分类与尺码表、链接原始信息报告、手机版）均不符合要求，全部重做且不留任何 HTML。

- **数据原生资源化**：新脚本 `/tmp/gen_native_json.py` 把分类页 HTML 解析为 `ItemManager/Resources/Midsummer/midsummer-style-chart.json`（15 分类/24 色卡/16 尺码表，色卡与尺码表图全部映射为 bundle `seed-*.jpg`）；两个 raw_data.json 生成 `midsummer-link-report.json`（2 链接/16+8 颜色分类/55+8 SKU 行）。小物节「一分类两表」按 chart-title 分块解析，title 里混入的价格拆到 priceNote 字段。
- **原生页面**：新增 `MidsummerStyleChartData.swift`（Codable 模型 + bundle 加载 + 图片解析 Bundle→ImageManager 兜底）、`MidsummerStyleChartCatalogView.swift`（目录 chips ScrollViewReader 跳转、色卡横滑、尺码表 SwiftUI `Grid` 原生排版、原图 fullScreenCover 放大）、`MidsummerLinkReportView.swift`（字段行/复制链接/Safari 打开/颜色分类逐项/SKU 折叠明细）。样式全走 MidsummerTheme 令牌，深浅色自适应。
- **路由接入**：`MidsummerRoute` 增 `.styleChartCatalog`/`.linkReport` 两态，品牌页 home 在「全部」行下加两条同款资料入口行（archiveEntryRow）。无任何 WKWebView/HTML。
- **HTML 清理**：删除 output/ 下全部 5 个 HTML（分类页/手机版/两份链接报告/page_snapshot），目录仅剩 img/ 与 JSON 结构化数据。
- **测试**：`MidsummerStyleChartDataTests`（解码 + 内容完整性 + 图片全部 seed- 前缀 + 与种子 JSON 选项名一致性）15 用例 0 失败；`MidsummerArchivePagesUITests` 2 用例（两页导航/渲染/跳转/放大/复制）TEST SUCCEEDED。
- **XCUITest 新坑**：①LazyVStack 只渲染屏内元素，断言目标可能「滚过头被回收」，找不到时先 swipeDown 回顶；②accessibilityIdentifier 含中文会被层级截断，UI 测试用的 identifier 必须纯 ASCII；③`.accessibilityElement(children:.combine)`+identifier 落在 StaticText 上而非 otherElements，用 descendants(.any) 查最稳。
- 验证：单测 15/15、UI 2/2 全过；最新构建已装回 iPhone 18 Pro 模拟器启动。

## 合并樱花小羊系列入口（用户圈选 3 条目 → 1 个链接）
- 需求：品牌页首页「款式分类与尺码表」入口行 + 樱花小羊主链卡 + 小物卡合并为一个入口。
- 实现：
  - `MidsummerBrandView`：`route` 单值改为 `path: [MidsummerRoute]` 导航栈（返回总是回到进入层）；
  - 首页 feed 按系列归组：单链接系列维持原商品卡（点→单品详情），多链接系列渲染新组件 `MidsummerSeriesGroupCard`（¥59–999 全系列口径，橙色圆 chevron，无 ⊕——多链接不替用户挑商品），点→ `.seriesDetail`；
  - 两条资料入口行下沉为共享组件 `MidsummerArchiveEntryRow`，迁入 `MidsummerSeriesDetailView` 顶部「系列资料」区，仅 `MidsummerStyleChartData.ArchiveContent.seriesID`（樱花小羊）显示；
  - 系列详情商品行名称加 ASCII identifier `midsummer-series-item-<itemID>`；首页卡 ⊕ 加 `midsummer-card-insert-<itemID>`（必须挂在 `MidsummerItemCard` 内部按钮上，挂外部会落到 contain 容器）。
- 测试（全绿）：
  - 单测 MidsummerBrandPageTests/PriceIntegrity/StyleChartData 32 例通过；
  - UI 测 ArchivePages(2)/GoodsItem(1)/DetailCover(2)/SpecSelection(3)/TimeHallEntryExploration testA/B/C/D 全部通过。
  - 新坑（已写入技能 12–16 条）：-only-testing 点语法被拒须斜杠语法；dock 压住的 ⊕ 点击会切 tab；规格抽屉滚动要用 spec-confirm 坐标锚点（LazyVGrid 屏外不渲染）；guard-return 空跑的旧测试在导航打通后爆出过期断言（spec-option-* id、投稿页改版锚点、「Sk粉色」→「现 sk 粉色」）——testD 的「名单提示」文案在改版后的投稿表单里已不存在，改为「① 选择类目与基本信息/存草稿」锚点。
- 验收截图：`output/验收截图_合并系列入口/`（首页合并卡 + 单品详情）。

## 系列详情页两条链接合并为一个条目（用户圈选樱花小羊+小物 2 行）
- 需求：系列详情「全部商品」里樱花小羊主链与樱花小羊小物两行合并为一个条目。
- 实现（数据层合并，非展示层拼合）：
  - `midsummer-series.json`：小物 8 个款式选项并入主链「颜色分类」组（24 项）、
    8 条 SKU 并入（63 条）、colors 并集追加「生成色」、sizeChartImages 16 张、
    variantImageNames（字典，按选项名）合并 24 项、note/priceNote 写明两页面来源，
    删除 `midsummer-2026-sakura-lamb-goods` 条目。价格区间自动派生为并集 ¥59–999。
  - 樱花小羊退回**单条目系列**：品牌页首页渲染普通商品卡（点→单品详情），
    `MidsummerSeriesGroupCard` 组件保留但暂无系列命中。
  - `MidsummerSeriesListView` 链接原始信息副标题改用 `MidsummerLinkReport.loadFromBundle()`（items.count 已失效）；
    `allEntryRow` 加 identifier `midsummer-entry-all-series`。
- 测试（68 例全绿）：
  - PriceIntegrity：items.count==1、variantCount==24、区间 59–999、含小物价与「现 草帽 生成色」选项；
  - GoodsItemUITest 重写为合并验收（单卡片/详情 24 款/规格面板含小物选项与 F 码）；
  - ArchivePages/SpecSelection/DetailCover/TimeHall 的导航从「系列组卡→系列详情→商品行」
    简化为「首页合并卡片→详情」（樱花小羊不再是多链接系列）。
  - 踩坑：python 合并 JSON 时 `variantImageNames` 是字典（key=选项名）不是数组，`+` 会炸。
- 验收截图：`output/验收截图_合并条目/`（合并卡片 / 详情 24 款 / 规格面板）。

## 款式分类与尺码表请求复核（用户重发分类整理需求）
- 结论：该需求已由原生「款式分类与尺码表」页完整承载（15 分类 / 16 张尺码表，
  同款不同色归一类、类下尺码表 Grid + 原图，全部转录取自两个淘宝链接）。
- 复核：合并后 24 个款式选项全部被 15 分类覆盖（2+2+2+2+1+2+1+1+3+1+2+1+1+1+2=24）；
  每张尺码表 rows 非空、原图 seed-sakura-chart-*.jpg 均在 SeedImages/。
- UI 测 testStyleChartCatalogPageRendersAllCategories 重跑通过；入口：
  品牌页 → 樱花小羊 → （全部商品 → 系列）系列详情「系列资料」→ 款式分类与尺码表。

## 全量原生交付复核（用户重申「仅原生 App、无 HTML」指令）
- 审计结论：仲夏物语模块已 100% 原生 SwiftUI，无需重做。
  - App 目标（ItemManager/）grep 无 WKWebView/UIWebView/loadHTMLString/HTML 标签；
  - output/ 无任何 .html（此前 5 个已删）；原始信息目录仅 jpg/json/png；
  - 原生工程：Views/Midsummer 7 个视图 + Models 2 个模型 + Resources 3 个 JSON + SeedImages，共约 6400 行；
  - 剩余 HTML 仅 ops/legal-site/（App 上架法务页静态站，运营合规资产）与 build/（GRDB 依赖自带），均不进 App 包。
- 复验：xcodebuild test（3 单测类 + 4 UI 测类）TEST SUCCEEDED；新包已装回 iPhone 18 Pro 模拟器。
- 截图：output/验收截图_全量原生复核/。

## 款式对应图按款式归组 + 组内尺码表（用户圈选 sk 粉/蓝绿合并归类）
- 需求：详情页「款式对应图」原为 24 行平铺；要求同款不同色归一组（SK 粉+蓝绿
  一组、定位JSK 粉+蓝绿一组、内搭独立组……），每组下方内嵌该款尺码表。
- 实现（`MidsummerBrandView.swift`）：
  - 新增 `VariantStyleGroup` + `variantStyleGroups`：选项名「现 <款式> <颜色>」
    去掉「现」后最后 token 是颜色、其余拼为款式族；族名与 `sizeChartImages.style`
    大小写不敏感匹配（sk↔SK / 定位花jsk↔定位花JSK），匹配上的组在其配色行下方
    经 `groupSizeChartRow` 内嵌尺码表图（a11y 标识沿用 midsummer-sizechart-<style>）；
    不足三段的历史命名整名自成一组。24 款 → 16 组。
  - `variantImageSection` 重写为分组渲染：组标题（款式名橙色 + 「N 个配色」）→
    各配色行（保留管理员补录/更换/删除）→ 组内尺码表。
  - 「单品尺码表」折叠面板保留（快速索引 + 兼容无规格组单品）。
- 测试：GoodsItemUITest 新增 3b 断言（「N 个配色」组标题 ≥2、midsummer-sizechart-SK
  在层级中）——37 例全绿；新包装回模拟器。
- 备注：seed-sakura-chart-*.jpg 是淘宝详情长图按空白裁切段，顶部以
  「Product Information 产品信息」卡开头、尺码数据在图片下半部，属素材原貌。

## 详情页信息区改版（用户圈注 4 处）
- 需求与实现：
  ① 详情页徽章（现货/套装）移除——封面下直接名称；
  ② 「商品页价」口径改名「现货价」（`MidsummerPriceKind.shop.labelZH`，全 App 生效，
     含首页卡片 a11y 标签、价格总表后缀）；价格行下新增「预约价」行
     （item.preorderPrice 有值显示 ¥x（全款预约），无值「待补充」）；
  ③ 「包含款式 24 款」行删除（UI 测反断言防回归）；
  ④ 尺码从小到大排序：`MidsummerItemDTO.sortedSizeLabels`（XXS…XXXL<F，
     未知名排后保序），单品与系列 sizesText 共用。
- 测试：37 例全绿（GoodsItem 用例改为：无徽章名称最上、现货价前缀、预约价行、
  尺码排序断言、「包含款式」不存在）；新包装回模拟器。
- 踩坑（已写技能 18 条）：XCBuildData 误判最新导致 test 全程不重编、用旧产物
  跑出假绿；防御=核对本轮 dylib mtime + 截图状态栏时间；touch 后 build 立刻
  暴露真实编译错误（Int? 比较缺解包）。
- 截图：output/验收截图_信息区改版/。

## 16:36 轮：详情页去重 + 配色图并排（用户圈注反馈）
- 删「单品尺码表」折叠区（sizeChartSection + sizeChartRow 整体移除，与款式
  对应图组内嵌尺码表重复）；尺码表统一由各组 groupSizeChartRow 承载。
- 款式组内配色图改 LazyVGrid 固定 3 列并排卡片（方图在上、款式名在下，
  admin 补录/删除按钮收纳为图标）；variantImageRow → variantImageCard。
- 测试同步：GoodsItemUITests「单品尺码表」改反向断言；DetailCover
  testSizeChartsShownPerStyle 重写为直接验收组内嵌图（label + identifier 双断言）。
- 2 类 3 例全绿（真绿：dylib 16:41:13、截图 16:44）。
- 截图：output/验收截图_详情页去重与并排/。

## 17:14 轮：款式卡名去「现」前缀（用户圈注反馈）
- variantImageCard 展示名去掉「现 」前缀（dropFirst("现 ".count)）；key 不动
  （图映射/identifier/上传槽位仍用全名），「现货价」行不受影响。
- 规格面板选项仍显示全名（「现 sk 粉色」），相关测试断言不受影响。
- GoodsItemUITests 1 例通过（真绿：dylib 17:15:38、截图 17:17）。

## 17:25 轮：规格抽屉去「现」+ 价格档位组（用户圈注反馈）
- SpecPanel 展示层剥「现 」前缀（文件级私有 midsummerOptionDisplayName /
  midsummerSpecDisplayText）：选项 chip、a11y label、已选文案、header 摘要、
  确认按钮标题；「现货价」不含空格不受影响；数据层 key / SKU / 入库备注 /
  吐司 / 链接原始信息页（原文照录）全用全名不动。
- JSON：樱花小羊 specGroups 插入 pricing 组（现货价/预约价/定金/尾款，role=other，
  位于 style 与 size 之间）；63 条 SKU 不提及它。
- Resolver 新增 requiredGroups：SKU 表非空时「至少被一条 SKU 提及」的组才必选
  （标注组概念）；isComplete / missingGroupNames 改用它——不选档位不算缺失、
  不丢命中 SKU 的价格（这正是要改完整性规则的根因）。defaultSelection 因此
  不再预选标注组。
- 坑：往 SpecResolverTests 插入新测试时 Edit 吞掉了下一个函数头（多出一个 `}`），
  读文件回检后修复——插入代码务必让 new_string 包住 old_string 的完整上下文。
- 测试：SpecResolverTests 加 testAnnotationGroupIsOptionalAndDoesNotBlockPrice；
  SpecSelection/ArchivePages/GoodsItem 三处 UI 断言同步剥前缀（ArchivePages 的
  链接原始信息页断言是原文照录、已还原保留「现」）。
- 4 类单测+UI 全绿（真绿：dylib 17:32:02、截图 17:35）。
- 截图：output/验收截图_规格抽屉价格档位/。

## 17:44 轮：详情页入库双按钮上移（用户指令）
- 「一键入库 / 加入并编辑」（TimeHallWardrobeInsertButtons, prefix midsummer-detail）
  从页面底部（note 之后）整块移到「款式对应图」上方（尺码行之后）；
  样式/交互/identifier 原样，quickInsertMessage 吐司随按钮一起走。
- 3 类详情页 UI 测试全绿（真绿：dylib 17:46:10、截图 17:50）。
- 截图：output/验收截图_按钮上移/。

## 17:59 轮：一键入库「多选配一套」（用户指令）
- 抽屉新增多选开关（spec-multi-toggle，仅 quickInsert 意图 + 款式组 ≥2 时出现）：
  款式组点击改勾选，尺码/价格档位仍单选；确认按钮带「N 件一套」。
- Resolver 新增 perVariantSelection(for:base:of:)：为每个勾选项生成单品级选择——
  尺码只对「存在带尺码 SKU 的款式」沿用（小物不带），标注组（价格档位）恒带。
- matchedSKU 去掉 isComplete 闸门（改为逐组判定：SKU 提及的组须一致），
  否则无尺码小物未选尺码时命中不了自己的价（单测逮到 ¥59→nil）。
- 入库：每件各落一条 Clothing（复用 makeDraft+QuickInserter），makeDraft 加
  extraNoteLines 参数；套装标记 = 每条备注同写「套装入库：<时间>（N 件一套）」
  +「套装成员：…」，凭标记归为同一套；吐司列成员。
- 顺手修两处过期测试：openDetail 卡片前缀「商品页价」→「樱花小羊，」（此前
  SpecSelection/DetailCover 部分用例空跑假绿）；testDetailHeroImageDoesNotOverlap
  重写（hero a11y frame 是 scaledToFill 溢出图框不可作几何锚点，改「现货价」行基准；
  源码加 .clipped()）。
- 测试：SpecResolverTests +2（perVariantSelection、extraNoteLines）；UI 新增
  testMultiSelectSetInsert 全流程通过；33 例相关全绿（dylib 18:19:18）。
- 技能补坑 19-21。截图：output/验收截图_多选配一套/。

## 18:30-19:30 轮：樱花小羊「上新工作台」（用户指令：商品上传上新系统 + 衣橱深度结合）
- 新文件：MidsummerListing.swift（listing 模型 + makeItemDTO 继承转换）、
  MidsummerListingStore.swift（本地 JSON 持久化 + 图片落盘 midsummer-listing-
  命名空间）、MidsummerListingWorkspaceView.swift（工作台 + 三步表单）。
- 继承规则（makeItemDTO）：款式组只保留勾选项（全名）、尺码组「S↔S码」容错匹配、
  价格档位组原样继承、款式对应图按下标映射、尺码表按「全名包含短名」继承
  （「现 sk 粉色」⊃ SK）、SKU 不继承（nil）、出处回退系列出处（Apple 5.2）。
- MidsummerStore 订阅 ListingStore（receive(on:main) 跳出 willSet）自动重算 feed；
  首页 feed 单链接/组卡判定只看基础条目（id 非 midsummer-listing- 前缀），
  上架新品以独立卡追加——否则一上架就把樱花小羊单品卡变组卡。
- 入口：系列详情 summaryCard「上新管理」（canContribute 三态门控 + 限樱花小羊）；
  CreatorMode 加 -ui-test-enable-creator-mode 启动参数解模拟器闸门。
- 测试：MidsummerListingTests 11 例（继承规则/存储/状态机）+ UI 全流程
  testPublishListingAndInsertToWardrobe（发布→上架→系列可见→一键入库）；
  既有 5 类 Midsummer UI 回归全绿（PriceIntegrity 读种子 JSON 不受存档影响）。
  真绿：dylib 18:54:26、截图 19:17。
- UI 测试坑：价格总表行与商品行同名（改用 midsummer-series-item-<id> 前缀锁定）；
  键盘「完成」按钮不可依赖（name 用 \n、numberPad 用 swipeDown 收起）；
  工作台 sheet 未关时同名文本双匹配（等 listing-open-form 消失再操作）。
- 遗留：上架存档在模拟器里跨轮累积（验收截图里已上架 4 条），需要时可在
  工作台逐条删除；图片上传流程 UI 测试未覆盖（表单支持但 XCUITest 驱动
  系统相册不稳定），由单测覆盖落盘/替换语义。

## 上新工作台改版（用户 20:00 前后，按用户新设计稿根因重做）
- 根因：旧表单是「单品级 3 步」（基本信息/商品图片/预览发布），用户改版稿是「系列级上新 4 步」（类目与信息/系列主图/单品与价格/预览提交）。
- `MidsummerListing` 新增可选字段（默认 nil，旧 JSON 兼容）：`stageRaw`/`launchTitle`/`hasKnownLaunchDate`/`launchDate`/`depositMin`/`depositMax`；新增 `MidsummerLaunchStage`（图透/定金/尾款/出货/再贩/现货，含 SF Symbol 图标）。
- `MidsummerListingFormView` 整体重写：顶栏「取消/存草稿」胶囊（任意步可存草稿，无名时落「未命名草稿」）；4 节点双行标签步骤条；第 1 步=上新阶段 chips(3 列带图标)+系列标题+已知上新日期(toggle 粉色+DatePicker)+定金区间(最低–最高元)+可用尺码(固定 7 项 XS/S/M/L/XL/均码/定制，不再追加系列尺码避免杂项)+系列说明；单品字段后移第 3 步。
- `MidsummerTheme` 新增 `accentPink` 令牌。
- 测试：单测新增 `testLaunchFieldsRoundtrip`/`testLegacyJSONDecodesWithNilLaunchFields`（13/13 绿）；`MidsummerListingFlowUITests` 改 4 步流程全绿（3 轮迭代：修尺码候选两次）。dylib mtime 20:10:35 真绿。
- 验收截图 `output/验收截图_上新工作台改版/`。注意：模拟器跑在浅色模式，截图是浅色皮；图 1 的深色是同套令牌的深色自适应，非样式缺失。

## 上新表单二次改版（用户 23:16，圈注步骤条「融合为一个步骤，不要1234」）
- 老 4 步（类目与信息/系列主图/单品与价格/预览提交）融合重组为新 4 步引导流程：
  ① 系列主图与信息（主图宫格+系列标题+已知上新日期+系列说明）→ ② 选择上新阶段 → ③ 尺码信息 → ④ 单品与价格（最后一步直接「发布上架」，原预览步取消）。
- 价格项与阶段联动（`MidsummerLaunchStage.priceFields`/`priceHint`）：图透=无、定金=定金+尾款(+定金区间)、尾款=尾款、出货/现货=现货价、再贩=现货价+预约价；定金/尾款标「可选」按需配置；阶段 chip 下实时显示该阶段价格配置预览。
- 必填校验逐步：①标题（≤30 字）②阶段必选 ④名称+关联款式+URL 格式；尺码可选。
- 单测新增 testStagePriceFieldsLinkage（14/14 绿）；UI 测试改新流程 + 断言现货阶段无定金/尾款字段，全绿（dylib 23:22:53 真绿）。截图归档 output/验收截图_上新工作台改版/（84/85/87/91/94/95/97）。

## 00:00 轮：上新上传去门控收口（用户指令：全部用最新工作台版本，旧版删掉）
- 用户反馈「测试没有」根因：入口被 `canContribute` 三态门控（CloudKit 白名单/创作者模式）+ 仅限樱花小羊系列挡住——代码在但入口不可达（真机非白名单=denied 连创作者模式都撬不开）。
- 删除：`MidsummerContributeView.swift`（1740 行旧投稿表单）+ `MidsummerContributeViewTests.swift`；品牌页顶栏「上传上新」按钮（MidsummerTopBar 的 canContribute/onContribute 参数）；页脚创作者模式引导块（MidsummerDataNote 收敛为纯信息说明）；系列详情「补录尺码表/价格表」入口；规格抽屉运营者补录入口（supplementEntry）；`MidsummerStore.canContribute` 计算属性。
- 保留：`creatorGate`/`isAdminUser`（仅 CloudKit 写入权限判定用）、CreatorMode 开关（设置页还在，暂成死开关——后续可议删）。
- 改为无条件：系列详情「上新管理」所有系列开放（去掉 canContribute && 樱花小羊限制）。
- 测试同步：快照测试两个顶栏用例合并为 testTopBarExportsSnapshot；TimeHallEntryExplorationUITests testD 重写为 testD_ListingWorkspaceEntryWithoutGate（免启动参数直达工作台→4步表单）；ListingFlowUITests 去掉 creator-mode 启动参数。
- 验证：主 target BUILD SUCCEEDED；TEST BUILD SUCCEEDED；单测 38 例全绿（Listing 14/BrandPage 17/Snapshot 等）；UI 测 MidsummerListingFlowUITests 全流程 + TimeHallEntryExplorationUITests 4 例全绿（真跑，截图状态栏 00:20-00:24）。
- 截图：output/验收截图_上新工作台去门控/（复现了用户六图全链路 84/85/87/91/93/94/95/97 + 免门控入口 30/31/32）。新构建已装回 iPhone 18 Pro 并启动。
- 经验：改 UI 结构时同一消息里对同一文件发多个 Edit 会互相覆盖，且反向 Edit（new_string 含 old_string 已删内容）会把删掉的东西加回去——同文件编辑必须串行+每次回读确认。
