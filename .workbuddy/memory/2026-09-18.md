# 2026-09-18


## 表单↔详情模板对照：分类多选 + 库存小字 + 款式批量录入
- 用户给两图：图一=上传上新第③步表单，图二=商品详情页模板（樱花小羊主条目详情 sheet，标题=系列名、右上角「完成」）。明确：**图二只是展示模板对照基准，不是要把上新并入该系列**；新商品仍走独立单品链路。
- 四点确认（AskUserQuestion）：①商品分类改**多选**；②价格横幅下固定展示「不跟店库数量 · 不影响品牌库存」；③款式加**快速批量录入**；④上传上新保持独立链路。
- `MidsummerListing.swift`：新增 `kindRaws: [String]?`（可选，旧档兼容）+ `kinds` 计算属性——getter 兼容旧档（nil→[kind]，无效 raw 跳过、全无效回退 kind）；setter 去重并同步 kindRaw=首个主分类，`makeItemDTO` 等旧代码读 kind 不失效。
- `MidsummerListingFormView.swift`：
  - draft.kind → draft.kinds（默认 [.op]）；kindCard chips 改多选 toggle（至少保留一个，最后选中项不可取消）；hint 注明「第一个选中的是主分类」；汇总卡新增「商品分类」行。
  - 款式区新增「批量添加款式」折叠区：TextEditor 多行输入，换行/顿号/中英文逗号/分号分隔；`parseBatchStyleNames(_:)` 抽成 static 纯函数供单测；addStylesInBatch 去重跳过并反馈「已添加 N 个，跳过重复 M 个」。identifier：listing-style-batch-toggle / -input / -add。
- `MidsummerBrandView.swift` `MidsummerItemDetailSheet`：价格 headline 下方固定小字「不跟店库数量 · 不影响品牌库存」（identifier midsummer-detail-stock-note）。
- `MidsummerListingWorkspaceView.swift`：列表行分类显示改 `kinds.map(shortLabel).joined("/")`。
- 测试：`MidsummerListingTests` 新增 5 例（多选 setter 同步主分类/旧档回退/去重/无效 raw 容错/批量解析），51/51 全绿（iPhone 18 Pro OS=27.0）。UI 全流程 testPublishListingAndInsertToWardrobe 回归通过（iPhone 17 Pro OS=26.3.1）。
- 坑：`XCTAssertEqual(listing.kindRaws, [.skirt.rawValue...])` 会把字面量推断成 [String] 编译错——可选数组与字面量比较需写全类型名（`MidsummerItemKind.skirt.rawValue`）。

## 表单③再精简：分类款式合并 + 商品名自动 + 尾款自动核算（深夜第二轮）
- 用户 6 点指示（对照第③步截图）：①删商品名称字段；②分类与款式合并、录入自动拼「分类名+颜色」；③保留批量添加；④删价格字段；⑤保留定金/尾款两项配置；⑥尾款不手填、自动计算并区分显示。
- 口径确认（AskUserQuestion）：尾款=预约价−定金（保留预约价输入）；商品名=款式名自动生成**且可修改**；款式逐款价一并删除；定金/尾款固定显示（不再按②阶段联动）。
- `MidsummerListingFormView` 大改：
  - `MidsummerListingStyleDraft` 删 priceText；draft 删 priceText/priceKind/balanceText/balanceEnabled/depositMinText/depositMaxText/visiblePriceFields；新增 nameManuallyEdited + autoBalance（预约价−定金，差值需为正）。
  - nameCard/kindCard/stylesCard 三卡合并为 categoryStyleCard（分类 chips + 款式区 + 自动商品名行，Divider 分隔）；步骤③改名「分类与款式」。
  - `autoStyleName(for:)`：输入不含已选分类 shortLabel / 大类名前缀 → 主分类 shortLabel + " " + 输入（「粉色」→「sk 粉色」）；自带前缀原样。自定义新增与批量添加都走它。
  - `syncAutoName()`：增删款式/改款名时，未手动改过商品名则跟随首个款式名；nameBinding 一输入即置 nameManuallyEdited。
  - 价格卡重写：预约价（全款）输入 + 定金开关/金额 + 尾款自动行（listing-balance-auto，橙底区分显示）+ 核算等式（定金+尾款=预约价）+ 差值≤0 报错行（listing-balance-error）/ 未配齐提示（listing-balance-hint）；删现货价/口径 picker/定金区间行/syncPreorderFromDepositBalance。
  - 校验新规则：预约价/定金整数；至少填预约价或开定金；开定金必填预约价；预约价>定金；删旧「定金+尾款=预约价等式」校验（天然成立）。
  - assembledListing：price=nil、priceKind=nil、balance=preorder−deposit（自动）、depositMin/Max=nil、款式 price=nil（旧档逐款价编辑保存后不保留——口径取舍，已记）；商品名兜底=首款式名。
  - prefill：编辑模式 nameManuallyEdited=true（存档名不被自动覆盖）；不再回显现货价/逐款价/定金区间。
- UI 测试适配：主流程断言改「价格卡固定显示预约价+定金开关」、现货价改填预约价；testDepositAndBalanceAreOptionalToggles 重写为「尾款自动行出现（199−50=149）/关定金回提示」；头注释 identifier 契约同步（listing-balance-auto/-hint/-error、listing-style-batch-*，删 listing-price/style-price/deposit-min/max）。
- 编译两处小坑：可选解包 `if depositValue != nil, let preorderValue, preorderValue <= depositValue` 里 depositValue 仍是 Int? → 改 `if let depositValue, let preorderValue, ...`。

## （续）UI 测试联调三连坑与修复（表单是可下拉关闭的 sheet）
- 坑 1：`.accessibilityIdentifier` 直接挂在 HStack 上会泄漏/合并到子元素造成多匹配（「Failed to get matching snapshot: Multiple matching elements」）——改 `.accessibilityElement(children: .combine)` 再挂 identifier，label 也合并（金额断言用它）。
- 坑 2：表单 sheet 可从内容任意位置下拉关闭——scrollToElement 的 swipeDown 循环在内容滚到顶后会把 sheet 拉关，后续全部元素消失（dump 里直接回到系列详情页）。修复：①scrollToElement 双向化+元素消失立即止损+向下滑限 3 次；②测试操作顺序严格**自上而下**（款式→商品名→尺码→价格），全程只 swipeUp，从根上避开回滚。
- 坑 3：数字键盘没有 return 键，`app.swipeDown()` 落点正好在键盘上=无效手势，键盘永远收不掉（P2 截图实锤）。修复：dismissKeyboard 改为对 `app.scrollViews.firstMatch` 下拉（scrollDismissesKeyboard(.interactively) 跟手收键盘）。
- 坑 4：商品名自动生成后字段非空，typeText 是光标处**追加**——先按 `value.count` 发 `\u{8}` 删净再输入。
- 最终：UI 4/4 全绿（deposit 77s / publish 98s / size 69s / validate 49s），单测 40/40（早前跑）。验收截图 `output/验收截图_表单精简尾款自动/`（11 张）。

## 上新指令链从樱花小羊扩展到仲夏物语全系列（2026-09-18 上午）
- 用户指令：把此前围绕樱花小羊做的整套上新/展示规则（四步表单、分类款式合并、自动商品名、批量录入自动拼名、四类价格、预约价+定金固定显示、尾款自动核算、定金-尾款状态机）统一应用到仲夏物语全部 13 个系列。
- 审计结论：产品代码本就按 `series: MidsummerSeriesDTO` 参数化（工作台/表单/store/详情页/入库/状态机全链路 series 无关），无硬编码系列分支；真正系列绑定的只有两处：①`MidsummerStyleChartData.ArchiveContent.seriesID` 写死资料入口；②UI 测试全从樱花小羊进入。
- 改动：
  - `ArchiveContent.seriesID` → `seriesIDs: Set<String>`（数据驱动清单）；`MidsummerSeriesListView.archiveSection` 改 `contains(series.id)`。新系列补齐 style-chart/link-report json 后把 id 加进清单即自动开放「系列资料」区块。
  - 注释口径清理：MidsummerListing.swift（文件头/seriesID/variantOptionNames/makeItemDTO）、MidsummerListingStore.swift、MidsummerListingWorkspaceView.swift（樱花小羊→仲夏物语全系列）。
  - UI 测试：`enterSakuraSeries` 参数化为 `enterSeries(app, name:)`（默认樱花小羊，旧调用点零改动）；新增 `testPublishListingOnAnotherSeries`——在「小熊博物馆系列」完整走指令链：批量录入「白色、黑色」自动拼「OP 白色/OP 黑色」→ 商品名自动生成=首款式 → 尺码 → 预约价 259 + 定金 60 → 尾款自动 199 → 发布 → 工作台已上架 → 系列详情可见。capture 前缀 C0–C15。
- 编译 TEST BUILD SUCCEEDED；测试结果见后续条目。
- 测试结果：testPublishListingOnAnotherSeries 通过（95s）；MidsummerListingFlowUITests 全套 5/5 全绿（395s）。
- 联调坑（跨系列用例）：①款式行容器 identifier `listing-style-row-<i>` 合并覆盖行内 `listing-style-name-<i>`——按 `app.textFields["listing-style-row-0"]` 类型过滤定位；②批量 TextEditor 键盘不收会让尺码 chips 处于键盘后（isHittable=false），且表单在顶部不能下拉收键盘（拉关 sheet）——点「收起批量添加」失焦最安全；③S/M chips 会被滚出视口——用 identifier IN 预设清单取第一个 hittable 的 chip；④跨系列用例商品名是自动生成的「OP 白色」，工作台断言目标不要用未输入的 itemName。
- 验收截图：`output/验收截图_全系列统一上新/`（C10 尾款自动核算 / C13 工作台已上架 / C15 系列详情可见）。

## 入库功能：解析填字段 + 多件合并一条衣橱记录（2026-09-18 第三轮）
- 用户三点需求（国牌=时光馆仲夏物语）：①单件入库解析商品信息填衣橱四字段（类型/颜色/尺码/价格）：「sk 粉色」→SK+粉色、「无腰op粉色」→op+粉色、尺码按用户实际选择、定金尾款类型价格填定金尾款信息；②多件入库合并同一条衣橱记录不拆分；③解析准确无误。
- `MidsummerSpecResolver` 新增纯函数：`parseVariantFields(_:)`（剥「现 」前缀→长词优先找分类短标（jsk 不被内部 sk 截胡、大小写不敏感）→类型=短标、颜色=短标后剩余文本剥分隔符；无短标返回 (nil,nil) 不猜）；`selectedColorName` / `selectedSizeName`（显式选择）。
- `MidsummerWardrobeDraftBuilder.makeDraft` 字段口径：types=款式名解析>单品 kind；colors=显式颜色组>解析颜色>单品配色；sizes=显式尺码>单品尺码；价格：定金计划（item.deposit!=nil）→ deposit=item.deposit、balance=item.balance ?? 预约价−定金、总额=预约价、isDepositPlan=true、备注写「价格口径：定金 ¥x + 尾款 ¥y = 预约价 ¥z」；现货→SKU 逐款价>现货价>预约价。
- 新增 `makeSetDraft`（合并口径）+ `MidsummerWardrobeInserter.quickInsertSet`：多选配一套由「每件一条 Clothing+备注套装标记」改为**一条记录**：名称=成员款式名「＋」连接+（套装）；types/colors/sizes 成员去重合并（顿号）；价格=成员求和（定金/尾款分别可对账）；stock=套数；备注保留套装标记+成员清单+逐成员规格与价格。`MidsummerBrandView.quickInsertSetToWardrobe` 改走合并链路（吐司文案不变，UI 测试无需改）。
- 测试：MidsummerSpecResolverTests +12 例（解析 7 + 单件字段 2 + 合并套装 2 + 既有 1），49/49 绿（iPhone 18 Pro/27.0）；MidsummerSpecSelectionUITests 4/4 绿（含 testMultiSelectSetInsert）；截图 `output/验收截图_入库解析与合并/`。

## 创作者价格编辑入口（2026-09-18 第四轮）
- 用户需求：系列管理里补价格编辑入口——改现货价/预约价/定金/尾款，校验与显示口径同现有字段，覆盖全部已录入商品（含不同状态），价格表可编辑可保存，商品图可上传/替换/放大预览，价格表在合适位置展示。
- 三点口径确认（AskUserQuestion）：①范围=上新商品（本地）+ 基础条目（CloudKit 发布）；②尾款**可手动改**（留空且预约价/定金齐全时保存自动补「预约价−定金」，手改后不强制等式）；③入口=工作台 + 系列详情页双入口。
- 新文件 `MidsummerPriceEditing.swift`：
  - `MidsummerPriceValidator` 纯函数（parseAmount/validate/resolvingAutoBalance/consistencyHint/autoBalance）——整数金额（元）、非负、至少一项；与表单「需填整数金额（元）」同文案。注意 `Result` 的 Failure 必须是 Error → 包了 `MidsummerPriceFieldError`。
  - `MidsummerListingPriceEditSheet`（本地 listing）：四价字段 + 商品图宫格（添加/点图替换/删除/设为主图/🔍放大预览），**点保存才生效**；saveImages 是替换语义且按 `midsummer-listing-<id>` 前缀清图会**连带删款式图**——必须在 saveImages 前把款式图读进内存（styleImages 字典），保存后按原槽位 saveStyleImage 重写（与表单保存同序）。有现货价而原本无口径→补 .shop；清空现货价→口径一并清。旧档只有定金+尾款时预填预约价=定金+尾款。
  - `MidsummerItemPriceEditSheet`（基础条目）：同四价字段，保存走 `MidsummerCloudService.publish` 整条重写（主图/附图/款式图按现存文件带上）→ `refreshFromCloud()` 全量回读；改了现货价才更新 priceCapturedOn。
- 工作台 `MidsummerListingWorkspaceView`：新增「系列价格总表」卡（publishCard 下，分组口径同系列详情：现货价/全款预约/定金尾款/待补，含草稿/已上架/已下架全部 listing，非已上架带状态小标）；每行可点（listing-price-row-<id>）进改价面板；商品行加「改价」按钮（listing-price-<id>）；保存后 upsert→updatedAt→订阅重算 feed+吐司。
- 系列详情 `MidsummerSeriesListView`：PriceRow 加 itemID；`isCreatorMode` 参数（品牌页传 viewModeStore.isCreator；快照测试直构默认 false 不受影响，**不能**用 @EnvironmentObject——快照测试裸构造会崩）；创作者视图下总表行可点（series-price-row-<itemId>）→ PriceEditTarget 路由：能反查 listing→本地面板，否则→CloudKit 面板；普通用户纯展示。
- 测试：`MidsummerPriceEditingTests` 12 例全绿；UI 新增 `testEditListingPriceInWorkspace`（发布现货品→总表点行→面板回显 199→清预约价填现货价 399→保存→总表「¥399」→系列详情总表「¥399（现货价）」），MidsummerListingFlowUITests 6/6 绿（iPhone 17 Pro/26.3.1）。既有 6 例宠物模块失败为历史遗留，与本次无关。
- 截图：`output/验收截图_价格编辑入口/`（P6 总表 / P7 面板回显 / P9 保存刷新 / P10 详情同步）。

## 创作者价格编辑入口 · 复核补齐（2026-09-18 第五轮，用户重发同一需求）
- 复核结论：第四轮功能已在 HEAD（`MidsummerPriceEditing.swift` 两个面板 + 工作台/系列详情双入口），但对照 6 条要求仍有 3 处真缺口，本轮补齐：
  1. **CloudKit 发布漏字段（真 bug）**：`MidsummerCloudService.publish(item:)` 写了 price/deposit/balance/priceKind，唯独**没写 preorderPrice**，而 `makeItem` 读取侧会读它 → 基础条目改预约价「保存成功、回读复原」。已补 `if let value = item.preorderPrice { record["preorderPrice"] = ... }`。
  2. **校验未对齐表单联动**：原 validate 只有「整数/非负/至少一项」，缺表单第 3 步的两条硬规则。已补：定金已填 → 预约价必填（「已配置定金：请再填预约价（全款），尾款会自动算出。」）；预约价 ≤ 定金 → 报错（「预约价需大于定金（当前差值 ¥X），否则算不出尾款。」），文案与 `MidsummerListingFormView` 逐字一致。
  3. **基础条目面板缺商品图入口**：抽出 `MidsummerPriceImageGrid`（images 绑定 + onChange 打 dirty，identifier 沿用 price-edit-image-* 全套），工作台面板改用之，`MidsummerItemPriceEditSheet` 新增该卡——prefill 从 coverImage+galleryImageNames 读图，保存时 dirty 才用编辑后宫格发布（`existingImages(of:)` 兜底未改动场景，publish 整条重写缺图会被抹掉）。
- 旧档兼容：新增 `MidsummerPriceValidator.prefill(preorderPrice:deposit:balance:)`——定金+尾款→补预约价=定金+尾款；**只有定金**（历史「全款预约价存定金里」，与 priceGroups 同口径）→ 迁移成「预约价=定金、定金留空」并置 `didMigrateLegacyDeposit`，面板显示说明行（price-edit-legacy-hint）。否则一进面板就被新增的必填校验拦死。
- 测试：`MidsummerPriceEditingTests` 12→19 例（新增联动校验 3 + 预填迁移 4），19/19 绿（iPhone 18 Pro/27.0）；`build-for-testing` **TEST BUILD SUCCEEDED**（含 UI 测试目标）。
- UI 用例本轮**没跑绿**（与改动无关，如实记录）：① 第一次 `testEditListingPriceInWorkspace` 在第 1 步 `focusAndType` 就失败——「Neither element nor any descendant has keyboard focus」，727s 后 fail，失败点在系列标题输入，远早于改价面板；② 第二次 test runner 启动即崩（`XCTWaiter(StallHandling) handleStalledWait`），一条都没执行。均为机器负载/模拟器卡顿。顺手把 `focusAndType` 的固定 `usleep(700ms)` 改成 `app.keyboards.firstMatch.waitForExistence(timeout: 4)`（负载高时键盘弹出慢，固定 sleep 会误判没聚焦）。该用例在第四轮（改动前）为绿。
- 环境坑（本机）：① `-destination 'name=iPhone 17 Pro'` 解析成 `OS:latest`(=27.0) 而该机型只有 26.3.1 → 报「Unable to find a device」，单测用 **iPhone 18 Pro**；② 单独跑 `-only-testing:ItemManagerTests/MidsummerListingTests` 时 xcodebuild 卡在 `CopySwiftLibs --sign`（UITests 目标签名阶段）**47 分钟无进展**，只能 kill——与该测试代码无关；③ 机器负载高时 UI 用例每一步都要等 60s app idle，一个用例十几分钟。

## 基于角色的权限控制（2026-09-18 第六轮）
- 需求：普通用户看不到也用不了创作者专属功能（上架管理/改价/换图）；仅创作者可操作；覆盖「创作者自建系列」+「历史内容与外部渠道（淘宝等）导入条目」两类内容，两类处理逻辑与校验一致；前端隐藏入口，服务层统一校验防绕过；不动普通用户浏览/购买。
- 改动前两个敞口（真问题）：①系列详情「上新管理」自 2026-09-16 起**对所有用户无条件开放**；②服务层（本地存档/CloudKit 发布）**无任何角色校验**，绕过界面即可写入。
- 新增单一真值 `ItemManager/Services/CreatorAccess.swift`：`CreatorRole`(viewer/creator) + `CreatorOperation`(8 种动作) + `CreatorAccessDenied`(带「哪一步/为什么/怎么办」) + `CreatorGateSnapshot`(NSLock 保护的判定快照，供同步校验) + `Error.userMessage` 扩展。判定矩阵：allowed→creator；denied→**viewer（本机开关撬不开）**；unresolved→看 `CreatorMode` 本机开关（模拟器/未登录 iCloud 维护者通路）。
- 服务层校验：`MidsummerListingStore.upsert/updateStatus/delete/saveImages/saveStyleImage` 全部 `throws` + 首行 `requireCreator`（saveImages 校验在**删旧图之前**，否则被拒也把原图清了）；新增私有 `write(_:)` 无校验落盘通道给**预售相位自动流转**用（时间驱动的系统行为，对普通用户也要生效，不能被门控挡住）；`MidsummerCustomSeriesStore.add`；`MidsummerCloudService.publish(series:/item:)`（建 CKRecord 之前）。
- 前端：系列详情「上新管理」按 `canEditContent=CreatorAccess.shared.isCreator` 门控；工作台发布/编辑/改价/上下架/删除 + 价格表行可点性按 `canEdit`（非创作者显示只读说明）；两个改价面板非创作者显示 `MidsummerCreatorOnlyNotice` 且无保存按钮；品牌页上传上新/视角切换/卡片编辑角标/款式图上传均加角色判断（`viewMode.isCreator && creatorAccess.isCreator`）。命名统一 `creatorAccess`（避开 POSIX `access()` 全局函数）。
- 设置页：denied 时开关 `.disabled` 锁定（不能自提权），新增「当前角色」行 + `.task { await refresh() }`。`MidsummerStore.canEnterCreatorView` 改为转发 `CreatorAccess.shared.isCreator`（creatorGate 仅作镜像刷新用）。`CreatorMode.setEnabled` 调 `CreatorAccess.refreshBasis()` 同步文案。App init 里 `_ = CreatorAccess.shared` + 启动 `refresh()`。
- 测试：新增 `CreatorAccessTests` 11 例（角色矩阵 4 + 服务层拦截 5 + 自动流转不受影响 1 + 实时读开关 1）全绿；既有 `MidsummerListingTests`/`MidsummerPresalePhaseTests`/`MidsummerCustomSeriesStoreTests` 在 setUp 注入 `.creator` 并把写调用改 `try!`，合 61 例全绿；`build-for-testing` TEST BUILD SUCCEEDED。
- UI 用例口径变化（行为变了，必须同步）：`MidsummerListingFlowUITests.launchApp` 加 `-ui-test-reset-creator-mode -ui-test-enable-creator-mode`；`TimeHallEntryExplorationUITests.testD_...WithoutGate` 改名 `...ForCreator`（需解闸），新增 `testD2_ListingWorkspaceEntryHiddenForViewer`（普通用户滚一圈确认无上新管理入口、无 series-price-row-* 按钮，但价格总表仍只读可见）。
- 文档：`docs/MIDSUMMER_CREATOR_ROLE_PERMISSION.md`（敞口/判定矩阵/前端清单/服务层清单/两类内容一致性/6 条边界）。
- 复用坑：`@ObservedObject` 属性别叫 `access`（与 POSIX `access()` 撞名，报「value of type '@Sendable (UnsafePointer<CChar>?, Int32) -> Int32' has no member 'isCreator'」）；`ObservableObject` 类里用 `@Published` 必须 `import Combine`。

## 入口跳转层级修复 + 上新管理页面级门控（2026-09-18 第七轮）
- 需求 C：①「入口跳转异常」——图一红框那张卡点进下一级与预期不符，要求跳转结果与所属层级一致；②「上新管理」仅创作者可用，非创作者看不到任何入口、深链也进不去，并排查其他入口。
- **先找回图一**：上下文压缩后原图已从对话消失，从 `~/.workbuddy/clipboard-images/`（18:31 那张 jpg）+ `~/.workbuddy/blobs/` 找回。红框圈的是品牌首页「浪漫蔷薇」那张**系列入口卡**（橙色 chevron、文案「浪漫蔷薇 系列 · 1 个商品链接」、¥258）。此路径已记入 `~/.workbuddy/MEMORY.md`（跨项目通用）。
- **①根因不在导航代码**，在首页 feed 决定「这一行出哪种卡」的判定：`MidsummerBrandView.itemList` 只按**基础条目**数量分支（`baseItems.count == 1` 出商品卡，否则一律出 `MidsummerSeriesGroupCard`）。自建系列（上传上新直达新建）没有基础条目 → 掉进「组卡」分支被当成多链接系列 → 首页同时出现「系列入口卡」和「同一件商品的商品卡」两张，同一件东西跨两个层级，且两张卡落点不同（系列卡→系列详情，还要再点一次才到商品）。
- 修法（`MidsummerBrandView.itemList`）：按**本系列实际有几件商品**判层级。①唯一基础条目 → 商品卡（不变）；②**无基础条目 + 恰好 1 条上架商品 → 商品卡（新增，本次修复）**；③其余（多基础条目 / 多条上架商品）→ 系列入口卡。既有交互保留：樱花小羊仍首页直开单品详情。
- **②页面级（深链）门控补齐**：`MidsummerListingWorkspaceView.body` 已是 `if canEdit { … } else { viewerNotice }`；`MidsummerListingFormView` 上轮编辑被限频截断未落盘，本轮补齐——`body` 拆成 `formContent` + `viewerPage`（`listing-form-viewer-notice`，不预填草稿、不渲染四步表单，只留「完成」关闭）。
- 文案收敛：`CreatorAccess` 新增 `deniedGuidance`（复用 `CreatorAccessDenied.recoverySuggestion`），工作台整页拒绝 / 表单整页拒绝 / `MidsummerCreatorOnlyNotice` 三处共用，避免文案漂移。
- 入口盘点（`MidsummerListingFormView` / `MidsummerListingWorkspaceView` / 两个改价面板的全部 present 点）：品牌页顶栏 sheet、工作台两个 sheet、系列详情两个改价 sheet——入口层都已门控，页面自身再守一次即可，无遗漏。
- 验证：`build` BUILD SUCCEEDED。临时探针 `_ProbeCardJumpUITests`（**改成 `print("PROBE|…")` 打到 xcodebuild 日志**，不再依赖会被清掉的 xcresult 附件）：修前首页 label = `浪漫蔷薇，系列入口，1 个商品链接，¥258`，点它 → 系列详情（`series-price-table` + `series-listing-workspace-button` + 上新（定金）1 件）；修后 `midsummer-series-card-midsummer-custom-rosedemo` 在首页**消失**，浪漫蔷薇那行变成商品卡 `吊带粉色，定金 ¥50 · 尾款 ¥199`，点它 → 单品详情（`midsummer-detail-hero`）；樱花小羊商品卡回归检查 = true。探针跑完已删（工程用 `PBXFileSystemSynchronizedRootGroup`，删文件不用改 pbxproj）。
- 模拟器数据：`midsummer-custom-rosedemo`（浪漫蔷薇，2026/定金期/尺码 S）+ `upload-rosedemo1`（吊带粉色，现货 ¥258 / 预约 ¥249 / 定金 ¥50 + 尾款 ¥199）。
- 文档：`docs/MIDSUMMER_CREATOR_ROLE_PERMISSION.md` 新增「三、前端门控清单」两行（两个整页拒绝）+ 新章节「八、附带修复：首页 feed 的卡片层级 == 跳转目标层级」。
- 环境坑：`-destination 'name=iPhone 17 Pro'` 会歧义（同名多 runtime）或解析到不存在的 OS；用 `xcrun simctl list devices available` 拿**准确 UDID** 再 `id=<UDID>`。本轮一度把 UDID 抄错成 `…-A281-2F82C7F631FF`，xcodebuild 报的是「Unable to find a device matching…」而不是参数错，容易误判设备被删。
- 回归验证（iPhone 17 Pro / 26.3.1，`826F5E71-D2BC-4E76-A8C5-4251C526A5D4`）：`ItemManagerTests` 全跑，唯一失败 5 例全在**宠物模块**（`PetChatGuidanceEngineTests` ×2、`PetGenerativeUITests` ×3），与本轮无关（历史遗留，见上轮记录）；非宠物 0 失败。权限门控 UI 用例 `testD_ListingWorkspaceEntryForCreator` / `testD2_...HiddenForViewer` / `testF_NormalUserSeesNoCreatorControls` **3/3 绿**。最终 `build` BUILD SUCCEEDED。
- 用户确认（本轮）：图二就是「浪漫蔷薇系列详情页」，即点系列卡进了系列层页面 —— 与本次修复口径一致（期望直接看到那件商品的详情）。已记：给用户看复现结论后确认，避免只凭截图猜。

## 移除商品卡「一键入库」快捷入口（2026-09-18 第八轮）
- 用户截图圈出品牌页每张商品卡右侧的橙色 ⊕ 圆钮（形态 A 一键入库快捷入口），要求创作者/用户两种界面同步移除，其他入库功能（详情页一键入库+规格面板、加入并编辑）保留。
- 改动：①`MidsummerBrandView.MidsummerItemCard` 删掉 `onQuickInsert/isInserting/didInsert` 三参数、`wardrobeInsertButton` 及其 HStack 挂载点，注释改写；`MidsummerHomeContent` 删 `quickInsertToWardrobe`/`insertToastText`/`insertingItemID`/`insertedItemIDs`/`insertToast` + 顶部吐司 overlay。②`MidsummerSeriesListView`（系列详情商品行）删行内 ⊕（「查看」保留为唯一右侧元素）、删同名 func + 状态 + 吐司 overlay。③删除已无使用者的共用组件 `MidsummerWardrobeIconButton`（BrandView 内）。
- 测试：`MidsummerSpecSelectionUITests.testCardQuickInsertReportsChosenSpecs` 整例删除（专测卡片快捷入口）；`TimeHallEntryExplorationUITests.testB_MidsummerWardrobeEntries` 断言 2 反转为「商品行上不应再有一键入库」、断言 5（点卡片⊕+吐司）整段下线，头注释同步。
- 验证：`build` BUILD SUCCEEDED；UI 回归 testB + testItemWithoutSpecsStillInserts。
- ⚠️ 环境坑：本轮 Edit 工具多次「返回成功但内容未落盘」（同批次部分成功部分丢失），且上轮限频中断在 `MidsummerSeriesListView` 留下过**重复的函数定义**。教训：批量编辑后必须 `grep`/`git diff` 复核实际落盘内容，不能信任 Edit 成功回执；发现编译「用法在、定义没了」的错位状态时先 sed 读文件真身再补刀。

## 商品表单第3步改版 + 系列删除（2026-09-18 第九轮）
- 用户需求：①框一改名「商品名」；②框二改「颜色分类/款式」逐款录入（每款必选尺码+独立价格+右侧独立图片上传）；③款式下自动生成该款尺码表；④最终展示对齐樱花小羊规格面板；⑤创作者页面加系列删除按钮。
- 数据层：`MidsummerListingStyle` 加 `sizes: [String]?`（逐款尺码，nil=旧档回退）；`makeItemDTO` 的 SKU 表改为「款式×尺码」逐组合写一条（约束款式组+尺码组）——规格面板选中款式后只剩该款尺码可点，与樱花小羊一致。
- 表单（`MidsummerListingFormView`）：`MidsummerListingStyleDraft` 加 `priceText`/`sizes`；`styleRow` 重写为 `styleCard`（名称+价格在左、PhotosPicker+删除在右、该款尺码 chips、有图+有尺码时自动渲染 `styleSizeTable` 尺码×价格表）；第3步卡序改为 **尺码池在前**（先备好尺码，逐款录入时立即点选，新加款式自动预填池内全部尺码）；逐款价发布时回退 `preorderPrice ?? expectedPreorderPrice`；`listing.sizes`/系列 sizes 取逐款并集；校验新增「款式 X 请至少选择一个尺码」「逐款价须为整数」。
- 展示对齐（图二）：规格面板本就从 `variantImageNames`+SKU 渲染款式缩略图与逐款价——表单把逐款价写回 styles 后自动达到樱花小羊效果，无需改面板。
- 删除系列：`CreatorOperation` 加 `seriesDelete`；`MidsummerCustomSeriesStore.remove(_:)`（requireCreator + 删封面图）；`MidsummerSeriesDetailView` 加 `deleteSeriesSection`（仅 `midsummer-custom-` 前缀+创作者可见，confirmationDialog 确认，先逐条 `listingStore.delete` 再 `remove`，`onSeriesDeleted` 回跳由宿主 `goBack()` 接）；Store 订阅自动重算 catalog。
- 测试适配：流程用例改为先选尺码池再加款式；`listing-style-row-0`→`listing-style-name-0`；⚠️ `scrollToElement` 原来先 `guard element.exists` 再滚动——**懒容器（LazyVGrid）元素滚进视口前 exists==false**，导致首屏外款式 chips 被判不存在；改为「边滚边查」后修复。此坑已写注释。
- 验证：build SUCCEEDED；`testPublishListingAndInsertToWardrobe`/`testEditListingPriceInWorkspace` 单跑通过（enterSeries 一次超时为机器负载抖动）。
- 收尾验证：6/6 上新流程 UI 用例全绿；ItemManagerTests 非宠物 0 失败；build SUCCEEDED。⚠️ 新坑：SwiftUI 给 VStack 卡片容器挂 accessibilityIdentifier 会**覆盖全部子元素的 identifier**（listing-style-card-0 覆盖名称/价格输入框）——卡片容器一律不要挂标识，子元素各自挂。

## 表单第3步删两个区块（2026-09-18 第十轮）
- 用户截图框选删除：①商品分类/商品名 chips 区（`kindSelectionSection`，listing-kind-*）；②「商品名（自动生成，可修改）」输入区（`autoNameRow`，listing-name）。其余不动。
- `MidsummerListingFormView.swift`：删这两个区块 + `nameBinding`；`categoryStyleCard` 只剩 `stylesSection`（hint 同步去掉框一描述）；删随输入框失效的两条校验（请填写商品名称 / 请至少选择一个商品名）；`draft.name`/`kinds` 字段保留（款式名拼接、落库、确认页汇总仍用），商品名恒由 `syncAutoName()` 取首个款式名。
- 测试适配（`MidsummerListingFlowUITests.swift`）：4 个用例去掉输入商品名的步骤；断言改用自动名（樱花小羊系列=「现 sk 粉色」，跨系列=「OP 白色」）。
- ⚠️ 新坑：工作台是卡片 sheet，背后系列详情页有同口径价格总表——同款商品在两层各有一行同名 label，`firstMatch` 会命中被 sheet 盖住的底层行（not hittable，坐标点击落在蒙层上）；且卡片 sheet 内可见元素的 `isHittable` 也会误报 false。解法：按「frame 在屏幕内」挑行 + `coordinate(...).tap()`。已写进用例注释。
- 验证：全文件 6/6 流程用例绿（TEST SUCCEEDED，708s）。
