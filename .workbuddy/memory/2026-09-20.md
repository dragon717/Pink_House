
## 2026-09-20 店家上新 Phase 2-6 全部落地
- Phase 2（只读浏览）：Services/ShopCatalog/ShopCatalogStore.swift（加载/索引/查询/上新窗口状态）+ Views/ShopCatalog/ 三个视图文件（店家列表/主页/系列详情/商品详情/大图查看器）+ Resources/ShopCatalog/shop-catalog.json 演示种子（Alice Girl 雪国来信等，图片复用 TimeHall 画册图）；入口=TimeHallBrandListHome 顶部 ShopCatalogEntryCard → fullScreenCover。
- Phase 3（图片）：大图查看器加多选批量保存（§14）。
- Phase 4（衣橱联动）：ShopCatalogWardrobeInserter.swift — makeDraft（现货/预约/心愿三口径）、makeSetDraft（主衣物 JSK/OP/SK/Blouse 决定主类型，小物写入 accessories 栏，多主衣物冲突检测 §23）、insert 复用 TimeHallWardrobeQuickInserter 并回写 catalogProductID/VariantID/SaleEventID；系列页多选加入衣橱 UI。
- Phase 5（心愿/尾款）：商品详情操作区（加入心愿/我已经预约/加入少女衣橱，按 catalogProductID 查重防重复）；ShopCatalogReservationSheet 预约记录页（定金可改、尾款自动算、尾款时间=预约结束日）。
- Phase 6（运营端）：ShopCatalogOps.swift — CatalogProductDraft、ShopCatalogTaobaoParser（分享文本解析标题/价格/定金尾款）、ShopCatalogDraftValidator（校验+店家别名/系列/商品去重+现货追加）、ShopCatalogDraftStore（草稿持久化 + 发布写覆盖层 shop-catalog-override.json）；ShopCatalogStore.reloadWithOverlay 合并覆盖层；运营页 ShopCatalogOpsView（CreatorAccess.isCreator 门控 + requireCreator(.listingPublish) 服务层校验）。
- 测试：ShopCatalogStoreTests 9/9 + ShopCatalogPhase456Tests 10/10 全过（中途修：wishlist 价格口径断言=现货优先 568、FlowChips 重名、Combine 导入、元组 !== 等）。冒烟：模拟器 iPhone 18 Pro 安装启动正常（bugod2.ItemManager，截图 output/店家伙家上新_phase2-6_冒烟截图_20260920.png）。
- 未做：真实淘宝页面抓取解析（现为分享文本正则）、运营审核多态（白名单即发布）、UI 自动化点击验收（XCUITest）留待下轮；改动未提交 git。

## 2026-09-20 时光馆重构实现方案（文档产出）
- 用户上传《少女心愿上新开发1.0版本.txt》（V1.1-Dev），要求参照少女心愿模式重构时光馆。
- 并行 Explore 摸清现状：TimeHall（3469 行巨石 TimeHallView、V3 DTO Bundle 种子、CloudKit 只读静态分发建好待用、三套品牌身份、Rebuild 半成品）+ ShopCatalog（七实体 Catalog + Clothing 3 引用字段 + 草稿状态机 + 白名单运营，Phase 2-6 已落地）。
- 产出 docs/时光馆_店家上新模式重构实现方案.md：定位=时光馆首屏即店家上新（复用 ShopCatalog 零新增模型）；旧 pinkHouse 编年史 + Midsummer 数据一次性迁入 Catalog（productCode 作 id 种子、SaleEvent 追加、收藏迁心愿）；TH 发布协议保留为 Phase 5 云分发底层；Phase 0-5 分阶段（基线→首屏切换→数据迁移→旧管道下线→清理→云分发），含 MVP 验收、风险回滚。仅方案，未动代码。

## 2026-09-21 编译修复：店家上新衣橱入库两处报错
- ShopCatalogWardrobeInserter.swift `ClothingEditDraft.with(...)`：`finalPaymentDate/End` 参数是 `Date?` 但 draft 字段非可选，改为 `?? self.xxx` 兜底（与其他字段同语义）。
- 连带暴露 ShopCatalogWardrobeMergeView.swift:261：`if/else` 后直接挂 `.frame(maxWidth: .infinity)` 报 contextual type 错，包一层 `Group` 解决。
- 验证：`xcodebuild -target ItemManager -sdk iphonesimulator`（+技能里三个沙箱开关）BUILD SUCCEEDED；未跑测试。
