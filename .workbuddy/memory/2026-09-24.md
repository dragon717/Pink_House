# 2026-09-24

## 需求批次：年月字段 / 价格表多图 / 时光馆加购图片根因修复（已完成并推送）

三项需求全部落地，实现细节见 `docs/年月字段与价格表多图与时光馆加购图片_需求实现说明.md`。

1. **年份→年月**：`CatalogSeries` 新增 `month: Int?`（Optional 零迁移）；解析/校验/展示唯一口径 `CatalogYearMonthText`（新文件 `ShopCatalogYearMonth.swift`），支持 `2026-10` / `2026年10月`，纯年份仍有效（存量回填兼容）；非法输入红字拦截不落库。年份筛选 chip / 排序不动。
2. **价格表多图**：`CatalogPriceChart` 新增 `sourceImages: [String]?`，`sourceImage` 恒=首图（单一展示/门禁口径不动，红线不破）；`ShopCatalogImagePickerButton` 升级系统多选（max 10），`.replace` 多选取第一张；录入文本每行一个引用，拆分口径唯一 `CatalogPriceChartImageText.references(fromText:)`；详情页第 2 张起由 `extraPriceChartImages` 补充展示，全屏查看器带全部原图。
3. **加购图片根因**：根因 = `ShopCatalogWardrobeDraftBuilder.makeDraft` 硬编码 `imagePaths: []`（ShopCatalog 与衣橱是两套图片体系：`local:`/Bundle 引用 vs ImageManager 文件名）。修复 = 新增 `materializeImagePaths`，按详情页同口径（`store.asset(id:)?.originalURL ?? 原值`）把商品图**复制**物化成 ImageManager 文件（上限 6 张，http 跳过，单张失败不阻塞）。注意 `product.images` 存的是 **CatalogAsset id** 不是直接引用。

## 验收

- `build-for-testing` → TEST BUILD SUCCEEDED；`-only-testing`（类名）6 类 73 例 0 失败，逐类核验过真实执行（防假绿）；中途改的 `ShopCatalogBatchDetailView.swift` 确认在 test run 里重编（grep 日志 4 行）。
- 新增测试：`ShopCatalogWardrobeEntryTests.testMakeDraftMaterializesProductImagesIntoWardrobe`（25 例）、`CatalogYearMonthTextTests`（12）、`CatalogPriceChartMultiImageTests`（4）。

## 提交

- `8b58d54c` fix: 启动图 LaunchSplash（pbxproj/Info.plist/素材/排查文档）
- `95e5c0a6` feat: 店家上新批次需求（77 文件，含此前多轮已完成未提交的工作：需求N、发售阶段、价格表精简、款式同步、删除守卫等）
- 已推送 `origin/main`（a4bc894d..95e5c0a6）。

## 踩坑补充

- 无新坑；skill `pink-house-xcodebuild-acceptance` 既有条目全部命中（UDID destination、类名 only-testing、BSD grep 空交替、编译期改文件后 grep 日志核验重编）。

## 补充批次（00:52）：表头「裙装名称」+ 查看器黑屏根因修复

- **表头**：`structuredTable` 加 `labelWidth` 参数（默认 64 不变，尺码表卡不受影响）；价格表卡传 `cornerLabel: "裙装名称", labelWidth: 96`——4 字表头与常见款式名单行放下。
- **黑屏根因**：截图计数器「1 / 0」= `references.count == 0`。根因在 `ShopCatalogSeriesMenuView`（系列点菜页）点缩略图**无条件** `viewerProduct = p`，无图商品（种子 12/109 无图）→ 查看器零页面纯黑。双层修复：① 调用点 guard 非空才打开；② `ShopCatalogImageViewer` 空数组渲染「暂无可查看的图片」占位（防未来调用方），计数器空时隐藏。`ZoomableImage` 加载失败本就走占位图，非黑屏来源。
- 提交 `f9f92b08` 已推送。验证边界：build-for-testing 0 错误 + 两个改动文件确认本轮重编；纯视图层改动未新增单测。
