# 2026-09-15

## 仲夏物语三项改造：主题统一 / 选中可切换 / 全系列归集

使用者一句话提了三件事，都是「照现有规范做，不要另起炉灶」。

### 1. 主题样式统一

先做的不是改代码，是把「哪些功能要适配、各对应哪条现有规范」列成表，
写进 `docs/MIDSUMMER_THEME_ADAPTATION.md`。11 项功能，只用 6 个既有槽位，
**一个都没新增**：

| 功能 | 槽位 |
|---|---|
| 顶栏容器 / 图标按钮 / 上传上新 | `topBarMain` / `topBarIconButton` / `topBarAddButton` |
| 年份栏 / 系列 chip / 规格选项 | `filterChip`（与 `MultiDimensionalFilterSheet` 同构） |
| 规格抽屉 | `filterSheet`（与筛选面板同槽位——同一种形态） |
| 卡片 / 步进器 | `sectionCard` + **fallback 背景** |
| 主按钮 / 圆形图标按钮 / 空状态 | `primaryButton` / `iconCircleButton` / `emptyState` |

**`MidsummerTheme` 全部改成深浅色自适应**，`adaptive(light:dark:)` 用
`UIColor` 的 trait provider 而不是 `@Environment(\.colorScheme)`——
几十处调用点一行没改就获得深色支持，快照测试里 `ImageRenderer` 带的
`\.colorScheme` 也能让取值正确。补齐 `surface` / `scrim` / `subtleFill` /
`onAccent` / `shadow` / `divider`，把视图里所有 `Color.white`、
`Color.black.opacity(…)`、`.white` 换掉（只剩令牌定义里还有）。

**三个刻意不接的地方**（写下来免得下次又改回去）：
- 底部抽屉**不用** `themeSkinAdaptiveSectionCard`——它会把四角都圆掉，
  抽屉下沿贴着屏幕边，圆角会在屏幕最下方露出页底色。只取 `.filterSheet`
  的深色可读性背板，版式一行没动。
- 上传页的系统 `Form` / `Section`：分组底色由 UIKit 提供，本来就跟随系统深浅色，
  硬套只会把原生列表变成自绘卡面。
- 抽屉容器**不打** `accessibilityIdentifier`：容器标识会覆盖直接子元素的标识
  （`spec-confirm` 曾因此从层级里消失）。

### 2. 一键入库：选中可切换

规则：**点已选项 = 取消选中**，款式 / 颜色分类 / 尺码三组同一条，
且允许一组都不选。

- `MidsummerSpecResolver` 新增 `toggling(groupID:optionID:in:of:)`。
  注意不能靠 `selecting` 加 if 实现——`selecting` 是恒定赋值，
  取消语义必须显式 `removeValue`。
- 取消后照样过 `normalized`：少一组约束只会让组合更宽松，
  所以**其它组已选不会被牵连清掉**（点掉尺码不会弄丢刚挑好的配色）。
- **确认按钮不再禁用**。既然允许不选，禁用就等于把规则作废——
  取消选择之后反而卡在面板里出不去。缺的规格由 `wardrobeMapping`
  回退到单品自带配色 / 尺码。提示语从「请先选择 X」改成
  「未选 X，将按商品默认信息入库」。
- 面板里加一行可发现性提示 `spec-toggle-hint`：以前点已选项只是重选同一个值，
  看上去像没反应。

### 3. 系列合并：30 → 13，每个系列 = 1 个商品

上一轮只归集了樱花小羊。这次**全部 13 个系列归集完毕**。

**粒度怎么定的**（这是最关键的决定）：原来的**每条单品降级为款式组的一个选项**。
一条记录本来就代表一个链接 / 一款，不再往下切分——
不把「切替 JSK / 斜襟 OP / 蛋糕开衫」拆成三款，因为来源把它们并在同一个价里，
拆开就是编数据。

- SKU 只在该款**自己**登记的颜色 / 尺码范围内生成，不假装有全交叉
  （卢瓦尔内搭只有生成色，就不会凭空多出酒红）。
- 款式名默认去掉系列名前缀；前缀对不上时（`小熊博物馆` / `比得兔`）
  用 `STYLE_NAME_OVERRIDES` 显式指定。
- 价格挪进 SKU 逐款标注并带 `priceKind`。**新增 `.balance`（尾款）口径**——
  卢瓦尔 400/160、小熊生日 152/28 都是尾款，塞进「参考价」会让使用者按全款估预算。
- 定金无法归到某一款 → 留在系列级 `depositMin` / `depositMax`
  （野草莓初代 47、比得兔 JSK 75）。
- 重建脚本 `scripts/rebuild_midsummer_series.py`，幂等。
  跑之前先 `cp` 备份（这次备份在 `/tmp/midsummer-series.bak.json`）。

结果：比得兔 6 → 1（¥160–554 + 定金 ¥75 都保住了）、草莓肥啾 5 → 1、
蝴蝶结 4 → 1、野草莓 3 → 1。

### 顺手修掉的一个真 bug

`matchedSKU` 要求每组都对上，而 `isAvailable` 说「SKU 没提及的组不参与约束」——
**两条规则自相矛盾**。后果是归集商品里「小物」这类**没有尺码**的款
（它的 SKU 只有款式 + 颜色）永远命中不了自己的 SKU，
明明写了价却退回「价格待补充」。改成与 `isAvailable` 同一条规则。

### 测试守卫（76 例全绿）

新增 9 例：切换选中 4 例（取消 / 重选 / 全空 / 来回切）、
SKU 匹配忽略未提及组 1 例、SKU 有价必带口径、归集商品必带采集日、
同系列多商品必须有各自链接、每个款式都要有 SKU。

### 本轮踩的坑

- **`rangePrefix` 未定义**：上一轮改 `priceView` 时留了个悬空引用，
  直到这次编译才发现。改视图时如果引用了还不存在的计算属性，
  一定要在同一轮里补完。
- **`grep -E "30|34"` 在中文 md 上返回空**：Bash 工具的 grep 对某些中文文件
  匹配异常，改用 Grep 工具就正常。遇到「明明有却搜不到」先换工具。

### 还没闭环

- **只有樱花小羊有真实 `itemURL`**，其余 12 个系列的归集依据是
  「同一系列 = 同一链接」这一**近似**。野草莓初代与 2.0 是两代、很可能是两个链接，
  已写进该商品 `priceNote`，拿到真实链接后要回来复核
  （守卫会报错提醒）。
- 归集后 `kind` 统一为 `set`，App 里没有按 `kind` 过滤的入口所以不丢功能，
  但「含 N 个款式」才是准确描述。
- CloudKit 规格读写仍按「无规格」处理，创作者上传的条目不带款式 / SKU。
- AGENTS.md 里的 `iPhone 16 Pro` 模拟器已不存在（现在用
  `iPhone 18 Pro,OS=27.0`），仍未同步。

## 2026-09-15 商品素材采集器（scrapers/）

- 调研开源方案（MarketSpider / pachong-cursor / floatin xianyu_spider / goofish mtop），选定 Playwright + 登录态注入方案
- 新建 `scrapers/`：collectors（taobao 含天猫 / xianyu）、common（http_client / image_downloader / storage）、run_collect.py CLI、login_helper.py 扫码登录
- 输出结构：`scrapers/data/runs/<ts>_<平台>_<关键词>/items.json` + `scrapers/assets/images/<平台>/<关键词>/<商品ID>/NNN.jpg`，`data/index.json` 汇总索引
- 已验证：语法检查、CLI --help、alicdn 图片下载落盘（31KB JPEG）✅；页面抓取首次实跑可能需按平台改版调选择器
- 用 venv：`/Users/sangyu/.workbuddy/binaries/python/envs/default/bin/python`

## 2026-09-15 时光馆运营白名单加人

- 使用者贴 CreatorGate 日志：本机 iCloud 标识 `_8a195c73786fc49283cc3d180dbf50bb` 判定 denied
- 白名单唯一入口是 `NoticeCloudKitService.adminIDs`（`ItemManager/Services/NoticeCloudKitService.swift`），已追加该 ID 并注明来源
- 编译验证：`xcodebuild -project ItemManager.xcodeproj -target ItemManager -sdk iphonesimulator`（按 skill 加三个绕沙箱开关），`** BUILD SUCCEEDED **`，退出码 0。只编译主 target，未跑测试
- 遗留：日志里 `⚠️ [Midsummer] 公共库读取失败…Did not find record type: MidsummerSeries` 与白名单无关，公共库缺 `MidsummerSeries` record type，未处理
- 提醒过使用者：界面闸门放行了，CloudKit Console 的 Security Roles 若未授权该账号，写入仍会被拒

## 2026-09-15（续）爬虫根因排查与修复 ✅ 已跑通

根因链（图片为 0 的三层原因）：
1. Playwright chromium 从未安装 → 采集器启动即崩（已装 chromium + headless-shell）
2. 无登录态：淘宝滑块验证码拦截 / 闲鱼搜索结果不渲染 → 用户已扫码生成 cookies/taobao_state.json 与 xianyu_state.json
3. headless-shell 被识别"非法访问" → 修复：channel="chromium"（新无头）+ STEALTH_JS（抹掉 navigator.webdriver 等指纹）写入 common/base.py

其他修复：login_helper 改为轮询 cookie 自动检测登录（input() 在沙箱不可用）；mkdir(exist_ok) 在 shim 下抛 EEXIST 的坑；闲鱼标题被站点名覆盖 / 描述混入导航前缀；详情页间歇性风控加冷却重试。

验证结果：双平台真实采集 4 个 run（JK制服/洛丽塔/水手服），136 张图 7.3MB 落盘 assets/images/，标题价格描述齐全，已目视确认是真实商品图。注意：闲鱼详情页风控是间歇性的，重跑需冷却。

## 2026-09-15（续2）链接规范化

- 淘宝/天猫 detail_url 统一为 https://item.taobao.com/item.htm?id=<ID>（无跟踪参数）；旧链接（detail.tmall.com、带 skuId/spm 的搜索链接）降级存 source_url 留档
- 改动：collectors/taobao.py 新增 canonical_item_url()，搜索卡片 detail_url 走规范化地址；README 记录链接规范
- 迁移已生成数据 8 条 detail_url（JK制服/洛丽塔 两个 taobao run）；闲鱼仍用 goofish.com/item?id=
- 跳转实测 2/2 PASS：id 保留、无登录跳转、页面标题与 items.json title 一致

## 2026-09-15（续3）上新咨询双方案设计

- 产出 docs/上新咨询双方案设计.md：方案A「上新速报」（客观快讯流，阶段驱动）+ 方案B「种草手帐」（搭配内容流，场景驱动），共用 Midsummer DTO，漏斗 B种草→心愿→A交易→衣橱闭环
- 资料机制：单品尺码表+系列价格表优先走 scrapers/ 淘宝详情采集（尺码表=详情页 alicdn 图），缺则白名单运营者经 MidsummerContributeView 补录（三入口：规格抽屉/系列菜单/深链）
- 关键复用点：Clothing.sizeChartImagePath 字段已存在、PhotosPicker+1200px 压缩管线、MidsummerWardrobeInsertion 一键入库、isAdminUser 白名单门控
- 待办：报错文案改"款名"（P0）、全款预约口径+入库映射（P2，涉及 CloudKit）

## 2026-09-15（续4）双方案 P0/P1 落地

- P0：MidsummerContributeView .noItems 文案改为"请至少填写一个单品的款名…"（与真实校验一致，测试断言 contains("单品") 仍通过）
- P0：补录入口两处——①MidsummerSeriesDetailView summaryCard「补录尺码表/价格表」按钮（isAdminUser 门控）→ sheet 打开 MidsummerContributeView(existingSeries:) 补录模式；②MidsummerSpecPanel 底部缺尺码/价格单品的运营者补录入口（同门控）
- P1：系列详情页新增「价格总表」区块（priceTableSection）：现货价/全款预约/定金尾款预约三组，全部从单品 price/deposit/balance 派生，不另存数据
- P1：scrapers 侧——taobao collector 新增 size_chart_urls 提取（desc/detail 容器内 alicdn 图，候选制）、BaseCollector.save_size_charts 落盘（sizechart_ 前缀）、export_midsummer.py 把 items.json 映射为 midsummer_import.json（kind 猜测/价格归类/规范 itemURL），已用 JK制服 run 实测生成 5 单品
- ⚠️ 构建受阻（环境级）：本机 Xcode 27.0 (27A266a) + macOS 27 的 SwiftPM 包解析 sandbox_apply 被系统拒（含非沙箱运行、IDEPackageSupportDisableSandbox、SWIFTPM_DISABLE_SANDBOX 均无效），xcodebuild 在解析阶段即失败，非本次改动引起。已用 swiftc -parse 通过语法验证；类型级验证与模拟器验收待构建环境修复后补跑

## 2026-09-15（续5）双方案代码对齐修复 ✅

用户反馈"方案未生效"，排查结论：P0/P1 UI 代码在（报错文案/两处补录入口/价格总表），但方案核心的数据链路根本没实现——DTO 无尺码表字段、详情页无尺码表展示、入库不映射 sizeChartImagePath、表单无尺码表上传。且 xcodebuild 从未成功过（SwiftPM 包解析 sandbox_apply 被拒，-disableAutomaticPackageResolution 也绕不过），实测的 App 是旧二进制。

本轮补齐（代码 ↔ 方案对应）：
- §四.1 MidsummerItemDTO.sizeChartImageName（var+默认值，旧 JSON/记录向后兼容）← MidsummerModels.swift
- §四/§五 CloudKit：publish(item:sizeChartImage:) 新增 sizeChartImage 资产字段；fetchSeries 读时把 CKAsset 复制进 ImageManager Images 目录（midsummer-sizechart- 前缀）← MidsummerCloudService.swift
- §四.2 makeDraft 传 sizeChartImagePath → TimeHallWardrobeQuickInserter 落 Clothing.sizeChartImagePath（衣橱零改动）← MidsummerWardrobeInsertion.swift
- §一.2 MidsummerItemDetailSheet 新增 sizeChartSection（DisclosureGroup，有图展开即看/无图如实"待补充"）← MidsummerBrandView.swift
- §五 DraftItem 加 sizeChartPhotoItem/sizeChartImage/sizeChartLoadError + itemsSection 单品级 PhotosPicker + submit 配对上传（顺带修了补录模式重发旧单品会清掉其 coverImage 资产的隐患）← MidsummerContributeView.swift

验证：swiftc -typecheck 全量 427 文件（两步：先编 GoogleGenerativeAI 模块再 typecheck，-I 指向模块），改动文件 0 错误；仅 iCloudSyncManager.swift 48 个 @Observable 宏加载错误（裸 swiftc 环境限制，与改动无关）。xcodebuild 仍需 Xcode GUI ⌘B 验证。

未实现（需向用户说明）：深链入口③（App 无 URL 路由基础设施，无 onOpenURL/CFBundleURLTypes，需 Info.plist+根路由，建议并入 P2 统一路由再做）；方案B手帐流+心愿单（P2，涉 CloudKit 新记录类型）；全款预约口径已通过派生达成（makeDraft deposit=总价/balance=0 → Clothing.isFullPaymentReservation 自动成立，无需新字段）。CloudKit Console 需给 MidsummerItem 加 sizeChartImage 字段（dev 自动建，production 要 promote schema）。

## 2026-09-15（续6）单品独立图片上传入口 ✅

用户要求：每个单品独立图片上传入口（不共用）+ 全环节排查 + 已有无图单品预留补录位。

排查发现：单品图此前完全没有独立入口（表单只有尺码表图 per-item，单品图只有系列级封面；publish(item:) 支持单品图资产但从未被调用、fetch 也从不回填 item.coverImage，系列列表行永远显示系列封面）。

改动（5 个文件）：
1. MidsummerContributeView：DraftItem 加 itemPhotoItem/itemImage/itemImageLoadError；itemsSection 每个单品独立 PhotosPicker「单品图片」+ 预览；loadSizeChart 泛化为 loadDraftImage；submit 配对 (item, coverImage, sizeChartImage) 逐单品上传
2. MidsummerCloudService：fetch 时 copyItemCoverAsset（midsummer-item- 命名空间）回填 DTO.coverImage；makeItem 加 coverImageName 参数
3. MidsummerBrandView MidsummerItemDetailSheet：顶部封面 displayItemCoverName（本会话补录 > 云端 > 系列封面）；新增 itemImageSection（有图展示/无图"待补充"+ 运营者专属「补录/更换单品图」PhotosPicker）；uploadItemImage 走 publish(item:coverImage:) 整条重写（DTO 字段齐全不丢数据）+ 本地落盘乐观回显 + refreshFromCloud
4. MidsummerSeriesListView itemRow：单品图优先 item.coverImage ?? series.coverImage
5. MidsummerSpecPanel：needsSupplement 加 item.coverImage == nil；文案改"缺单品图、尺码表或价格"

验证：swiftc -typecheck 全量，5 个改动文件 0 错误；仅 PetDataManager.swift 6 个预存 actor 隔离错误（与本轮无关，未触碰该文件）。

## 2026-09-15（续7）商品上传按千牛发布路径重做 ✅

用户反馈单品上传实现质量差，要求按千牛商家后台「发布宝贝」路径重做。核心交互对齐：每商品 5 格主图宫格、第 1 格主图、逐格添加/删除/点图重传/设主图、满格隐藏添加位、发布时逐商品进度。

改动：
1. MidsummerModels：MidsummerItemDTO 加 var galleryImageNames: [String]?（附图文件名，主图仍存 coverImage；var+默认值向后兼容）
2. MidsummerCloudService：publish(item:images:sizeChartImage:) 新签名——images[0]→coverImage 资产、images[1..4]→galleryImage2..5 字段；fetch 加 copyItemGalleryAssets 回填；修正中途引入的 record["balance"] 缺 = 笔误
3. MidsummerContributeView：DraftItem 改 itemImages: [UIImage]（≤5）+ itemAddPhotoItem/itemReplacePhotoItem/itemReplaceIndex；itemsSection 重做为 itemImageGrid 宫格（+ 添加格 88pt 虚线框/主图角标/× 删除/设为主图移到首位/点图 simultaneousGesture 记录格位后替换）；submit 三元组 (dto, images, sizeChart) 逐单品上传 + uploadStatusText 进度
4. MidsummerBrandView MidsummerItemDetailSheet：重做为宫格展示（主图角标+附图）、运营者 multi-select 追加（maxSelectionCount 补满 5）、单张删除、都走整条重写（本地已有图 loadImages 读回随 DTO 一起传）、乐观回显 uploadedImageNames、refreshFromCloud

验证：swiftc -typecheck 全量，4 个改动文件 0 错误；仅 iCloudSyncManager @Observable 宏环境错误（已知，与改动无关）。

## 2026-09-15（续8）「改动未生效」核查结论 ✅

用户反馈刷新后看到的结果与上次一模一样。核查结论：**代码改动全部在盘且自洽，App 是旧二进制**。

证据链：
- git 工作区 9 文件 832 insertions（git diff --stat 可见全部 Midsummer 改动）
- 关键符号逐一 grep 确认：galleryImageNames(MidsummerModels:253)、itemImageGrid/itemImages/itemAddPhotoItem(ContributeView:312/764/766)、copyItemGalleryAssets(CloudService:325)、uploadStatusText(:50)
- swiftc -typecheck 全量复核：Midsummer 全部 0 错误，仅 iCloudSyncManager @Observable 宏环境错误（已知）
- **根因**：模拟器/DerivedData 的 ItemManager.app 二进制时间是 01:35，早于单品图/宫格全部改动（≈02:30+）；刷新 App ≠ 重新编译
- 重试构建：xcodebuild 4 种参数组合（含 -disableAutomaticPackageResolution + 既有包缓存 + skipMacroValidation）均被 sandbox_apply 系统级问题挡住；AppleScript 驱动 Xcode 被 TCC -10004 拦（自动化权限未授予本环境）
- 结论：需用户在 Xcode GUI ⌘R 重新构建运行（GUI 在 01:35 成功构建过，通道可用）；验收宫格 UI：投稿表单每单品 88pt 宫格"+ 添加主图 n/5"

## 2026-09-15（续9）上传上新/补录单品按千牛「发布宝贝」分步流程重写 ✅

- MidsummerContributeView 整页重写为四步向导：①类目与信息（阶段=类目、系列标题30字、日期、定金区间、尺码配色、简介、原文出处）→②系列主图（虚线主图位+×移除）→③单品与价格SKU（每单品白卡：款名/类型/现货价-定金-尾款/口径/备注/5格主图宫格/尺码表图）→④预览与提交（系列预览+单品清单缩略行+逐单品进度）
- 千牛交互对齐：顶部步骤条（圆形序号+连接线，完成打勾、当前橙色、可回点）、每步「下一步」先校验当前步不满足红字提示不跳步、底部固定操作栏（上一步/下一步/提交宝贝信息）、存草稿（UserDefaults JSON 文字字段+恢复/丢弃 banner，发布成功自动清）、发布成功页（✓+继续发布下一个/完成）
- 补录单品=千牛「编辑宝贝」：existingSeries 模式跳过①②直接进③（补录目标卡）→④；Validator 拆出 validateSeriesName/validateSourceURL/validateItems 分步校验，validate() 组合保持原 API（测试兼容）
- 保留：DraftItem、5格宫格交互、publish(item:images:sizeChartImage:) 提交链路、buildSeries/prefill
- typecheck：改写文件 0 错误（仅 iCloudSyncManager @Observable 宏环境错误，已知）
- 修复 MidsummerCloudService.swift:270 编译错误：MidsummerItemDTO 成员初始化器按声明顺序要求 sizeChartImageName 在 specGroups 之前，调整 makeItem 调用处参数顺序。全量 typecheck 确认仅剩 iCloudSyncManager 已知宏噪音，无其他错误。
- 修复 MidsummerContributeView 三类编译错误：①themeSkinLegibleText level .title → .hero（ThemeSkinLegibilityLevel 只有 inline/chip/badge/preview/hero）；②itemCard(item:) 参数是普通 Binding 不是属性包装器，.x 全部改为 item.x（子绑定）、读值/写值改 item.wrappedValue、onChange(of:) 传 item.wrappedValue.xxx 而非 Binding；③DraftSnapshot 与视图是同文件不同类型，private @State 不可见，系列字段 11 个 @State 去掉 private。typecheck 仅剩 iCloudSyncManager 已知宏噪音。
- 修复上传失败根因：CloudKit 不允许用空数组初始化新字段（field 'colors' ... cannot use an empty list），publish(series:) 与 publish(item:) 的 sizes/colors 改为非空才写入，读取侧本来 ?? [] 兜底。- 修复存草稿「无反应」：draftMessage 原来只画在步骤①的恢复 banner 里，其他步骤点存草稿看不到提示；改为全局提示条贴在底部操作栏上方（safeAreaInset），任意步骤可见。
- 修复提交闪退 CKException 'recordName can not be empty'：submit 里 itemDraftPairs 的 DTO id 是空串（DraftItem.makeItem 硬编码 id:""），而 buildSeries 只给内部 attachedItems 拷贝分配了 id，上传循环却用前者 → CKRecord.ID(recordName:"") 抛 ObjC 异常。修复：buildSeries 改返回 (series, newItems)，上传循环 zip(newItems, itemDraftPairs) 用带 id 的 DTO 配对图片；CloudService publish 两个入口加空 id 防御（转 MidsummerUploadError.other 可捕获错误）。
- 修复发布页图片「上传了却不显示」：makeAsset 原来只写临时目录给 CKAsset，乐观更新 DTO 里又没图名。现在 makeAsset 同时在 ImageManager Images 目录落持久副本（midsummer-upload- 命名空间）并返回文件名；publish(series:)/publish(item:) 改 @discardableResult 返回本地图名；submit 回填 coverImage/galleryImageNames/sizeChartImageName 后再 applyUploaded，详情页/列表立刻有图。- 草稿升级：DraftSnapshot 图片落盘（midsummer-draft- 命名空间）快照存文件名，恢复时读回 UIImage（含封面、宫格、尺码表）；草稿同时只存一份——保存新草稿/丢弃/发布成功前都 purge 旧图文件。coverImage @State 去掉 private 供 DraftSnapshot 写入。
- 修复 MainActor 隔离报错：ImageManager 整类 @MainActor，makeAsset 从 nonisolated static 改回类内 MainActor；DraftSnapshot 的 draftImageURL/saveDraftImage/loadDraftImage/purgeDraftFiles/init(from:)/restore(into:) 全部标 @MainActor，用 StrictConcurrency 复验通过。
- 修复 purgeDraftFiles 静态方法误引实例属性：if let coverFile → if let cover = snapshot.coverFile。StrictConcurrency 全量 typecheck 通过。
