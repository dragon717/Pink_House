# 2026-09-26 工作日志

## 公共数据库字段配置 + iOS 运营上传（两份方案实施）

按 `docs/Pink_House-公共数据库字段配置实施方案.md` 与 `docs/Pink_House-iOS运营上传实施方案.md` 落地。

### 发布端（Python，`tools/time_hall/publication/`）

- `protocol.py`：新增 `MEDIA_MIME_ALLOWLIST`（jpeg/png/gif/webp/heic）、`MAX_MEDIA_BYTES`（20 MiB）、
  `MEDIA_REFERENCE_PREFIX`；新增 `validate_shop_catalog_media(doc, known_media_hashes)` +
  `_shop_media_references` —— 补上方案 §2.3 点名「必须补上的发布前错误」：包内每个
  `mediaKey` / `thmedia:` 引用必须命中本次会上传的 THMedia，残留 `local:` 一律判失败。
- `build_release.py`：`load_media` 校验 manifest 三键（mediaKey/fileName/mimeType）、
  `mediaKey == 文件 SHA-256`、MIME 白名单、字节上限；`collect_shop_catalog_media` 同套约束 +
  改写时写入 canonical `CatalogAsset.mediaKey`（取原图摘要）；主流程加资产引用校验。
- `selftest_shop_catalog.py`：新增 7 条断言（mediaKey 写入 / 悬空引用 / 残留 local: / 非法 hex）。
  全绿。

### iOS 消费端（`ItemManager/`）

- `CatalogAsset` 新增可选 `mediaKey`（旧包缺键照常解码），`originalURL` 保留为来源/回退。
- `ShopCatalogSyncProtocol.resolvedMediaKey(_:fallbackReferences:)` 成为**唯一解析口径**
  （canonical `mediaKey` 优先 → 回退 URL 里的 `thmedia:<hash>`）；`ShopCatalogAssetImage`
  新增 `mediaKey` 参数，商品轮播 / 衣橱合并页已传入。
- `ShopCatalogCloudSyncValidator.compress(_:)`：新增 gzip 压缩（RFC 1952）+ 自写 CRC32 表；
  mtime 固定 0 保证同内容同字节（内容寻址的数据包不能因为「只是重发了」就换 hash）。
  ⚠️ 第一版把「2 字节 zlib 头 + 4 字节 Adler32 尾」剥掉了，产物全部解不开 —— 见下方验尸要点。

### iOS 运营上传（新文件）

- `Models/ShopCatalog/ShopCatalogUploadJob.swift`：`@Model` 任务表 + 阶段/状态枚举 +
  `ShopCatalogUploadPolicy`（MIME 白名单、20 MiB、5 次上限、指数退避）。
- `Services/ShopCatalog/ShopCatalogOpsUploadStore.swift`：**独立** ModelContainer
  （`cloudKitDatabase: .none`，方案 §2.1 硬要求；主容器开 iCloud 时是 `.private`）。
- `Services/ShopCatalog/ShopCatalogOpsMediaStaging.swift`：扫 `local:` → 算 hash/MIME/字节数 →
  改写为 `thmedia:` + 回填 `mediaKey`（字段清单与 Mac 端逐字段对齐）。
- `Services/ShopCatalog/ShopCatalogOpsCloudWriter.swift`：THMedia/THDataPack/THRelease 写入；
  发布头用归档系统字段（changeTag）做条件更新；CKError → blocked/retryable/conflict/failed。
- `Services/ShopCatalog/ShopCatalogOpsPublisher.swift`：全流程编排 + 只读端回读验证。
- `Views/ShopCatalog/ShopCatalogOpsUploadSection.swift` + 挂进 `ShopCatalogOpsView`。

### 验尸要点（下次别踩）

- ⭐ **`NSData.compressed(using: .zlib)` 给的是裸 deflate（RFC 1951），不是 zlib 封装。**
  名字骗人。2026-09-26 实测（macOS 26）：输入 0B→2B `03 00`；1B→3B `73 04 00`；
  512B→20B 首字节 `0x7B`。首字节从不是 `0x78`。20 字节恰等于 Python
  `zlib.compress(src,9)` 去掉 2+4 封装后的 deflate 段长。
  **所以绝不能剥「2 字节 zlib 头 + 4 字节 Adler32 尾」** —— 那会把 deflate 数据本身截断，
  产物必然 `deflate 解压失败 / Cocoa 5377`。同文件 `decompress` 的注释早就写明了这条，
  写 `compress` 时要先跟它对齐口径（我当时没看，白跑两轮 6 分钟的全量测试）。
  自检手法：`xcrun swift` 跑个 10 行探针，把产物喂 Python `gzip.decompress`，比等 xcodebuild 快两个数量级。
- 「判别是否 zlib 封装」若要写，阈值**必须 ≥ 8**（2 头 + 2 字节空 deflate 块 + 4 Adler32）。
  我第一版拍脑袋写 `>= 6`，结果空数据（2B）与 1 字节输入（3B）被判成「长度异常」直接抛错。
- `@MainActor` 的 store（持有 SwiftData 容器）进单测，**测试类本身要标 `@MainActor`**，
  只标在 publisher 那个类上不够；`job.nextRetryAt?.timeIntervalSince(now)` 是 `Double?`，
  直接喂 `XCTAssertEqual(_,_,accuracy:)` 报类型错，要 `try XCTUnwrap`。
- `ShopCatalogImageStore.url(for:)` **只拼路径、不查盘**，对不存在的文件也返回非 nil。
  直接用它会让人「文件不存在」掉进 `unreadable` 分支，而 `fileMissing` 的语义
  （沙盒重置 / 漏带图 → 重新选图）与 `unreadable`（文件在但读失败 → 重试）对运营是两回事。
  默认 `resolveFile` 已补 `fileExists` 检查。
- `ShopCatalog` 等模型有**显式 init**：参数顺序必须按声明序（sizeCharts 在 assets **之前**），
  且 `CatalogProduct` 的 `category` 无默认值。
- 会话中途**另一个 agent 会话**把领域层 `git mv` 到 `Packages/SharedCatalog` 又整体回滚，
  我的领域层改动被覆盖两次 → 本仓库并行会话活跃时，改完必须立刻 `git diff` 复核。
- 构建用独立 `OBJROOT=build/objUpload`（避免与并行会话撞 build.db 锁）。
- **别在脚本运行中途改脚本**：bash 按字节偏移增量读取，我在 `run_ops_upload_tests.sh`
  运行到一半时改了文件头，结果脚本从错位的偏移继续读 → `line 51: syntax error`，
  收尾的结论行全部没打印（xcodebuild 结果本身没受影响，但白白多一轮排查）。
- `xcodebuild` 参数里误写相对路径会生成 `ItemManager.xcodeproj/-Xcc/`（26M clang 模块缓存，
  x86_64 模拟器）。已删并加进 `.gitignore`。

### 第二轮：补完方案二 §5 的「重试入口」

核对两份方案逐条落地情况时发现唯一一处**代码内**未完成项：
方案二 §5 要求「下载失败显示明确占位**和重试入口**，不把图片当作没有图片」，
而 `ShopCatalogAssetImage` 失败后只是 `remoteMediaFailed = true`，占位图与
「本来就没图」长得一样，且**没有任何恢复路径**。

- `ShopCatalogMediaStore.clearSessionFailure(mediaKey:)`：新增生产可用的重试 API。
  只清「本次会话失败记忆」，不动已装好的缓存。
- `ShopCatalogAssetImage`：
  - 新增 `failedPlaceholder`（暖底 + 「加载失败，点按重试」+ 可点按 + a11y button 语义），
    与原有 `placeholder`（照片图标 = 本来没图）在视觉与可操作性上都区分开；
  - `content` 里插入 `else if remoteMediaFailed { failedPlaceholder }` 分支
    **排在本地兜底图之后** —— 本地 `UIImage` 能解出来时优先显示真图，不能被失败提示盖住；
  - `AsyncImage` 的 `.failure` phase 也改用 `failedPlaceholder`（原来落进 `default:` 显示普通占位）；
  - `retryLoading()` 必须**同时**做两件事：清 store 的会话失败记忆 **且** 换
    `asyncImageReloadToken`。只把 `remoteMediaFailed` 置回 false 是无效的 ——
    store 会直接返回 nil，表现成「点了没反应」。
- 新增 `ShopCatalogMediaRetryTests`（3 例，`@MainActor`）：用一个**永远失败**的
  `private actor FailingMediaReader` 数「到底有没有真的打网络」。
  走失败路径时 `download` 在 catch 里直接返回 nil、**不碰缓存目录**，所以这组用例零落盘、
  不需要 `useTemporaryForTesting()`。断言：首拉计 1 次 / 重复拉不再计数 /
  `clearSessionFailure` 后计 2 次（证明重试真的重新发请求）/ 清一个键不影响另一个键 /
  `mediaKey == nil` 零网络。

### 一轮审计结论（两份方案逐条）

- **代码内可完成的全部完成**：7 个测试类 35 例全绿（`** TEST SUCCEEDED **`），
  Python 发布端自检全绿。
- 方案一 §5.3 `minimumReaderVersion` **不提升**，理由已写进
  `ShopCatalogOpsPublisher` 的注释：`ShopCatalogSyncProtocol.readerVersion` 恒为 1，
  历史构建也都是 1（加入 `thmedia:` 支持时没跟着抬），所以这个版本号目前区分不出
  「认不认 mediaKey 的读端」；此时提升会**同等拦下所有已发布客户端**。
  要启用这条闸门必须配套：① 消费端 `readerVersion` 抬到 2 并发出新包 →
  ② 把 publisher 的默认值显式写死成 2（别再依赖 `?? readerVersion`，
  否则运营端一升级就把自己变成「只允许 2」的发布者）→ ③ 再发布 `minimumReaderVersion=2`。
- 注意：`ShopCatalogOpsMediaStaging.rewrite` 把 `local:` 改写成 `thmedia:<hash>` 时
  **连 `originalURL` 一起改写**，所以不认 `thmedia:` 的旧客户端**没有 URL 兜底**，
  商品图会全部落到占位（正文/价格仍显示）。这是 §5.3 的既定前提，不是 bug。
- 剩余全部是**代码外**、需要真实凭证的项：CloudKit Console 双环境 schema 部署 +
  Security Role、两份方案的 §6 端到端验收清单（含「普通只读账号不能写入」）。

---

## Mac 原生运营工具 P0+P1 落地（**独立 worktree** `Pink_House_macops`，分支 `feature/mac-ops-tool`）

按 `docs/Pink_House-Mac原生运营工具开发计划.md` 开发。三条口径已与用户确认：
**发布通道封装现有 Python CLI**（不写 CloudKit、私钥不进 App）、**交付范围 P0+P1**、
**共享代码用新建本地 Swift Package**。

### ⚠️ 与并行会话的分工（必须知道）

发现另一会话在同一时段改同一片区域（`ShopCatalogMediaStore` / `ShopCatalogOpsPublisher` /
`ShopCatalogOpsMediaStaging.rewrite` / `ShopCatalogStore`），且它引用了当时**尚不存在**的
`CatalogAsset.mediaKey`。经用户裁决：**我隔离到独立 worktree 继续，`mediaKey` 由对方添加、我不动**。
→ 两条线在做**重叠能力**：对方在 **iOS 侧**做 `local:` → `thmedia:` 的发布器，
我在 **Mac 侧**做离线门禁 + 待发布包（交给 `tools/time_hall/publication`）。
**合并前必须先对齐「谁负责改写 `local:`」**，否则会出现两套改写口径。

### 产物

- `Packages/SharedCatalog`（本地 SPM 包，`platforms: .macOS(.v11)/.iOS(.v14)`，
  `.swiftLanguageMode(.v5)`）：迁入 4 个文件（`ShopCatalogModels` / `ShopCatalogCloudSyncProtocol` /
  `ShopCatalogJSONCoding` / `ShopCatalogExportArchive`）+ 3 个新增
  （`ShopCatalogMediaStaging` 规范化与内容寻址、`ShopCatalogMediaJob` 上传状态机、
  `ShopCatalogPublicationGate` 发布门禁）。
- `PinkHouseOps/`（新 macOS target，bundle id `bugod2.ItemManager.Ops`，沙盒 +
  `files.user-selected.read-write` + `network.client`）：`OpsWorkspace`（编排/落盘）+
  `OpsRootView`/`Overview`/`CatalogEditor`/`MediaLibrary`/`Validation`/`Preview` 六视图 +
  SwiftData 草稿与任务台账 + 单一反馈通道的状态条。
- `ItemManager.xcodeproj/project.pbxproj`：由 `tools/patch_pbxproj_mac_target.py`
  **幂等**打补丁（`must_replace_once` 守卫）加入 Mac target 与本地包依赖。
- 迁移脚本 `Packages/SharedCatalog/Tools/{apply_migration_edits,add_public,add_public_inits}.py`。

### 验收（命令 / 状态 / 边界）

| 项 | 命令 | 结果 |
|---|---|---|
| 共享包测试 | `swift test --disable-sandbox`（cwd=`Packages/SharedCatalog`） | **46 例 0 失败**（3 个套件，0.37s） |
| iOS 主 target | `xcodebuild -project … -target ItemManager -sdk iphonesimulator …` | **BUILD SUCCEEDED**，0 error，且迁移文件本轮确实被重编 |
| Mac target | `xcodebuild -project … -target PinkHouseOps -sdk macosx …` | **BUILD SUCCEEDED**，0 error，产物已签名（entitlements 读回正确） |
| 首启冒烟 | `open -n build/sym/Debug/PinkHouseOps.app` | 进程存活、**未崩溃**；容器建出 `Application Support/PinkHouseOps/staging/<UUID>/` 与 `default.store`；草稿表 1 行、标题「2026-09-26 上新」、`coverageStatus=complete` |
| iOS 测试目标 | `xcodebuild -scheme ItemManager … build-for-testing`（`-derivedDataPath build/DerivedDataMacOps`） | **TEST BUILD SUCCEEDED**，0 error，`ShopCatalogCloudSyncTests.swift` 本轮确实被编译 |

**未做的**：Mac UI **视觉验收（无截图）** —— 只证明了编译通过 + 首启不崩 + 落盘正确。
从沙箱 shell 直接跑 GUI 二进制会被 **SIGTRAP(-5)** 杀掉（不是代码问题），必须 `open -n` 走 launchd。

---

## 第二段：合并 `feature/mac-ops-tool` → `main` + 补完剩余开发（同日稍晚）

### 合并时发现的事实

`main` 在 worktree 建好之后**前进了一个提交** `912e5139`「新增iOS运营上传发布功能」——
就是之前标记的并行会话那条线。它**恰好改动了我要搬走的那几个文件**，所以冲突是必然的：

| 文件 | main 侧 | 我这边 | 冲突 |
|---|---|---|---|
| `ShopCatalogModels.swift` | +12（`CatalogAsset.mediaKey`） | 已移入共享包 | modify/delete |
| `ShopCatalogCloudSyncProtocol.swift` | +117（`resolvedMediaKey` + gzip `compress`） | 已移入共享包 | modify/delete（git 自己跟了重命名） |
| `ShopCatalogUploadJob.swift` | 新增（`import SwiftData`） | 目录被我改名 | **file location** → git 把它塞进了共享包 |
| `ShopCatalogOpsView.swift` / `.gitignore` | 各自小改 | 各自小改 | 内容冲突（自动合并成功） |

### 三处必须手工处理（机器判不了）

1. **`ShopCatalogUploadJob.swift` 被按目录重命名搬错地方**：git 顺着我这次的目录改名
   把它放进 `Packages/SharedCatalog/Models/`，但它 `import SwiftData`（iOS 侧 `@Model`）——
   留在包里会破坏包边界并让 Mac 编译挂掉。→ 搬回 `ItemManager/Models/ShopCatalog/`。
   （`git mv` 不接受冲突路径，要 `mv` + `git rm --cached` + `git add`。）
2. **`CatalogAsset.mediaKey` 落在被搬走的文件里**：保留「已搬走」，把它以 `public var`
   并入包内副本，**并补进跨模块 `public init`**（否则 iOS 侧 new 不出来）。
3. **跨模块可见性**：自动合并带进来的 `resolvedMediaKey` / `compress` 仍是 `internal`，
   而 iOS 侧四处跨模块调用它们 → 会直接编不过。→ 两个补 `public`
   （`normalizeToRawDeflate` / `crc32` 只被 `compress` 内部用，保持 `private`）。

### 补完的剩余开发：图片引用字段清单收口（计划 §6 P4 的硬约束）

合并后**同一个概念出现了两份 Swift 副本**：Mac 门禁 `ShopCatalogPublicationGate.findings`
（6 组）与 iOS 改写端 `ShopCatalogOpsMediaStaging.ownedReferences`（4 组）——
两边注释都写着「必须与对方逐字段一致」，而这正是本仓库反复出事的模式
（新增字段只改一边 → 「改写了却没上传」/「上传了却没校验」，表现都是**数据好、图空**）。

做法：新增共享包 `ShopCatalogMediaReferences` 作为唯一清单，并区分两个标记 ——
`isRewrittenByPublisher`（文件引用，`local:` 要换 `thmedia:`）vs
`allowsAssetID`（可能是 asset id；一起改写会把 id 换成摘要、**商品图直接丢**）。
门禁 / iOS `ownedReferences` / iOS `localFileName`（含 `plan` 里第三份 `local:` 解析）全部改为消费它。
新增 `ShopCatalogMediaReferenceTests`（9 例）把字段路径**逐字写死**当回归锁。

⚠️ 收口时差点引入**纯文案回归**：拼 owner 用了 `"\(prefix) / \(label)"` 模板，
而历史口径是「带 `「」` 的名字后面**不加**空格、纯 id 后面加空格」。
owner 文案直接显示在运营界面上、也是既有用例的匹配锚点 → 已逐字还原（7 条用例红了才发现）。

### 补完的剩余开发：Mac UI 视觉验收（此前唯一缺口）

`open -n` 起的是 GUI 会话，但受限 shell 的 `screencapture` 没有「屏幕录制」TCC 授权
（`could not create image from display`）。→ 改为**应用内** `NSHostingView` + `cacheDisplay`
抓位图，绕开权限。新增 `PinkHouseOps/SnapshotHarness.swift`。

两个踩到的坑（都已写进代码注释与 skill）：

- ⭐ **触发必须走文件，命令行参数没用**：`open -n <app> --args --snapshot-dir X` 时
  App 的 `CommandLine.arguments` **收不到 X**（照常进正常流程，看起来像 harness 没编进去）。
  改成往容器写 `snapshot.request`，App 在 `applicationDidFinishLaunching` 读到即跑、
  跑完消费掉文件并退出（挂 delegate 而非 SwiftUI `.task` —— 必须在窗口真的出现之后抓）。
- ⭐ **抓图窗口必须 `.borderless`**：带标题栏时宿主视图顶部被占掉约 28pt，而 `cacheDisplay`
  抓的是视图自身 bounds → 每张快照**顶部被裁一条**（侧栏第一行、三栏表头正好落在那条里），
  看起来像布局 bug，其实是抓图的锅。

### 验收（命令 / 状态 / 边界）

| 项 | 结果 |
|---|---|
| 共享包测试 | **55 例 0 失败**（4 套件；新增 `ShopCatalogMediaReferenceTests` 9 例） |
| iOS 相关单测 | 8 个套件 **60 例 0 失败**（含 `ShopCatalogOpsMediaStagingTests` —— 被重构的那块） |
| iOS 主 target | **BUILD SUCCEEDED**，0 error |
| iOS 测试目标 | **TEST BUILD SUCCEEDED**，0 error |
| Mac target | **BUILD SUCCEEDED**，0 error |
| Mac 正常启动回归 | 无 `snapshot.request` 时进程**存活**、未被快照钩子误杀 |
| Mac UI 视觉验收 | **5 张分区快照**（2560×1680 @2x），内容全部正确；预览页**两张样例图真的渲染出来**（取图链路通） |

提交链（`main` 与 `feature/mac-ops-tool` 同一提交 `7453892e`）：
`ee837d78`（Mac 工具 + 共享包）→ `f445bf6e`（merge main）→ `cad63284`（字段清单收口）→ `7453892e`（快照 harness）。
**未 push 到 origin**（`main` 领先 origin 4 个提交）。

### 仍未闭环 / 留给下次

- ⚠️ **`local:` 改写仍是两条路径（属计划 §6 P4 的待决策项，不是重构能顺手解决的）**：
  iOS 侧 `ShopCatalogOpsPublisher`（直连 CloudKit，等于把 P2 在 iOS 实现了）vs
  Mac 侧「导出待发布包交给 Python CLI」。**字段清单已统一**，但**状态机仍两套** ——
  共享包 `MediaUploadJobState`（5 态，正是 P0 要求的）vs iOS `ShopCatalogUploadStatus`
  （多了 `mediaVerified/published/blocked/stagedFailed`）。需要用户拍板：共用同一 Publisher，
  还是把 iOS 入口降级为只读/导出。
- `MEMORY.md` 已按「红线 + 指针」重构（加了 RULES.md 分节指针、把可执行流程移进 skill），
  从 18724 字节降到约 15700（-16%）。**再压就要删红线本身**，没有继续压。
- `build/ops-snapshots/` 下留了 5 张验收截图（`build/` 已 gitignore）。

