# Live2D 少女人偶 Harness · 验收清单

## 静态资源
- [ ] `audit_avatar_assets.py` 返回 `ok: true`。
- [ ] `avatar_girl_v1_static.png` 不是透明占位，比例接近 3:4。
- [ ] `MANIFEST.yaml` 覆盖 11 个动作、6 个表情和 `magic_sticker_v1` 质量基线。
- [ ] 缺透明视频包或 Cubism 包时只给 warning，不影响静态/分层 fallback。

## 分层与视觉
- [ ] `audit_avatar_layer_edges.py` 返回 `ok: true`，并生成长发素体、短发素体、默认裙装四帧预览。
- [ ] `edge_contact_warnings` 中的头发、裙摆、袖口、鞋边残片已人工看图确认。
- [ ] 长发、短发、默认裙装不出现明显头发碎片、关节断层、层级穿帮；素体包含大臂、小臂、手、大腿、小腿、脚独立层。
- [ ] 内置发型和默认衣服继续隐藏于普通贴纸列表。

## 魔法贴纸
- [ ] 旧 `ootd_mannequin_default` 仍显示为基础线稿人台。
- [ ] “更换底图”里出现 `少女素体·长发` 和 `少女素体·短发`，且说明不含默认裙装、可叠衣服贴纸。
- [ ] 少女素体运行时默认不自动套默认裙装；衣服贴纸可放大到超过旧 2.0 上限。
- [ ] 编辑器、缩略图、预览、分享/快照走同一背景解析，快照为静态帧。
- [ ] 选择少女小人后不会清空贴纸，也不会残留图库背景。

## 动效与性能
- [ ] 魔法贴纸编辑页可手动开启动效，离页、换人台、后台、低电量时停到静态帧。
- [ ] 分层资源重复打开不重复磁盘解码，缓存命中后无明显首帧卡顿。
- [ ] idle/talkLoop 目标 24fps，手势类动作目标 30fps；缩略图和快照不跑 Timeline。
- [ ] 缺少女视频或 Cubism 包时不黑屏、不崩溃。

## Cubism Spike
- [ ] SwiftUI 可创建 `Live2DAvatarView`。
- [ ] `MTKView` 透明背景，不遮挡业务背景。
- [ ] spike shell 限制 24fps，正式 Cubism 包未交付前不作为主渲染路径。
